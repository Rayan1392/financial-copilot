# Tasks

- Add admin authorization policy.
- Add sync trigger endpoints that publish ingestion requests consumed by `005-data-ingestion-and-normalization`.
- Add sync run query endpoint.
- Add provider health endpoint using the health-check capability defined in `004-third-party-data-provider-abstraction`.
- Add integration tests for admin permissions.
