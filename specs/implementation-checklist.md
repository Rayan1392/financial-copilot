# Implementation Checklist

## Purpose

This file is the implementation control checklist for FinancialCopilot. An implementation agent must read this file before starting backend work, select the first eligible unchecked item, implement the corresponding `user-story.md` and `tasks.md`, verify it, and update the checkbox and evidence in this file.

The checklist orders delivery by dependency and product priority rather than folder number. It preserves:

- The modular monolith architecture.
- `POST /api/ai/v1/query` as the single public chat-query endpoint.
- Deterministic financial execution and versioned semantic metrics.
- Explainable results and billing-grade accounting.
- Provider-neutral AI execution.
- Future-ready platform capabilities without premature infrastructure.

## How To Use This Checklist

1. Start with the first unchecked story whose prerequisite stories are complete or whose required existing implementation has been verified.
2. Read both the linked user story and task file before changing code.
3. Keep implementation within the story boundary; do not bypass dependency rules defined in `specs/README.md`.
4. Mark a story complete only after its required behavior, persistence/contracts where applicable, tests, and relevant documentation are verified.
5. Record concise evidence in the completion log, including test commands and important deferred items.
6. If existing code appears to satisfy a story, audit it against the acceptance criteria and tasks before checking the item.
7. Use `[~]` only while actively implementing one story in a working branch; replace it with `[x]` when verified or `[ ]` if work is paused/incomplete.

## Completion Gate

A checklist item may be changed to `[x]` only when applicable criteria are met:

- Acceptance criteria in the story are satisfied.
- Required tasks are implemented or explicitly documented as deferred by the story scope.
- Code follows the project dependency and SOLID rules.
- Public chat execution still uses only `POST /api/ai/v1/query`.
- Relevant unit, integration, and architecture tests pass.
- Contracts and documentation affected by behavior are updated.
- No LLM directly performs deterministic financial calculations, billing decisions, confidence calculation, or SQL execution.
- The completion log below identifies verification evidence.

## Ordered Delivery Checklist

### Stage 1 - Backend Foundation And Commercial Boundary

These stories establish the executable solution, actor/tenant isolation, and billing boundary required before billable AI workflows are expanded.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 01 | [001](./001-project-foundation/user-story.md) / [tasks](./001-project-foundation/tasks.md) | Project Foundation | Establish solution structure, configuration, test hosts, and architecture rules. Verify existing foundation before checking. |
| [x] | 02 | [002](./002-auth-and-tenancy/user-story.md) / [tasks](./002-auth-and-tenancy/tasks.md) | Authentication and Tenant Context | Depends on `001`; establish authenticated actor, tenant, API client, authorization, and rate-limit context. |
| [x] | 03 | [013](./013-billing-and-credits-domain/user-story.md) / [tasks](./013-billing-and-credits-domain/tasks.md) | Billing and Credits Domain | Depends on `001`, `002`; establish the isolated Billing bounded context, account model, ledger, pricing, entitlement, and reservation contracts. |

### Stage 2 - Governed Financial Data And Calculation Core

These stories establish canonical financial meaning, normalized source data, and deterministic calculation evidence used by the Scanner Tool.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 04 | [003](./003-financial-domain-model/user-story.md) / [tasks](./003-financial-domain-model/tasks.md) | Financial Domain Model | Depends on `001`; define financial entities, periods, invariants, and semantic metric primitives. |
| [x] | 05 | [015](./015-financial-semantic-layer/user-story.md) / [tasks](./015-financial-semantic-layer/tasks.md) | Financial Semantic Layer and Ontology | Depends on `003`; establish canonical `MetricCode`, bilingual aliases, versioned policies, and registry/strategy calculator contracts before expanding calculated metrics or parser assumptions. |
| [x] | 06 | [004](./004-third-party-data-provider-abstraction/user-story.md) / [tasks](./004-third-party-data-provider-abstraction/tasks.md) | Third-Party Data Provider Abstraction | Depends on `001`, `003`; isolate financial/market data providers and health/reliability behavior. |
| [x] | 07 | [005](./005-data-ingestion-and-normalization/user-story.md) / [tasks](./005-data-ingestion-and-normalization/tasks.md) | Data Ingestion and Normalization | Depends on `003`, `004`; ingest and persist normalized reproducible scanner inputs. |
| [x] | 08 | [006](./006-derived-metrics-engine/user-story.md) / [tasks](./006-derived-metrics-engine/tasks.md) | Derived Metrics Engine | Depends on `003`, `005`, `015`; implement deterministic, versioned calculations through registered metric strategies. |

### Stage 3 - Provider-Neutral AI And Internal Observability Foundation

These stories make AI execution replaceable and traceable before the public query workflow depends on it.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 09 | [014](./014-ai-model-provider-abstraction/user-story.md) / [tasks](./014-ai-model-provider-abstraction/tasks.md) | AI Model Provider Abstraction | Depends on `001`; implement provider-neutral AI contracts, deterministic fake provider, capability routing, and normalized execution facts. |
| [x] | 10 | [018](./018-ai-observability-and-telemetry/user-story.md) / [tasks](./018-ai-observability-and-telemetry/tasks.md) | AI Observability and Telemetry | Depends on `002`, `013`, `014`; implement the minimum OpenTelemetry-compatible correlation/trace contracts needed by AI facade workflows. Advanced dashboards remain incremental. |

### Stage 4 - Scanner MVP Through The Single AI Facade

These stories deliver the user-visible scanner workflow, explainability, accounting integration, performance, and data operations.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 11 | [007](./007-natural-language-scanner-parser/user-story.md) / [tasks](./007-natural-language-scanner-parser/tasks.md) | Natural Language Scanner Parser | Depends on `002`, `013`, `014`, `015`, `018`; implement AI facade orchestration, conversation contracts, intent/tool routing, semantic metric resolution, and validated plans. |
| [x] | 12 | [008](./008-scanner-execution-engine/user-story.md) / [tasks](./008-scanner-execution-engine/tasks.md) | Scanner Execution Engine | Depends on `006`, `007`; execute validated plans against deterministic data/metrics and produce table-ready results. |
| [x] | 13 | [009](./009-explainable-results/user-story.md) / [tasks](./009-explainable-results/tasks.md) | Explainable Scanner Results | Depends on `008`, `015`; return citations, confidence, metric/policy evidence, follow-up suggestions, and structured answer presentation. |
| [x] | 14 | [010](./010-usage-metering-and-billing-readiness/user-story.md) / [tasks](./010-usage-metering-and-billing-readiness/tasks.md) | Usage Metering and Billing Readiness | Depends on `007`, `009`, `013`, `014`; complete facade reservation/finalization and returned usage metadata using Billing contracts. |
| [x] | 15 | [011](./011-cache-and-performance/user-story.md) / [tasks](./011-cache-and-performance/tasks.md) | Cache and Performance | Depends on `008`, `010`; add caching without bypassing accounting, evidence, or freshness. |
| [x] | 16 | [012](./012-admin-data-operations/user-story.md) / [tasks](./012-admin-data-operations/tasks.md) | Admin Data Operations | Depends on `004`, `005`; expose authorized ingestion/health operations needed to operate the scanner datasets. |

### Stage 5 - Intelligence Platform Evolution

These stories are part of the future-ready platform architecture. They should not delay the Phase 1 Scanner MVP unless explicitly promoted into a release milestone.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 17 | [017](./017-ai-evaluation-and-regression/user-story.md) / [tasks](./017-ai-evaluation-and-regression/tasks.md) | AI Evaluation and Regression Framework | Depends on `007`, `008`, `009`, `014`, `015`; add internal golden datasets, prompt/workflow version evaluation, and regression reporting. |
| [x] | 18 | [016](./016-derived-feature-foundation/user-story.md) / [tasks](./016-derived-feature-foundation/tasks.md) | Derived Feature Foundation | Depends on `005`, `006`, `015`; establish reproducible historical feature contracts and asynchronous computation without implementing a full ML platform. |
| [x] | 19 | [019](./019-conversation-memory-strategy/user-story.md) / [tasks](./019-conversation-memory-strategy/tasks.md) | Conversation Memory Strategy | Depends on Conversation/Message delivery in `007` and tenancy in `002`; define or implement only approved consent-aware memory scope beyond Phase 1 persistence. |

### Stage 6 - CodalDB Data Provider (Coexisting Source)

These stories add CodalDB (MS SQL Server, vendor Noavaran Amin) as a second financial-data source behind the `004` abstraction, feeding the same normalized PostgreSQL tables and `DerivedMetrics` read path as CyclicalWaves. They are ordered by their dependency chain (foundation → symbol linkage → ingestion → derived metrics → scheduled sync & recalculation) and sit after the Phase 1 MVP; they do not alter the public query endpoint or scanner engine. Schema reference: [../docs/codaldb-datasource.md](../docs/codaldb-datasource.md). Order 26 (spec `027`) is the orchestration capstone: it adds the nightly incremental sync and the derived-metric recalculation outbox processor that closes the precompute gap, so growth metrics are computed automatically after ingestion and read instantly at query time.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 19a | [020](./020-cyclicalwaves-data-provider/user-story.md) / [tasks](./020-cyclicalwaves-data-provider/tasks.md) | CyclicalWaves HTTP Data Provider | Depends on `004`, `005`; implement the CyclicalWaves HTTP API as the primary `"ConfiguredFinancialProvider"` behind the `004` provider abstraction. Verified as part of orders 06 and 07: the configurable typed HTTP adapter with credential-based auth, timeout/retry/circuit-breaker handling, raw-payload capture (SHA-256 dedup), and DI wiring was delivered in spec `004`; the `SymbolPayloadNormalizer`, `FinancialStatementPayloadNormalizer`, and `MonthlyReportPayloadNormalizer` feeding normalized PostgreSQL tables were delivered in spec `005`. No additional code changes required. |
| [x] | 20 | [021](./021-codaldb-provider-foundation/user-story.md) / [tasks](./021-codaldb-provider-foundation/tasks.md) | CodalDB Provider Foundation | Depends on `004`, `005`; expose read-only SQL Server access behind the financial-provider interfaces as a `"CodalDb"` provider that coexists with CyclicalWaves. Serialize queried rows into `ProviderRawPayload` (JSON + SHA-256), add `CodalDbConnectionFactory`/options/secrets, SQL resilience (timeout/retry/fast-fail), health check, and Infrastructure DI. No `IMarketDataProvider`; no writes/DDL. |
| [x] | 21 | [022](./022-codaldb-company-symbol-sync/user-story.md) / [tasks](./022-codaldb-company-symbol-sync/tasks.md) | CodalDB Company & Symbol Sync | Depends on `021`, `003`/`005`; extend `NormalizedCompanyRow` (EF migration: English name, industry/group/market ids+names, ISINs, `InstrumentCode`, `SourceModifiedAt`). `CodalDbSymbolNormalizer` upserts company/symbol rows with canonical `SymbolCode` linkage (`InstCode` → ISIN → `CoTSESymbol` → `CompanySymbol`; never `InstrumentRef`), cross-provider alignment with CyclicalWaves, and provider routing via optional `DataSyncRequest.ProviderName`. Idempotent. |
| [x] | 22 | [023](./023-codaldb-financial-statement-ingestion/user-story.md) / [tasks](./023-codaldb-financial-statement-ingestion/tasks.md) | CodalDB Financial Statement Ingestion | Depends on `021`, `022`, `005`/`003`/`015`; `CodalDbFinancialStatementNormalizer` writes curated income/balance line items (data-driven `ItemId → MetricCode` map), two statement rows per period (income/balance), canonical variant selection (`CodalDbStatementSelectionPolicy`: audited → latest representment → consolidated/parent), absolute Gregorian fiscal-period mapping, and publishes `DerivedMetricRecalculationRequested`. Idempotent. |
| [x] | 23 | [024](./024-codaldb-monthly-activity-ingestion/user-story.md) / [tasks](./024-codaldb-monthly-activity-ingestion/tasks.md) | CodalDB Monthly Activity Ingestion | Depends on `021`, `022`, `005`/`006`; `CodalDbMonthlyReportNormalizer` normalizes monthly production/sales into `NormalizedMonthlyReportRow`/line items (Jalali→Gregorian via the shared resolver — do not re-implement), satisfies `MONTHLY_SALES` by summing across products so `MONTHLY_SALES_GROWTH_*` recompute with no engine changes, publishes recalculation. Idempotent. |
| [x] | 24 | [025](./025-codaldb-precomputed-ratios/user-story.md) / [tasks](./025-codaldb-precomputed-ratios/tasks.md) | CodalDB Precomputed Ratios | Depends on `021`, `022`, `023`, `015`, `008`/`009`; add `ProviderDataset.FinancialRatios` + `IFinancialRatioProvider`; `CodalDbRatioNormalizer` persists curated vendor-precomputed ratios as `DerivedMetricRow`s (`CalculationPolicyVersion = "codal-ratio-source-v1"`, source-marked) reusing the statement selection policy. Bounded/paged queries against the 5.1M-row table. Governed metric definitions + bilingual aliases; scannable with no engine changes; never overwrites engine-calculated metrics. |
| [x] | 25 | [026](./026-codaldb-derived-growth-metrics/user-story.md) / [tasks](./026-codaldb-derived-growth-metrics/tasks.md) | CodalDB Derived Growth Metrics | Depends on `023`, `025`, `006`, `015`, `008`/`009`; register YoY/QoQ growth metrics for revenue, net/operating/gross profit, EPS, EBIT, equity via reused `PercentageGrowthMetricCalculator`; `EBIT` additive composite (`NET_PROFIT + FINANCE_COSTS + INCOME_TAX`, operating-profit proxy fallback); `CodalDiscreteQuarterDeriver` for cumulative→discrete QoQ; generic `LineItemMetricInputSource` (DRY); ingest vendor-precomputed growth ratios via the `025` mechanism under distinct codes. Idempotent; missing/zero-denominator → warning, never exception. |
| [x] | 26 | [027](./027-codaldb-scheduled-sync-and-recalculation/user-story.md) / [tasks](./027-codaldb-scheduled-sync-and-recalculation/tasks.md) | CodalDB Scheduled Sync & Derived-Metric Recalculation | Depends on `021`–`026`, `005`/`006`/`012`/`018`; add the missing **provider-agnostic** `MetricRecalculationProcessor` that drains the `MetricRecalculationRequests` outbox (`ProcessedAt IS NULL`, bounded batch, idempotent on the `DerivedMetrics` unique key) and invokes the existing `006` `DerivedMetricRecalculationCommand` — calculation stays in the engine (one source of truth). Drained by a worker on a configurable interval, mirroring Billing `Worker`/`BillingOutboxProcessor`/`PeriodicTimer` (benefits all providers). Add `CodalDbScheduledSyncService` for nightly **incremental** sync (watermark on CodalDB `ModifiedDateTime`; bounded parallelism) enqueuing `DataSyncRequest`s with `ProviderName="CodalDb"`, plus `DataAdmin` endpoints `POST /api/v1/admin/codaldb/full-sync` / `…/incremental-sync`. No query-time computation; per-company failure isolation + telemetry. |

### Stage 7 - Query Intelligence & Self-Improving Data Platform

These stories capture and act on missing data, missing metrics, and parser limitations to continuously improve
the catalog, data coverage, and AI intent recognition without explicit engineering intervention.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 27 | [028](./028-missing-answer-feedback-pipeline/user-story.md) / [tasks](./028-missing-answer-feedback-pipeline/tasks.md) | Missing-Answer Feedback & Auto-Improvement Pipeline | Depends on `007`/`008`/`009`, `015` (metric registry); capture every unanswered/partially answered query (metric gap, calculation gap, data coverage gap, data quality, parser limitation) as classified structured feedback in PostgreSQL, fire-and-forget during query execution. `IMissingAnswerFeedbackCollector` interface + `EfCoreMissingAnswerFeedbackRepository`, coalesced by actor/query-hash/classification/date-bucket to avoid log bloat. Admin visibility: `GET /api/v1/admin/missing-answer-feedback` + summary (counts by classification). No query-time regression; feedback collection is isolated, asynchronous, swallows errors. Phase 1: fire-and-forget default (`NoOpMissingAnswerFeedbackCollector`); future: ML retraining feedback export. |

### Stage 8 - Schema Corrections

These stories fix design gaps discovered after production-shape data was loaded. They are
strictly remediation: the corrected schema absorbs what earlier specs (003, 005, 020, 023) left
incomplete or inconsistent.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 28 | [029](./029-financial-statement-schema-fix/user-story.md) / [tasks](./029-financial-statement-schema-fix/tasks.md) | Financial Statement Schema Fix | Depends on `003`, `005`, `020`, `023` (and `004` for the configured-provider contract). Introduce `StatementType` column on `NormalizedFinancialStatementRow` (uses the existing `FinancialStatementType` enum: `IncomeStatement` / `BalanceSheet` / `CashFlow`), change the unique key from `(ProviderName, ExternalStatementId)` to `(ProviderName, ExternalStatementId, StatementType)`, and fix **all three** financial-statement normalizers consistently: `CyclicalWavesFinancialStatementNormalizer` (which writes the wrong literal `"IncomeStatement"` into `PeriodType`, making rows unparseable by `LineItemMetricInputSource`), `CodalDbFinancialStatementNormalizer` (drops the `:INC`/`:BS` `ExternalStatementId` suffix workaround in favor of the new column), and `FinancialStatementPayloadNormalizer` (the configured HTTP provider contract gains a required `StatementType` field plus enum validation for both `Period` and `StatementType`, so a future HTTP adapter cannot reintroduce the bug). Migration strategy: TRUNCATE `FinancialStatements`/`FinancialStatementLineItems`/`DerivedMetrics`/`MetricRecalculationRequests`/`MonthlyReports`/`MonthlyReportLineItems`/`ProviderRawPayloads` + reset CodalDb watermark, then add `StatementType` as NOT NULL with no default and the new indexes — clean slate is simpler than backfilling and avoids invalidating `DerivedMetrics.SourceEvidenceJson` references. Operator runs `POST /api/v1/admin/codaldb/full-sync` after migration to repopulate. Discovered after a CodalDb full-sync produced 19,682 correctly-shaped rows alongside 1,825 broken CyclicalWaves rows (`PeriodType="IncomeStatement"` can't be parsed by `LineItemMetricInputSource`); the third normalizer was found during scope review as a latent vulnerability. Out of scope: cash-flow line items, balance-sheet point-in-time semantics. |

### Stage 9 - StockMarketDB Trading Statistics

This story adds fresh trading statistics from the separate local `StockMarketDB` SQL Server
source. It follows the CodalDB financial source but uses dedicated time-series ingestion,
watermarks, retention, and polling cadence. Verified source mapping:
[stockmarketdb-trading-statistics-datasource.md](../docs/stockmarketdb-trading-statistics-datasource.md).

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 29 | [030](./030-stockmarketdb-trading-statistics-sync/user-story.md) / [tasks](./030-stockmarketdb-trading-statistics-sync/tasks.md) | StockMarketDB Trading Statistics Synchronization | Depends on `004`, `005`, `011`, `012`, `022`; add a separate read-only SQL Server adapter for `Tse.Instrument`, one-minute intraday `tse.Trade`, daily `TSE.TradeRefined` (`TradeOneDay`, one price/day/instrument), five-minute intraday `tse.IndexB1LastDay`, and daily `Tse.IndexNew` scoped by named-index `InstrumentRef` GUIDs (شاخص کل, فرابورس, بازده نقدی و قیمت, ۵۰ شرکت فعال‌تر, قیمت هم‌وزن, کل هم‌وزن). Support bounded full-sync (historical backfill) and overlap-watermark incremental sync per dataset, sharing idempotent normalizers. Normalize PostgreSQL time-series rows plus a `LatestMarketQuotes` projection. Link optional companies through `InsCode -> InstrumentCode`; never treat non-company instruments as companies and never use CodalDB's placeholder `InstrumentRef`. **Evidence (`2026-06-09`):** Reconciled the adapter to the authoritative sources — daily trades now read `Tse.TradeRefined` (GUID `Id`/`ChangeTime` keyset, `DailyInstrumentTrades.ExternalTradeId` migrated `bigint`→`uuid` via `20260609143955_ChangeDailyInstrumentTradeExternalIdToGuid`) and daily indices read `Tse.IndexNew` scoped to the six named-index GUIDs owned by `StockMarketNamedIndices`. Ran a full bounded backfill end-to-end into PostgreSQL via the DataAdmin endpoints: `DailyInstrumentTrades` = 2,776,064 (matches the live `Tse.TradeRefined` count), `DailyIndexSnapshots` = 7,445 (2,856 `HistoricalBackfill` from `Tse.IndexNew` + 4,589 `IntradayClose`), `IntradayTradeSnapshots` = 466,799, `IntradayIndexSnapshots` = 46,968, `TradingInstruments` = 77,204, `LatestMarketQuotes` = 20,081; all dataset watermarks current to `2026-06-09` with no pending continuation cursors. Two issues fixed during the run: (1) the per-row upsert lookups were batched into one preload query per page, cutting page time ~3m42s→~9s (≈26×); (2) `PersistInstrumentsAsync` company linkage threw `ArgumentException` when one `InstrumentCode` mapped to multiple provider-scoped companies (NoavaranArchiveSql + NoavaranCurrentApi) — now picks the most-recently-synchronized canonical company deterministically. Tests: 16 `StockMarketDb`/named-index unit tests (incl. within-page-duplicate and shared-`InstrumentCode` regressions), 452 unit tests total, 48 integration tests, and 5 architecture tests pass; full solution builds clean. |

Implementation progress (`2026-06-01`): added the read-only `StockMarketDb` SQL adapter
(`ApplicationIntent=ReadOnly`, bounded parameterized pages, retry/error mapping); normalized
PostgreSQL rows and EF migrations for `TradingInstruments`, intraday/daily trades,
intraday/daily indices, `LatestMarketQuotes`, and dataset watermarks; optional
`InsCode -> Companies.InstrumentCode` linkage; idempotent source-id upserts; intraday-first
latest-quote projection with daily fallback; current daily-index close projection from the last
intraday index snapshot; config-gated `PersistedMarketDataProvider`; DataAdmin manual sync route;
and configurable Worker polling. Added 30-day default retention cleanup for append-heavy
intraday history and documented the future partition migration tradeoff; composite
timestamp/source-id continuation cursors preserve bounded-page progress while overlap replay
handles late arrivals. Verification: `dotnet test src/backend/FinancialCopilot.sln
--configuration Release --no-restore` passed (`299` unit, `172` integration, `3` architecture).
Applied all PostgreSQL migrations locally,
drained the `76,810`-row instrument dimension in bounded pages, and live-verified two
`Tse.Trade` pages (`5,000` rows persisted each) with forward watermark progress.

### Stage 10 - Frontend Backend Integration

These stories replace the Lovable/TanStack prototype's Supabase authentication/chat persistence,
mock credit decrements, and canned financial responses with the .NET backend. Implementation
must proceed in order: establish backend-owned identity first, cut chat over to the AI facade
second, then replace supporting widget mocks, then add optional assisted metadata controls. Audit source:
[frontend-backend-api-requirements.md](../docs/frontend-backend-api-requirements.md).

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 30 | [031](./031-frontend-authenticated-api-bridge/user-story.md) / [tasks](./031-frontend-authenticated-api-bridge/tasks.md) | Backend Identity Authentication And Permission Authorization | Depends on `002`, `010`, `013`; replace Supabase authentication with ASP.NET Core Identity, short-lived JWT access tokens, rotating hashed refresh tokens, persisted `Users`/`Roles`/`Permissions`/`UserRoles`/`RolePermissions`/`RefreshTokens`, tenant membership, and permission policies (`Role -> Permissions -> Claims`). Add layered plan-capability and AI-credit enforcement for scanner, stock analysis, reports, watchlist, portfolio, and deep research without moving accounting into authorization. Preserve SaaS API-key auth and never trust a browser-supplied tenant id. Reference: [authorization-and-plan-entitlements.md](../docs/authorization-and-plan-entitlements.md). |
| [x] | 31 | [032](./032-frontend-chat-conversation-cutover/user-story.md) / [tasks](./032-frontend-chat-conversation-cutover/tasks.md) | Frontend Chat And Conversation API Cutover | Depends on `031`, `007`, `009`, `010`; complete actor-scoped conversation lifecycle APIs and reloadable structured assistant content, then replace Supabase thread/message writes, `generateMockReply`, and local credit decrement with `POST /api/ai/v1/query` plus generic conversation reads. Hide unsupported prototype controls. |
| [x] | 32 | [033](./033-frontend-usage-watchlist-market-summary/user-story.md) / [tasks](./033-frontend-usage-watchlist-market-summary/tasks.md) | Frontend Usage, Watchlist, And Market Summary Integration | Depends on `031`, `010`, `030`; connect `GET /api/v1/usage/me`, add actor-scoped watchlist APIs enriched from `LatestMarketQuotes`, expose normalized market-summary reads, and remove `STOCK_DB`/`MARKET_SNAPSHOT` widget mocks. Unsupported analytics remain nullable or hidden. |
| [x] | 33 | [034](./034-frontend-assisted-query-metadata/user-story.md) / [tasks](./034-frontend-assisted-query-metadata/tasks.md) | Frontend Assisted Query Metadata | Depends on `031`, `015`, `032`; connect governed metric metadata and add bounded period/symbol/industry discovery for optional assisted prompt composition. The final prompt still goes only to `POST /api/ai/v1/query`; no public scanner parse/execute endpoints. |

### Stage 11 - Admin Management APIs

This story adds a controlled API-only administration surface over the owned Identity, tenancy,
and Billing boundaries. It prepares stable backend contracts for a future React Admin UI
without implementing that UI or moving Identity/Billing domain rules into controllers.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 34 | [035](./035-admin-identity-and-entitlement-management/user-story.md) / [tasks](./035-admin-identity-and-entitlement-management/tasks.md) | Admin Identity And Entitlement Management | Depends on `031`, `013`; add granular permission-protected, tenant-scoped, audited Admin APIs for users, refresh-session revocation, roles, permission mappings, tenant memberships, Billing-owned plans and `PlanCapabilities`, subscriptions, manual credit adjustments, immutable usage-ledger reads, and security/Billing audit visibility. Preserve the final active `SuperAdmin` administration path transactionally. React Admin UI is out of scope. |

### Stage 12 - Frontend Local Integration Remediation

This story closes the local browser/API routing gap discovered after the backend-auth cutover.
The owned Identity endpoints already exist; the remaining work aligns browser-side API-base
configuration, server-side configuration semantics, local credentialed CORS, and smoke
verification.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 35 | [036](./036-frontend-local-api-connectivity/user-story.md) / [tasks](./036-frontend-local-api-connectivity/tasks.md) | Frontend Local API Connectivity | Depends on `031`; configure browser-side FinancialCopilot API requests to reach the .NET API host instead of the frontend SSR origin, align server/browser URL resolution, allow the actual local frontend origin through explicit credentialed CORS, document startup configuration, and smoke-test register/refresh/logout from the frontend origin. |

### Stage 13 - Frontend Administration Panel

This story exposes the completed backend Admin Management API through a permission-aware React
administration experience. Backend policy enforcement remains authoritative.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 36 | [037](./037-frontend-admin-panel/user-story.md) / [tasks](./037-frontend-admin-panel/tasks.md) | Frontend Administration Panel | Depends on `031`, `035`, `036`; add protected admin routes, permission-aware navigation, typed Admin API integration, user administration, roles and permissions, tenant memberships, subscriptions, credit adjustments, usage-ledger reads, and security/Billing audit visibility. Preserve backend tenant scope, audit, idempotency, concurrency, and final-`SuperAdmin` rules. |

### Stage 14 - NADPCO HTTP API Provider

These stories add the attached Noavaran Amin HTTP APIs as a provider named `NadpcoApi`.
`NadpcoApi` coexists with the completed `CodalDb` SQL synchronization path and feeds the same
normalized PostgreSQL and scanner read models.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 37 | [038](./038-nadpco-api-provider-foundation/user-story.md) / [tasks](./038-nadpco-api-provider-foundation/tasks.md) | NADPCO HTTP API Provider Foundation | Depends on `004`, `005`; add `NadpcoApi` token authentication, secrets-backed configuration, resilience, health checks, raw payload capture, secret redaction, and provider-name routing without replacing `CodalDb`. |
| [x] | 38 | [039](./039-nadpco-api-company-catalog-sync/user-story.md) / [tasks](./039-nadpco-api-company-catalog-sync/tasks.md) | NADPCO API Company Catalog Synchronization | Depends on `038`, `022`; normalize `/api/v3/BaseInfo/Companies`, preserve metadata and provenance, and align canonical symbols across providers without duplicate securities. |
| [x] | 39 | [040](./040-nadpco-api-financial-statement-sync/user-story.md) / [tasks](./040-nadpco-api-financial-statement-sync/tasks.md) | NADPCO API Financial Statement Synchronization | Depends on `038`, `039`, `029`; ingest bounded balance-sheet, income-statement, and cash-flow batches using reviewed item mappings, corrected statement types, source dates, variant selection, and recalculation requests. |
| [x] | 40 | [041](./041-nadpco-api-fundamental-index-sync/user-story.md) / [tasks](./041-nadpco-api-fundamental-index-sync/tasks.md) | NADPCO API Fundamental Index Synchronization | Depends on `038`, `039`, `015`, `008`, `009`; persist reviewed vendor-precomputed indexes as source-marked `DerivedMetricRow` observations with governed aliases and no scanner-engine changes. |
| [x] | 41 | [042](./042-nadpco-api-monthly-activity-sync/user-story.md) / [tasks](./042-nadpco-api-monthly-activity-sync/tasks.md) | NADPCO API Monthly Activity Synchronization | Depends on `038`, `039`, `006`; normalize bounded product-sales and service-sales monthly activity, preserve evidence, retain zero periods, and reuse deterministic monthly-growth recalculation. |
| [x] | 42 | [043](./043-nadpco-api-sync-orchestration/user-story.md) / [tasks](./043-nadpco-api-sync-orchestration/tasks.md) | NADPCO API Synchronization Orchestration | Depends on `038`-`042`, `012`, `018`; add bounded full/incremental orchestration, endpoint-appropriate progress, overlap reconciliation where modified-since is unavailable, failure isolation, DataAdmin operations, telemetry, and cache invalidation. |
| [x] | 43 | [044](./044-nadpco-api-scheduled-sync-worker/user-story.md) / [tasks](./044-nadpco-api-scheduled-sync-worker/tasks.md) | NADPCO API Scheduled Synchronization Worker | Depends on `043`, `011`, `012`, `018`; add automatic scheduled incremental NADPCO synchronization through the existing bounded orchestration pipeline with configurable cadence/datasets/batching/concurrency/retry policy, overlap prevention, persisted scheduled-run history, diagnostics, and scanner-cache invalidation through the existing data-sync path. |

### Stage 15 - NADPCO Company Catalog Authority Remediation

These items implement the 2026-06-05 change requests added to specs `020`, `039`, `043`, and
`044`. The prior NADPCO stories remain completed historically, but the change requests are not
complete until these follow-up rows are implemented and verified. This stage promotes NADPCO
`/api/v3/BaseInfo/Companies` to the authoritative PostgreSQL `Companies` source, adds a
controlled clean-slate backfill path, keeps recurring refreshes non-destructive, and prevents
CyclicalWaves from writing company catalog rows.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 45 | [039](./039-nadpco-api-company-catalog-sync/user-story.md#change-request---2026-06-05) / [tasks](./039-nadpco-api-company-catalog-sync/tasks.md#change-request-tasks---2026-06-05) | NADPCO Company Catalog Clean-Slate Backfill | Depends on `038`, existing `039`, and current normalized company schema. Add a controlled operation that deletes existing PostgreSQL `Companies` rows with documented FK/dependency handling, then repopulates from `https://data3.nadpco.com/api/v3/BaseInfo/Companies`. Verify DTO/schema coverage and normalized persistence for every NADPCO company response field (`coID`, `coCode`, names, symbols, floor, industry, market, TSE codes, ISINs, status/rights, all Jalali/Gregorian dates, registration, fund, national-id, Pinglish, market-board fields). Add missing columns or related normalized tables where needed; no field may remain only in raw payload evidence. Treat `coID` as the external id and add sample-row/idempotency tests for `coID=13226` / `آبین`. |
| [x] | 46 | [043](./043-nadpco-api-sync-orchestration/user-story.md#change-request---2026-06-05) / [tasks](./043-nadpco-api-sync-orchestration/tasks.md#change-request-tasks---2026-06-05) | NADPCO Company-Catalog-Only Orchestration | Depends on order `45`, `012`, and existing `043`. Add a DataAdmin/company-catalog-only orchestration mode with two run modes: destructive `CompanyCatalogCleanSlate` for explicit maintenance/backfill and non-destructive `CompanyCatalogRefresh` for ordinary refreshes. Record run mode in telemetry, isolate failures, trigger scanner-cache invalidation/linkage follow-up on metadata changes, and verify authorization, clean-slate behavior, idempotency, telemetry, and recovery. |
| [x] | 47 | [044](./044-nadpco-api-scheduled-sync-worker/user-story.md#change-request---2026-06-05) / [tasks](./044-nadpco-api-scheduled-sync-worker/tasks.md#change-request-tasks---2026-06-05) | Scheduled NADPCO Company Catalog Refresh | Depends on order `46` and existing `044`. Include `CompanyCatalog` in scheduled NADPCO dataset selection with daily refresh as the production recommendation after credentials and initial backfill are verified. Scheduled runs must insert/update only, must never call the clean-slate delete path, must expose company-catalog run counts/status/history, and must prove newly listed NADPCO companies are inserted on a later daily run. |
| [x] | 48 | [020](./020-cyclicalwaves-data-provider/user-story.md#change-request---2026-06-05) / [tasks](./020-cyclicalwaves-data-provider/tasks.md#change-request-tasks---2026-06-05) | Stop CyclicalWaves Company Catalog Writes | Depends on orders `45` and `46`. Disable or remove CyclicalWaves `Companies` upserts. CyclicalWaves may continue to provide financial statements, monthly observations, and valuation ratios, but it must resolve linkage through existing NADPCO-backed company/symbol metadata, emit data-quality warnings when linkage fails, and never overwrite NADPCO names, symbols, industry, market, ISIN, registration, or listing metadata. Add regression tests for non-overwrite and successful financial observation persistence when NADPCO linkage exists. |

### Stage 16 - Corrected Data Source Strategy and Operations

These open stories are the current active backlog priority. Correct the Noavaran Amin source
model first, then import/archive and current API data, then add catch-up coverage and market-data
cutover/UI operations. This ordering prevents additional provider work from reinforcing the wrong
archive-vs-current source boundary.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 52 | [051](./051-noavaran-archive-and-current-api-strategy/user-story.md) / [tasks](./051-noavaran-archive-and-current-api-strategy/tasks.md) | Noavaran Amin Archive and Current API Source Strategy | **Priority: Critical.** Correct the source model before additional provider work. Noavaran Amin is one logical vendor with archive and current API source modes. |
| [x] | 53 | [052](./052-noavaran-archive-one-time-import/user-story.md) / [tasks](./052-noavaran-archive-one-time-import/tasks.md) | Noavaran Archive One-Time Import | **Priority: High.** Depends on `051`; import frozen/archive Noavaran data once, validate coverage, and freeze the archive state. |
| [x] | 54 | [053](./053-noavaran-current-api-ingestion/user-story.md) / [tasks](./053-noavaran-current-api-ingestion/tasks.md) | Noavaran Current API Ingestion | **Priority: High.** Depends on `051`, `052`; ingest missing/current data from 1403 onward through Noavaran API services. |
| [x] | 55 | [050](./050-nadpco-api-all-fundamental-index-catchup/user-story.md) / [tasks](./050-nadpco-api-all-fundamental-index-catchup/tasks.md) | NADPCO API All Fundamental Index Catch-up | **Priority: High.** Depends on `038`, `039`, `041`, `043`; should follow `051`-`053` source-boundary decisions. Add a DataAdmin-only catch-up mode for all local NADPCO/Noavaran company ids from Shamsi 1403 through 1405 using `CompanyFundamentalIndex/Values` with batched `companyIds` and `companyIndexIds: []`, persisting all returned vendor index observations as coverage data while only reviewed indexes are promoted to governed scanner metrics. |
| [x] | 56 | [054](./054-stockmarketdb-to-tsetmc-direct-feed-migration/user-story.md) / [tasks](./054-stockmarketdb-to-tsetmc-direct-feed-migration/tasks.md) | StockMarketDB to Direct TSETMC Feed Migration | **Priority: Medium.** Depends on `030`, `051`; keep StockMarketDB as a bridge source and define future direct TSETMC ingestion/cutover. **Phase 1 (bridge stabilization) complete:** `TsetmcWebService` ProviderSourceDescriptor added to `ProviderSources` catalog; 3 nullable provenance columns (`LogicalVendor`, `PhysicalSource`, `SourceMode`) added to `StockMarketSyncStates` with EF migration `20260613000000_AddProvenanceToStockMarketSyncState`; provenance stamped on every `StockMarketSyncStateRow` upsert; `AdminStockMarketSyncStateResponse` exposes provenance fields (AC #9); `ITsetmcDirectFeedSyncService` interface added to Application with `NullTsetmcDirectFeedSyncService` stub in Infrastructure (AC #4); `IMarketQuoteSourcePriority` interface and `MarketQuoteSourcePriorityOptions` in Application with `ConfiguredMarketQuoteSourcePriority` in Infrastructure defaulting to `StockMarketDb` (AC #8); architecture tests verify `StockMarketDb.DefaultMode == MigrationBridge` (AC #2) and scanner code does not reference `StockMarketDb` physical source name (AC #10). **Phase 2 (Direct TSETMC Provider Foundation) implemented (`2026-06-13`):** `TsetmcWebServiceOptions` (SectionName=`TsetmcWebService`, `Enabled=false` default, configurable flows/date-ranges/credentials/timeout/retry); `ITsetmcWebServiceClient` interface + `TsetmcWebServiceClient` implementation (raw SOAP 1.1 over `HttpClient`, no WCF dependency) covering `Instrument`, `TradeLastDay`, `TradeOneDay`, `IndexB2`, `IndexB1LastDayLastData` ASMX operations with credential injection, retry, typed DataSet→record mapping; `TsetmcInstrumentRecord`, `TsetmcIntradayTradeRecord`, `TsetmcDailyTradeRecord`, `TsetmcDailyIndexRecord`, `TsetmcIntradayIndexRecord` DTOs; `ITsetmcDirectFeedSyncService` extended with 5 sync operations + `TsetmcSyncResult`; `TsetmcDirectFeedSyncService` persists into the same canonical PostgreSQL tables as StockMarketDb (TradingInstruments, IntradayTradeSnapshots, DailyInstrumentTrades, IntradayIndexSnapshots, DailyIndexSnapshots, LatestMarketQuotes) with `ProviderSources.TsetmcWebService` provenance and deterministic GUID idempotency; daily trade backfill uses fixed config date range (from/to yyyyMMdd); intraday trades use all-flows-per-poll strategy; `NullTsetmcDirectFeedSyncService` updated to implement full interface; ServiceCollectionExtensions registers `TsetmcWebServiceClient` via typed `HttpClient`, conditionally wires real vs null service based on `Enabled` flag; `appsettings.json` adds `TsetmcWebService` section; DataAdmin endpoints `GET /api/v1/admin/tsetmc/status` and `POST /api/v1/admin/tsetmc/{dataset}/sync` with `AdminTsetmcDirectFeedStatusResponse`/`AdminTsetmcSyncResponse` contracts. **Phase 2b (Scheduled Worker) implemented (`2026-06-14`):** `TsetmcPollingWorker` hosted service drives automatic collection on the Iranian equity market calendar — intraday (trades+indices) every 60s during 09:00–12:30 IRST on Sat–Wed; end-of-day (instruments+daily-trades+daily-indices) once after 17:00 IRST on Sat–Tue, once after 20:00 IRST on Wed; no calls on Thu/Fri (market holidays); schedule evaluated in fixed UTC+3:30 (Iran has no DST); `TsetmcPollingOptions` (SectionName=`TsetmcPolling`, `IntradayTickSeconds=30`, `IntradayIntervalSeconds=60`) registered in Program.cs and `appsettings.json`; IRST time math uses `DateTimeOffset.ToOffset(+3:30)` with `TimeProvider` injection for testability. **Phase 3 (Parallel Validation) implemented (`2026-06-14`):** `MarketQuoteMismatchRow` entity + `MarketQuoteMismatchRowConfiguration` EF config + manual EF migration `20260614100000_AddMarketQuoteMismatches` (table `MarketQuoteMismatches`, FK → TradingInstruments, indices on ComparedAt and TradingInstrumentId+ComparedAt); `ITsetmcValidationService.ValidateLatestQuotesAsync` in Application; `TsetmcValidationService` in Infrastructure — joins `LatestMarketQuote` rows from both providers on `InstrumentCode`, compares `LatestPrice` and `PriceChangePercentage`, flags pairs where relative diff > 0.1 % tolerance, persists all flagged rows; `IMarketQuoteMismatchReader`/`MarketQuoteMismatchReader` aggregate persisted rows per-field; both services registered in DI; DataAdmin endpoints: `POST /api/v1/admin/tsetmc/validate`, `GET /api/v1/admin/tsetmc/mismatches?recentDays=7`, `GET /api/v1/admin/tsetmc/source-priority`; response contracts `AdminTsetmcValidationResponse`, `AdminTsetmcMismatchSummaryResponse`, `AdminMarketQuoteSourceStatusResponse`. **Phase 4 (Cutover) implemented (`2026-06-14`):** `PersistedMarketDataProvider` now injects `IMarketQuoteSourcePriority` instead of `StockMarketDbProviderOptions`; `_providerName` evaluates `sourcePriority.PrimarySourceName` per-request so a live config change takes effect without restart (AC #8 fully wired); `appsettings.json` `MarketQuoteSourcePriority:PrimarySourceName` = `TsetmcWebService` (Phase 4 cutover active); set to `StockMarketDb` to roll back to bridge without any code change (AC #6). All ACs satisfied: (1) separate datasource ✓, (2) bridge still valid ✓, (3) TsetmcWebService physical source ✓, (4) direct ingestion behind abstraction ✓, (5) parallel validation + mismatch reports ✓, (6) cutover without scanner contract change ✓, (7) provenance on all rows ✓, (8) priority-driven quote projection ✓, (9) DataAdmin endpoints for both bridge and direct feed ✓, (10) architecture test confirms scanner independence ✓. Verification: `dotnet build` 0 errors, 543/543 tests passed (7 architecture + 536 unit). |
| [x] | 57 | [055](./055-frontend-data-management-console/user-story.md) / [tasks](./055-frontend-data-management-console/tasks.md) | Frontend Data Management Console | **Priority: Medium.** Depends on `012`, `031`, `037`, `051`-`054`; add UI for data operations, import/sync monitoring, health, run history, and reconciliation. Implemented: `DataManagementShell` sidebar layout with nav (Overview, Archive, Noavaran, StockMarketDB, Live Monitor, Reconciliation); `DataManagementGuard` shared auth/permission wrapper (reuses `data.sync.manage`); `DataManagementOverviewPage` with source cards for all 5 logical vendors (NoavaranAmin/Archive, NoavaranAmin/CurrentApi, CyclicalWaves, StockMarketDb, TsetmcWebService); `ArchiveImportPage` (dry-run, import, validate, freeze, re-import, coverage stats, run history table); `NadpcoCurrentApiPage` (health/latency, scheduled sync status/trigger, dataset watermarks, monthly-activity backfill progress/trigger, fundamental-index catch-up runs/trigger); `StockMarketDbPage` (per-dataset sync state table with freshness indicator, individual sync/full-reload triggers, MigrationBridge transitional notice with provenance fields, TSETMC direct feed placeholder); `ReconciliationPage` (placeholder with coming-soon state, awaiting spec 054 Phase 3 shadow comparison backend); Routes `/admin_/data/`, `/admin_/data/archive`, `/admin_/data/noavaran`, `/admin_/data/stockmarket`, `/admin_/data/reconciliation` (all DataAdmin-gated); updated `/admin_/data/monitor` to use shared `DataManagementGuard` (spec 058 overlap — no duplicate code); admin-page.tsx header now has both "Data Console" and "Live Monitor" buttons. `data-admin-client.ts` extended with all required API types and functions. Reconciliation queries deferred pending spec 054 Phase 3 backend read models. `bun run build` succeeded (0 errors). Backend: `dotnet test` 512/512 passed (7 architecture + 505 unit). |
| [x] | 61 | [057](./057-nadpco-monthly-activity-freshness-and-sales-lookup/user-story.md) / [tasks](./057-nadpco-monthly-activity-freshness-and-sales-lookup/tasks.md) | NADPCO Monthly Activity Freshness and Monthly Sales Lookup | **Priority: High.** Depends on `042`, `043`, `053`, `012`, `015`, `006`, `045`. Two verified gaps. (1) Ingestion — no month-by-month acquisition model: **Phase A** adds a separate DataAdmin-only manual backfill operation that walks Shamsi months newest-first (`140502 → 140501 → 140412 → … → 140401`, latest published month first) with durable per-month progress, resume-on-interruption, and a backfill-complete marker; **Phase B** — once the marker exists, scheduled refresh requests **only the previous Shamsi month** from the 1st of each month (never re-sweeping back to `140401`). (2) Read path — Persian «فروش» resolves only to quarterly `REVENUE`; `MONTHLY_SALES` has no Persian alias and no governed metrics exist for monthly sales quantity (مقدار فروش), production quantity (مقدار تولید), or sales rate (نرخ فروش), so «آخرین فروش غگلپا» never reads the Noavaran `MonthlyReports`/`MonthlyReportLineItems` tables — add the monthly metric codes, fa aliases, recalculation wiring, and a symbol-lookup integration test answering from monthly data. Includes live verification of the vendor month-bound date format (`140502` vs `1405/02/01`). |

### Stage 17 - Scanner and AI Platform Enhancements

These stories extend the Phase 1 Scanner MVP and AI orchestration platform. They share the existing
single AI facade endpoint, billing pipeline, and frontend scanner table component. Remaining open
items are lower priority than the corrected data-source backlog above.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [ ] | 43b | [008](./008-scanner-execution-engine/user-story.md) / [tasks](./008-scanner-execution-engine/tasks.md) | Scanner Column Policy and Valuation-Ratio Zero-Value Correction | **Priority: High.** Bug: `2026-06-19`. Depends on `008`. Three fixes required: (1) `IScannerResultColumnPolicy` must output only `نماد`+`شرکت` identity columns plus explicitly requested/filtered metrics — no automatic `LATEST_PRICE`, `DAILY_CHANGE_PCT`, or `MARKET_CAP` for PE/PS scanner queries; (2) internal/debug `symbols` column must never appear in user-facing scanner output; (3) scanner condition evaluation must exclude rows with `PE_TTM = 0`, `PS_TTM = 0`, or `PB = 0` for `<`/`<=` filter conditions (zero = missing/invalid for valuation ratios). Point-lookup quote enrichment (`ShouldIncludeMarketContext`) scope must be restricted to `SymbolLookup` intent only. See bug spec: [bugs/scanner-pe-ps-column-and-zero-value-regression-2026-06-19.md](./bugs/scanner-pe-ps-column-and-zero-value-regression-2026-06-19.md). |
| [x] | 44 | [045](./045-symbol-metric-point-lookup/user-story.md) / [tasks](./045-symbol-metric-point-lookup/tasks.md) | Symbol Metric Point Lookup | **Priority: High.** Depends on `007` (AI facade and intent routing), `008`/`009` (scanner table contract and explainability), `015` (metric alias resolver), `005` (DerivedMetrics and LatestMarketQuotes reads). Add `SymbolLookup` intent type, `ISymbolLookupParser` (LLM structured output), `ISymbolNameResolver` (match raw names against Symbols/Companies), and `ISymbolMetricLookupService` (query DerivedMetrics + LatestMarketQuotes for resolved pairs). Extend `AiQueryOrchestrationService` with the new intent branch; billing and feedback collection follow the scanner pattern. Re-use `ScannerTableResult` shape so the frontend renders lookup results without a new component. |
| [x] | 49 | [047](./047-microsoft-agent-framework-orchestration-v2/user-story.md) / [tasks](./047-microsoft-agent-framework-orchestration-v2/tasks.md) | Microsoft Agent Framework Orchestration V2 | Depends on `007`, `008`, `009`, `010`, `014`, `017`, `018`, `019`, and `045`; introduce a backward-compatible V2 AI orchestration path using Microsoft Agent Framework Agents, Workflows, tool/function adapters, and middleware. Preserve `POST /api/ai/v1/query`, existing response DTOs, Billing reservation/finalization, deterministic scanner/symbol lookup execution, provider-neutral AI model resolution, memory disclosures, telemetry, evaluation metadata, and V1 rollback by configuration. |
| [x] | 51 | [049](./049-llm-provider-switching-deepseek-support/user-story.md) / [tasks](./049-llm-provider-switching-deepseek-support/tasks.md) | LLM Provider Switching & DeepSeek Support | Depends on `014`, `010`, `018`, and `047`; support configuration-only switching between OpenAI and DeepSeek behind provider-neutral LLM contracts without changing business logic, orchestration workflows, billing behavior, or frontend integrations. Add DeepSeek chat/reasoning support, structured output, tool calling, streaming, provider/model/token/cost usage persistence, active-provider diagnostics, secret-safe configuration, and compatibility validation for `POST /api/ai/v1/query`, Scanner, Symbol Lookup, Billing, conversation memory, explainability, missing-answer feedback, and Microsoft Agent Framework V1/V2. |
| [x] | 58 | [046](./046-dynamic-metric-alias-learning/user-story.md) / [tasks](./046-dynamic-metric-alias-learning/tasks.md) | Dynamic Metric Alias Learning | **Priority: Medium.** Depends on `015`, `007`, `045`, and `028`. Replace code-only operational alias expansion with a PostgreSQL-backed dynamic alias layer and learning loop: unresolved user metric terms are logged, grouped into candidates, validated against existing governed metrics, and promoted automatically or by DataAdmin review. The learning loop may add aliases only; it must never create metric definitions, formulas, calculators, SQL, billing behavior, or deterministic financial logic from user prompts or LLM output. |
| [x] | 59 | [048](./048-frontend-ai-orchestration-v2-awareness/user-story.md) / [tasks](./048-frontend-ai-orchestration-v2-awareness/tasks.md) | Frontend AI Orchestration V2 Awareness | **Priority: Medium.** Depends on `047`, `032`, and `037`; adds orchestration metadata awareness, diagnostics, rollout support, and V1/V2 evaluation capabilities while preserving the existing chat experience. |

| [x] | 60 | [056](./056-native-microsoft-agent-framework-workflows/user-story.md) / [tasks](./056-native-microsoft-agent-framework-workflows/tasks.md) | Native Microsoft Agent Framework Workflows | **Priority: Medium-High.** Depends on `047`, `018`, `017`, and `019`; replace the current manual V2 orchestration chain with native Microsoft Agent Framework Workflow primitives, workflow state management, workflow telemetry, step-based execution, and future-ready orchestration patterns. Preserve `POST /api/ai/v1/query`, existing DTOs, Billing finalization, deterministic Scanner/Symbol Lookup execution, conversation memory, explainability, and V1 rollback compatibility. Prepare the architecture for future Deep Research, Multi-Agent workflows, Human-in-the-Loop approval, workflow branching, and parallel tool execution without requiring orchestration redesign. |

### Stage 17b - Monthly Activity Output Type Segmentation

Fetch and store all 5 `outputTypeId` variants (0–4) of the Noavaran Amin product-sales endpoint per company-month, so the AI query layer can serve both single-month and year-to-date sales figures without additional API calls at query time.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 63 | [059](./059-monthly-activity-output-type-segmentation/user-story.md) / [tasks](./059-monthly-activity-output-type-segmentation/tasks.md) | Monthly Activity Output Type Segmentation | **Priority: High.** Depends on `042` and `057`; fetch all 5 `outputTypeId` variants (0–4) per company-month from `api/v2/MonthlyActivity/ProductSales`, persist each as a separate `MonthlyReports` row with new `OutputType` column, add backward-compat normalizer for old two-field envelope payloads, add `MonthlyActivityOutputTypeResolver` for AI query routing (Story C, separate milestone). EF migration required. **All stories complete.** Story C: `MonthlyActivityQueryIntent` enum + `IMonthlyActivityOutputTypeResolver` interface in Application; `DefaultMonthlyActivityOutputTypeResolver` in Infrastructure (SingleMonth default, YTD on explicit hint); `MonthlyReportAggregateInputSource` now filters `OutputType == 0 OR null` by default so `MONTHLY_SALES`/`MONTHLY_SALES_QUANTITY`/`MONTHLY_PRODUCTION_QUANTITY`/`MONTHLY_SALES_RATE` read only single-month rows; 8 resolver unit tests + 1 updated normalizer test. Canonical identity correction: fallback `MonthlyReports.ExternalReportId` must stay `ProductSales:{externalCompanyId}:{jalaliYear}-{jalaliMonth:D2}:output-{outputType}` (or `ServiceSales:...:output-none`) and must never include `categoryId`. `dotnet test` 524/524 passed (7 architecture + 517 unit, 0 failures). |

### Stage 19 - Trading Instrument Unification

`TradingInstruments` is a provider-neutral dimension table. Currently the table has two separate
populations (`ProviderName = "StockMarketDb"` and `ProviderName = "TsetmcWebService"`) and all
`Persist*Async` methods in `TsetmcDirectFeedSyncService` filter by provider name when resolving
instrument ids, causing all intraday trade records to be silently dropped on days when the TSETMC
instrument sync has not yet run. TSETMC is the authoritative source of exchange-listed instruments;
Noavaran Amin's `tseCode`/`tseCIsinCode`/`tseSIsinCode` fields are the canonical cross-source
join keys.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 64 | [064](./064-trading-instrument-unification/user-story.md) / [tasks](./064-trading-instrument-unification/tasks.md) | Trading Instrument Unification | **Evidence (`2026-06-14`):** Phase 1.1 — removed `ProviderName` filter from `InstrumentMapByInsCodeAsync` in `TsetmcDirectFeedSyncService`; now resolves instruments from any provider. Phase 1.2 — added `EnsureInstrumentStub` helper; all four `Persist*Async` methods auto-create stub rows for unseen InsCodes instead of silently dropping records. Phase 1.3 — `StockMarketDbSyncService.PersistInstrumentsAsync` converted to no-op (no writes to `TradingInstruments`); `InstrumentMapAsync` `ProviderName` filter removed. Phase 2 — already satisfied: `NadpcoApiCompanyNormalizer` maps `tseCode` → `InstrumentCode`, `tseCIsinCode` → `CompanyIsin`, `tseSIsinCode` → `SymbolIsin`; `TsetmcDirectFeedSyncService.PersistInstrumentsAsync` already joins companies via `InstrumentCode`. Phase 3 — 4 new tests in `TradingInstrumentUnificationTests.cs` (stub auto-creation, stub reuse, StockMarketDb resolves TSETMC instrument, StockMarketDb no longer writes to table); 3 stale tests in `StockMarketDbSyncServiceTests.cs` updated to reflect new no-op behaviour. 540 unit tests pass, 0 build errors. Deferred: unique constraint on `InstrumentCode` pending one-off cleanup of legacy `ProviderName = "StockMarketDb"` rows. |

### Stage 18 - Live Data Sync Monitor

This story adds a live monitoring surface for all active and recent data synchronization runs.
The backend aggregates existing run-state rows from all providers into a single snapshot
endpoint and an SSE stream; the frontend admin page subscribes to the stream and shows live
run cards, record counts, error counts, and duration — updating automatically without page
refresh. No new persistent tables are introduced.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 62 | [058](./058-live-data-sync-monitor/user-story.md) / [tasks](./058-live-data-sync-monitor/tasks.md) | Live Data Sync Monitor | **Completed 2026-06-13.** `IDataSyncActivityReader` + `EfCoreDataSyncActivityReader` (aggregates 6 provider readers with per-source isolation). `PollingDataSyncActivityMonitor` singleton (5 s poll-diff, `SemaphoreSlim` connection cap, SSE fan-out via `Channel<DataSyncActivityEvent>`, graceful shutdown on `IHostApplicationLifetime.ApplicationStopping`). `GET /api/v1/admin/data-sync/activity` (snapshot REST, recentPerProvider 1–20, DataAdmin policy). `GET /api/v1/admin/data-sync/activity/stream` (SSE, 429 on cap). Frontend: `data-admin-client.ts`, `useDataSyncActivityStream` hook (SSE + exponential-backoff polling fallback), `DataSyncMonitorPage` (summary strip, active/recent run tables, detail panel, filter bar), route `/admin_/data/monitor`, nav link in admin page header. Tests: 15 unit tests pass (reader isolation, duration, recentPerProvider cap, monitor diff, connection cap), 4 integration tests added to `AdminDataOperationsEndpointTests`. `dotnet test` 505/505 passed; `bun run build` succeeded. |

### Stage 20 - CyclicalWaves Comprehensive Analysis Sync and AI Query

This stage adds a new bounded context — `ComprehensiveAnalysisSync` — that fetches, stores, and
refreshes the **تحلیل جامع** (Comprehensive Analysis) content published by CyclicalWaves through
`GET /api/blog/getComprehensiveAnalysis`. The context is independent of the financial data
ingestion pipeline; it populates a dedicated read model used by the AI assistant to answer
market-analysis questions without calling the external API at query time.

Two run modes are supported:
- **Full sync** — a one-time seed triggered manually (CLI, admin endpoint, or startup flag) that
  iterates all pages for all seven allowed tag categories with no date filter.
- **Daily incremental sync** — a Hangfire recurring job that runs once per day and fetches only
  content created since yesterday, using `filter[from_date]` / `filter[to_date]`.

The CyclicalWaves Bearer-token auth flow (10-day expiry, automatic re-authentication on 401) is
isolated inside this context and shares no token state with the existing CyclicalWaves financial
data provider.

Order `66` closes the gap between stored content and AI-answerable questions: it adds the
`ComprehensiveAnalysis` intent type, a plain-text summary column (HTML stripped at write time),
a narrow query use case and tool adapter, and structured citations — all through the existing
`POST /api/ai/v1/query` endpoint with no new public routes. Order `65` must be complete (or
progressed to the point where the persistence schema and upsert path exist) before order `66`
is started, because `066` amends the upsert to populate `PlainTextSummary` and reads from the
same `ComprehensiveAnalysisProviderDbContext`.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 65 | [065](./065-cyclicalvawes-comprehensive-analysis-sync/user-story.md) / [tasks](./065-cyclicalvawes-comprehensive-analysis-sync/tasks.md) | CyclicalWaves Comprehensive Analysis Sync | Depends on `001`, `002`, `012`; add Bearer-token auth client, `ComprehensiveAnalysis` entity + EF migration, upsert repository, full-sync service (all pages × all 7 tag categories, no date filter), daily incremental sync service (`filter[from_date]=yesterday`, Hangfire recurring job `"0 6 * * *"`), manual trigger via admin endpoint `POST /api/v1/admin/comprehensive-analysis/full-sync`, Polly retry policy (3× exponential back-off on 500), client-side category validation against the 7 allowed Persian tag values, structured Serilog logging, and `IHealthCheck` for the CyclicalWaves auth endpoint. The main category filter is set server-side; the client must never send numeric ids. Idempotent upsert key: `(Title, CreatedAt)`. Apply the `065-amendment` from spec `066` during this story: add `PlainTextSummary` column to the row type and EF config, inject `IHtmlTextStripper` into the upsert method, and add `SyncedAt` to the read model summary DTO — these ≤8-line changes are prerequisites for order `66` and are cheapest to deliver alongside the upsert implementation. |
| [x] | 66 | [066](./066-comprehensive-analysis-ai-query/user-story.md) / [tasks](./066-comprehensive-analysis-ai-query/tasks.md) | Comprehensive Analysis AI Query | **Priority: High.** Depends on `065` (schema, upsert with `PlainTextSummary`, read model with `SyncedAt`), `047`/`056` (MAF V2 tool adapter pattern), `009` (citations + confidence), `028` (missing-answer feedback). Add `ComprehensiveAnalysis` intent type to `AiIntentType`; `IComprehensiveAnalysisQueryParser` (LLM structured output → symbol names, topic tags, date); `IComprehensiveAnalysisQueryRepository` (EF Core tag-based retrieval with 2 000-char summary cap); `QueryComprehensiveAnalysisUseCase`; `ComprehensiveAnalysisToolAdapter` registered in MAF V2 tool registry; V1 orchestration branch for rollback compatibility; `ComprehensiveAnalysisResult` response shape with `DataCitation` per item; confidence score wired from parser certainty + resolved symbol ratio + `SyncedAt` freshness; `MissingAnswerFeedbackCollector` wired for `DataCoverageGap` and `ParserLimitation`; `PlainTextSummary` backfill admin endpoint; message persistence for conversation reload; unit + integration + architecture tests. See `066-amendment.md` for the ≤8-line changes to spec `065` artifacts that must be applied if not already done in order `65`. |

### Stage 21 - CyclicalWaves Company Mapping

This stage permanently resolves the three-way identifier mismatch between CyclicalWaves tickers,
`Companies.ExternalCompanyId` (NADPCO CoID), and `FinancialStatements.ExternalCompanyId`. Before
this stage, CyclicalWaves normalizers published tickers (e.g. `شغدیر`) as the recalculation
reference, but `MetricRecalculationProcessor` looked up `Companies` by `ExternalCompanyId` (a
numeric NADPCO CoID), so `DerivedMetrics` were never written for CyclicalWaves data.

The fix is architectural: `IFinancialPayloadNormalizer.NormalizeAsync` now returns
`NormalizationOutcome(ProcessedRecords, CanonicalExternalCompanyId?)`, propagating the canonical
company ID through the pipeline so no heuristic resolver is needed downstream.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 67 | [067](./067-cyclicalwaves-company-mapping/user-story.md) / [tasks](./067-cyclicalwaves-company-mapping/tasks.md) | CyclicalWaves Company Mapping | **Implemented 2026-06-15.** Depends on `020`, `039`, `048` (CyclicalWaves linkage, NADPCO catalog authority, stop-CW-writes). **Architectural fix:** `IFinancialPayloadNormalizer.NormalizeAsync` signature changed from `Task<int>` to `Task<NormalizationOutcome>` (carrying `ProcessedRecords` + `CanonicalExternalCompanyId?`); all 13 normalizer implementations updated; `FinancialDataSyncProcessor` publishes `outcome.CanonicalExternalCompanyId ?? request.ExternalReference`; `MetricRecalculationProcessor` simplified to direct `Companies.ExternalCompanyId == reference` lookup (heuristic multi-step resolver removed). **Schema:** `Ticker` (varchar 64, partial unique index WHERE NOT NULL) and `EnTicker` added to `Companies`; nullable `CompanyId` FK (ON DELETE SET NULL, non-unique index) added to `FinancialStatements` and `MonthlyReports`; EF migration `AddTickerEnTickerAndCompanyIdFks`. **Normalizer (TASK-003):** `PersianSymbolNormalizer` static class in Domain — strips ZWNJ/ZWJ/RLM/RLE/BOM/SHY, maps Arabic Ye→Persian Ye (U+064A→U+06CC), Arabic Kaf→Persian Kaf (U+0643→U+06A9). **Resolver (TASK-004):** `ICompanyResolverService` interface in Application; `CompanyResolverService` in Infrastructure — 5-step normalized lookup (Ticker → TseSymbol → EnTicker/SymbolIsin/CompanyIsin → InstrumentCode → CompanySymbol/Name), never throws, logs unresolved at Debug. **Mapping service (TASK-005):** `CyclicalWavesCompanyMappingService` syncs NADPCO→Companies `Ticker`/`EnTicker` (primary match by `SymbolIsin`, fallback by normalized Persian ticker; never overwrites existing values; idempotent). **Normalizer wiring (TASK-006):** `CyclicalWavesFinancialStatementNormalizer` and `CyclicalWavesMonthlyReportNormalizer` inject `ICompanyResolverService`, call `ResolveBySymbolAsync(ticker)`, set `CompanyId` on every saved row; log Warning on null resolution. **Backfill (TASK-007/008):** `BackfillCyclicalWavesCompanyIdService` (batch-500, covers both `FinancialStatements` and `MonthlyReports`, safe to re-run); `POST /api/v1/admin/cyclicalwaves/backfill-company-id` (DataAdmin only) → `{ resolved, unresolved }`. **Tests:** 11 unit tests for `PersianSymbolNormalizer`, 9 for `CompanyResolverService`, 6 for `CyclicalWavesCompanyMappingService`; existing `CyclicalWavesNormalizerTests` updated to pass `NullCompanyResolverService.Instance`; 4 integration tests for backfill endpoint (200+counts, 403 non-DataAdmin, zero-unresolved, idempotent). **Verification:** 567/567 unit tests pass, 266/267 integration tests pass (1 pre-existing unrelated failure). |

### Stage 22 - Companies-First Symbol Resolution and ExternalCompanyId Unification

This stage removes the `Symbols` table and makes `Companies.ExternalCompanyId` (NADPCO coID) the
single canonical identifier across the entire platform. All metric lookups, financial statement
lookups, monthly reports, scanner executions, and AI queries will operate through one consistent
company key. `EfCoreSymbolNameResolver` is replaced by `CompanyResolverService`. `DerivedMetrics`
and `FeatureSnapshots` replace their `SymbolId` FK with `ExternalCompanyId`. CyclicalWaves and
CodalDB normalizers stop writing to `Symbols`. Data migration is explicitly out of scope — all
affected data is re-ingested from source providers after deployment.

| Done | Order | Spec | User story | Dependency / implementation intent |
|---|---:|---|---|---|
| [x] | 68 | [068](./068-companies-first-refactor/user-story.md) | Companies-First Symbol Resolution and ExternalCompanyId Unification | **Implemented 2026-06-15.** Depends on `039`, `067`. **Schema (PHASE-1):** `Symbols` table dropped; `SymbolId (uuid)` removed from `DerivedMetrics`, `FeatureSnapshots`, `FeatureComputationJobs`; `ExternalCompanyId (varchar 64, NOT NULL)` added to `DerivedMetrics` and `FeatureSnapshots`; `ExternalCompanyId (text, nullable)` added to `FeatureComputationJobs`; unique indexes recreated on `ExternalCompanyId`; EF migration `RemoveSymbolsAddExternalCompanyId`. **Resolution layer (PHASE-2):** `EfCoreSymbolNameResolver` deleted; `ISymbolNameResolver` interface deleted; `CyclicalWavesSymbolNormalizer` deleted; `CompanyResolverService` is sole resolver; `ISymbolNameResolver` DI registration removed. **Domain (PHASE-3/4/5):** `DerivedMetric` entity: `SymbolId (Guid)` → `ExternalCompanyId (string)`; `FeatureSnapshot` entity: same change; `MetricCalculationContext`, `FeatureCalculationContext`, `FeatureSnapshotQuery`, `FeatureRecalculationRequested`, `CalculateDerivedMetricCommand`, `CalculateDerivedFeatureCommand`, `FeatureComputationJob` all updated from Guid to string. **Infrastructure:** `DerivedMetricResultStore`, `PersistedFeatureServices`, `CodalDbRatioNormalizer`, `NadpcoApiFundamentalIndexNormalizer`, `EfCoreSymbolMetricLookupService`, `EfCoreScannerExecutionService`, `MetricRecalculationProcessor`, `CodalDbSymbolNormalizer` (constructor simplified to 1 arg) — all updated to use `ExternalCompanyId`. **Architecture scan:** Solution-wide `Symbols`/`SymbolId` references eliminated from production code. **Tests:** All unit and integration test files updated — `SymbolNameResolverTests` rewritten, `CyclicalWavesNormalizerTests` stripped of deleted-class tests, `CodalDbSymbolNormalizerTests` updated to 1-arg constructor, `DerivedMetricPersistenceTests`/`DerivedFeaturePersistenceTests`/`SymbolLookupEndpointTests`/`ScannerExecutionEndpointTests`/`CodalDbGrowthMetricScannerTests` and others updated. **Build:** 0 errors, 0 warnings. Data migration is intentionally out of scope — re-ingest from source after deployment. |

| [x] | 69 | [069](./069-noavaran-monthly-sales-composite-lookup/user-story.md) / [tasks](./069-noavaran-monthly-sales-composite-lookup/tasks.md) | Noavaran Monthly Sales Composite Lookup | Depends on 057, 059, 068. Harden composite metric alias resolution and implement persisted multi-value Noavaran monthly sales snapshots (latest month, prior fiscal-year comparable month, fiscal-year-to-date sales, fiscal-year-to-previous-month sales) using ExternalCompanyId-based lookup and precomputed DerivedMetrics. Monthly production/sales lookup responses suppress market quote context (`LATEST_PRICE`, `DAILY_CHANGE_PCT`). |

| [x] | 70 | [070](./070-cyclicalwaves-monthly-sales-average-snapshot/user-story.md) / [tasks](./070-cyclicalwaves-monthly-sales-average-snapshot/tasks.md) | CyclicalWaves Monthly Sales Average Snapshot | Depends on 020, 068, 069. CyclicalWaves-only monthly/latest sales snapshots default to identity columns, latest monthly sales, `AVG_12M_MONTHLY_SALES`, YTD, and YTD-to-previous-month; explicit same-period requests replace the average column with prior-year same-month sales. Monthly production/sales answers stay compact and omit market quote columns (`LATEST_PRICE`, `DAILY_CHANGE_PCT` / `آخرین قیمت`, `درصد تغییر آخرین قیمت`) while quote context remains valid for valuation, screening, price, ratio, and market-statistic questions. Noavaran monthly activity behavior from spec 069 remains unchanged. |

| [x] | 71 | [071](./071-cyclicalwaves-derivedmetrics-full-snapshot-persistence/user-story.md) / [tasks](./071-cyclicalwaves-derivedmetrics-full-snapshot-persistence/tasks.md) | CyclicalWaves DerivedMetrics Full Snapshot Persistence | Depends on 020, 006, 068, 070. CyclicalWaves-only persistence fix: all supported ticker endpoint sales, profit, margin, monthly, and valuation snapshots must be recalculated into `DerivedMetrics` with CyclicalWaves/Rials passthrough evidence; Noavaran monthly activity behavior and unit normalization remain unchanged. |

| [x] | 72 | [072](./072-centralize-financial-metric-alias-and-intent-routing-registry/user-story.md) / [tasks](./072-centralize-financial-metric-alias-and-intent-routing-registry/tasks.md) | Centralize Financial Metric Alias and Intent Routing Registry | Depends on 020, 047, 057, 070, 071. Centralize Persian/English metric aliases, direct intent phrases, and routing capabilities into one canonical registry so workflow phrase gates, deterministic parser fallbacks, prompt trigger lists, and dynamic alias resolution stop duplicating metric vocabulary across separate hard-coded layers. |

| [x] | 72b | [073](./073-cyclicalwaves-direct-period-metric-query-coverage/user-story.md) / [tasks](./073-cyclicalwaves-direct-period-metric-query-coverage/tasks.md) | CyclicalWaves Direct Period Metric Query Coverage | **Priority: High.** Depends on `020`, `045`, `070`, `071`, and `072`. Close the remaining AI direct-question gap for CyclicalWaves snapshot fields: period-aware lookup for Q0/Q1/Q4 margin metrics, M0/M1/M12 monthly sales, M0/M12 average 12-month sales, and PE/PS valuation ratios. Add aliases, parser period selectors, exact-period DerivedMetrics lookup by `ExternalCompanyId`, Persian display labels, Missing/null behavior without period substitution, and end-to-end AI regression tests. |

| [x] | 73 | [074](./074-database-backed-metric-definition-and-alias-registry/user-story.md) / [tasks](./074-database-backed-metric-definition-and-alias-registry/tasks.md) | Database-Backed Metric Definition and Alias Registry | **Priority: High.** Depends on `015`, `058`, `072`. Migrate the in-memory `PhaseOneFinancialSemanticCatalog` to a PostgreSQL-backed registry so metric definitions, bilingual aliases, routing capabilities, and intent phrases are governed data — editable by DataAdmin without code deployments. The in-process `IMetricAliasResolver` contract remains unchanged so no scanner, symbol-lookup, or AI orchestration code requires modification. Includes DataAdmin CRUD endpoints, EF migration, seed-on-startup from the existing catalog, and alias-resolution regression tests. |

| [ ] | 74 | [075](./075-company-product-revenue-mix/user-story.md) / [tasks](./075-company-product-revenue-mix/tasks.md) | Company Product Revenue Mix | **Priority: Medium.** Depends on `068`, `072`. Enable AI queries about a company's most important products and revenue composition so users can understand which products generate the majority of revenue and evaluate business concentration risk. Current slice adds a DataAdmin one-time backfill over already-persisted Noavaran single-month `ProductSales` rows so historical `CompanyProductRevenueMix` data can be populated without re-ingestion. |


### Stage 23 - Monthly Production and Sales Trend

## Purpose

Use this checklist to implement the production/sales trend capability in dependency order.

## Ordered Checklist

| Done | Order | Spec | User story | Dependency / intent |
|---|---:|---|---|---|
| [x] | 76 | [076](./076-nadpco-monthly-activity-trend-snapshot/user-story.md) / [tasks](./076-nadpco-monthly-activity-trend-snapshot/tasks.md) | NADPCO Monthly Activity Trend Snapshot | Depends on Noavaran monthly activity ingestion and output-type segmentation. Builds the derived company-month trend table. |
| [x] | 77 | [077](./077-ai-monthly-production-sales-trend-query/user-story.md) / [tasks](./077-ai-monthly-production-sales-trend-query/tasks.md) | AI Monthly Production and Sales Trend Query | **Implemented 2026-06-24.** Depends on spec 076. Added deterministic monthly-trend intent detection plus active V2 workflow routing/persistence, chart-ready API contract/renderer output, snapshot-backed query use case wiring, and V2 API-boundary regression coverage for symbol/company-name trend questions. |
| [x] | 78 | [078](./078-frontend-monthly-activity-trend-chart/user-story.md) / [tasks](./078-frontend-monthly-activity-trend-chart/tasks.md) | Frontend Monthly Activity Trend Chart | **Implemented 2026-06-24.** Depends on `032`, `048`, `076`, and `077`. Added `MonthlyActivityTrendResult` / `MonthlyActivityTrendChartPoint` / `MonthlyActivityTrendInsight` / `MonthlyActivityTrendMissingDataPoint` TypeScript interfaces to `chat.functions.ts` mirroring the backend `MonthlyActivityTrendChartResponse` contract; extended `AssistantContentResponse` and `AssistantChatBlock` with the new field; mapped it in `mapAssistantBlock`. Created `MonthlyActivityTrendChart` component (`src/frontend/src/components/app/monthly-activity-trend-chart.tsx`) using Recharts `ComposedChart` with two `Bar` series (previous-year indigo, current-year emerald) and one `Line` for 12-month average (amber); RTL X-axis with Persian fiscal month labels; Persian tooltip; Persian digit formatting; insights and missing-data notes below the chart. Rendered conditionally in `AssistantBlock` inside `message-list.tsx` — works for live responses and conversation reload. `null` current-year values render as missing bars (not zero). `bun run build` succeeded — 0 errors. |
| [x] | 79 | [079](./079-switchable-monthly-activity-snapshot-direct-lookup/user-story.md) / [tasks](./079-switchable-monthly-activity-snapshot-direct-lookup/tasks.md) | Switchable Monthly Activity Snapshot Direct Lookup | **Implemented 2026-06-25.** Added `MonthlyActivityLookup.DirectLookupSourceMode` (`DerivedMetrics` default, `TrendSnapshot` optional), introduced a switchable `ISymbolMetricLookupService` boundary that preserves the legacy `DerivedMetrics` path, and added a snapshot-backed monthly direct lookup provider reading `CompanyMonthlyActivityTrendSnapshots` for monthly sales, same-month previous-year sales, YTD sales, YTD-through-previous-month sales, latest production/sales quantity, sales rate, sales/production growth, and sales-to-production ratio. Extended the governed semantic catalog and direct metric routing for the new monthly metric family and added focused unit coverage for parser/routing resolution, snapshot reads, and config switching. |
| [x] | 80 | [080](./080-monthly-production-sales-quality-ranking/user-story.md) / [tasks](./080-monthly-production-sales-quality-ranking/tasks.md) | Monthly Production and Sales Quality Ranking | **Implemented 2026-06-27.** Reuses `CompanyMonthlyActivityTrendSnapshots`, `CompanyProductRevenueMix`, normalized monthly activity rows, and company/industry metadata to calculate deterministic production/sales quality scores, persist `MonthlySalesQualityRankingSnapshots`, expose ranking/query + admin recalculation endpoints, trigger snapshot recalculation after single-month NADPCO monthly activity ingestion, and route Persian ranking questions through the active AI facade/V2 workflow without using LLMs for numeric scoring. |
| [ ] | 81 | [082](./082-noavaran-financial-statement-full-item-and-variant-persistence/user-story.md) / [tasks](./082-noavaran-financial-statement-full-item-and-variant-persistence/tasks.md) | Noavaran Financial Statement Full-Item And Variant Persistence | Depends on `029`, `040`, `051`; add the prerequisite data-layer changes before full financial-statement query correctness: current-API statement requests can use `items: []`, all returned vendor line items are persisted through one shared source-item catalog `(ProviderName, StatementType, SourceItemId, TitleFa, TitleEn, Unit)` plus a separate governed `MetricCode` mapping table, and same-period standalone/consolidated plus other variant rows remain separately retrievable instead of being collapsed before persistence. |
| [ ] | 82 | [081](./081-ai-financial-statement-period-analysis-query/user-story.md) / [tasks](./081-ai-financial-statement-period-analysis-query/tasks.md) | AI Financial Statement Period Analysis Query | Depends on `029`, `040`, `045`, `047`, `072`, `074`, `009`, `010`, `082`; add AI support for Persian questions about latest or requested 3/6/9/12-month NADPCO financial statements, with deterministic statement selection, consolidated/non-consolidated and audited/unaudited variant binding, income-statement/balance-sheet/ratio calculations, YoY comparisons, Persian rendering, source metadata, and regression tests, after the statement storage model can distinguish variants and resolve metrics from persisted item mappings. |

## Completion Gate

A checklist item may be marked complete only when:

- Acceptance criteria in the corresponding `user-story.md` are satisfied.
- Required tasks are implemented or explicitly deferred by scope.
- AI query path does not aggregate raw monthly report line items.
- No market quote columns leak into production/sales trend answers.
- Unit/integration/API-boundary tests pass.
- `dotnet build FinancialCopilot.sln -c Release` passes.
- `dotnet test` passes.

## Backfill Gate

Before enabling the AI trend query in production:

- [ ] Trend snapshots are rebuilt from 1403 onward.
- [ ] At least 12 months of data exist for companies where a full 12-month average is expected.
- [ ] Missing months are flagged, not backfilled with zero.
- [ ] Sample companies are manually verified against Noavaran raw outputType 0 totals.

## Supersession Notes

- `044-nadpco-api-scheduled-sync-worker` should not be implemented as a recurring worker for an archive source.
- Any scheduled ingestion should target `NoavaranCurrentApi`, not the frozen archive source.
- Spec `053` (order 54) confirms this: the only recurring fundamentals worker is `NadpcoScheduledSyncWorker` (the `NoavaranCurrentApi` source); the archive (`NoavaranArchiveSql`) is one-time/manual-only and the `NoavaranArchiveSource_IsNotDrivenByARecurringHostedWorker` architecture test enforces it.
- `030-stockmarketdb-trading-statistics-sync` remains valid as a bridge implementation, but should be followed by direct TSETMC ingestion migration.


## Deferred and Open Items

These items are explicitly deferred from their originating spec. They are not new user stories but are tracked here so future sessions have a single place to review outstanding work. Each item records the originating spec, the deferral reason, and the condition required to unblock it.

| Status | Item | Origin | Blocked on / condition to unblock |
|---|---|---|---|
| **Open** | Enable `NadpcoScheduledSync` in production | `044` | Vendor token endpoint response shape and token lifetime must be verified against a live credential. Until confirmed, `NadpcoScheduledSync:Enabled` stays `false`. DataAdmin trigger endpoint is available for controlled manual runs. |
| Deferred | `ParserLimitation` feedback classification for clarification-incomplete queries | `028` | Requires conversation-state tracking to detect when the LLM abandoned a clarification loop. Conversation-state tracking is not a Phase 1 requirement. |
| Deferred | Timer-based nightly `CodalDbNightlySyncWorker` registration | `027` | The `POST /api/v1/admin/codaldb/incremental-sync` DataAdmin endpoint is sufficient for Phase 1; a background timer is not registered. Promote when an external scheduler or operator runbook is no longer acceptable. |
| Deferred | Live SQL integration tests for `CodalDb` (`QueryChangedCompanyIdsAsync`, `QueryMaxModifiedDateTimeAsync`) | `021`, `027` | Requires a live `StockMarketDB` / CodalDB connection. Gate behind `CODALDB_INTEGRATION` env flag when added. |
| Deferred | Live NADPCO token smoke test | `038` | Requires vendor credentials. Add a `NADPCO_INTEGRATION`-gated test that calls `/api/v2/Token` and confirms the response shape and token lifetime before enabling the scheduled worker. |
| Deferred | Bounded customer-account search endpoint for Admin Billing UI | `037` | The React Admin Panel accepts an explicit Billing account GUID because no search endpoint exists. Implement `GET /api/v1/admin/billing/accounts?search=` when the panel needs discovery without a known GUID. |
| Deferred | NADPCO bounded catch-up queueing (per-dataset fan-out with `BatchSize`/`MaxConcurrency`) | `044` | `ExecutionTimeUtc`, `BatchSize`, `MaxConcurrency`, and `DatasetSelection` are persisted as scheduler run snapshots. Deeper bounded catch-up queueing remains owned by the existing NADPCO orchestration. Promote after the vendor token shape is confirmed and scheduled runs accumulate operational history. |
| Open | Runtime quote resolver filters canonical price rows by `ProviderName` — provider-name mismatch causes `Missing` quote cells | `030` / `054` | Root cause confirmed (2026-06-19): `PersistedMarketDataProvider` and related resolver code passes `MarketQuoteSourcePriorityOptions.PrimarySourceName` (default `StockMarketDb`) as a `WHERE ProviderName = ...` predicate against `IntradayTradeSnapshots`, `DailyInstrumentTrades`, and `LatestMarketQuotes`. Quote rows written by `TsetmcWebService` are silently skipped, causing `LATEST_PRICE` and `DAILY_CHANGE_PCT` to be `Missing` for affected symbols (confirmed with `شگل`). Fix: remove `ProviderName` filter from all runtime quote retrieval queries; treat `ProviderName` as provenance metadata only. Regression test seeded in `V2ShgolDirectPriceRegressionEndpointTests`. See diagnostic: `specs/bugs/shgol-direct-price-runtime-diagnostic-2026-06-19.md`. |
| Completed | TSETMC intraday trades silently dropped when instruments sync has not yet run | `054` / `064` | Fixed in order 64: `ProviderName` filter removed from `InstrumentMapByInsCodeAsync`; `EnsureInstrumentStub` auto-creates rows for unseen InsCodes. |
| Completed | `StockMarketDbSyncService` still writes duplicate `TradingInstrumentRow` records | `054` / `064` | Fixed in order 64: `PersistInstrumentsAsync` in `StockMarketDbSyncService` is now a no-op; `InstrumentMapAsync` `ProviderName` filter removed. |
| Deferred | Unique constraint on `TradingInstruments.InstrumentCode` | `064` | Legacy `ProviderName = "StockMarketDb"` rows must be cleaned up before the constraint can be added. Run the SQL cleanup script from spec 064 tasks.md after TSETMC instruments sync has covered all active instruments, then add the EF migration. |
| Deferred | Noavaran Amin company linkage via `tseCode` cross-source join | `039` / `064` | `tseCode` is already mapped to `NormalizedCompanyRow.InstrumentCode` and the join in `TsetmcDirectFeedSyncService.PersistInstrumentsAsync` already uses `InstrumentCode`. Dedicated `TseCode`/`TseCIsinCode`/`TseSIsinCode` columns are not needed unless performance or disambiguation issues arise. |
| Completed | CyclicalWaves financial statement hex ID → company linkage mismatch | bug: pe-ttm, order `48` | `CyclicalWavesFinancialStatementNormalizer` now resolves the ticker/enticker to existing NADPCO company metadata and stores the NADPCO `ExternalCompanyId` on linked financial/monthly observations. If linkage fails, the source `_id` is retained with a `MissingData` warning instead of silently creating an unjoinable company row. Existing CyclicalWaves observations should be re-normalized after the NADPCO catalog backfill so recalculation requests join through the corrected company ids. |
| Completed | NADPCO authoritative company catalog change request | `020` change request | Orders `45`-`48` implemented schema-backed NADPCO company persistence, clean-slate cleanup, company-catalog-only DataAdmin orchestration, scheduled non-destructive company refresh, and removal of CyclicalWaves company catalog writes. |

## Milestone View

| Milestone | Stories required | Completion condition |
|---|---|---|
| Foundation ready | `001`, `002`, `013` | Secure modular backend and Billing boundary exist for subsequent work. |
| Deterministic financial core ready | `003`, `015`, `004`, `005`, `006` | Normalized data and governed deterministic metric results support scanning. |
| AI execution foundation ready | `014`, minimum required scope of `018` | Model execution is provider-neutral and correlated for operational tracing. |
| Phase 1 Scanner MVP ready | `007`, `008`, `009`, `010`, `011`, `012` plus prerequisite stages | React UI can replace mocks using the single AI facade with explainability, billing, and operable data workflows. |
| Platform evolution ready | `016`, `017`, `019` and expanded `018` as promoted | Future features, evaluation, and optional memory are delivered under approved scope. |
| CodalDB source live | `021`, `022`, `023`, `024`, `025`, `026`, `027` | CodalDB coexists with CyclicalWaves: companies/symbols, statements, monthly activity, precomputed ratios, and derived growth metrics flow into the normalized tables and `DerivedMetrics`, recomputed automatically on a nightly incremental schedule and scannable through the existing facade with no engine changes. |
| NADPCO HTTP API source live | `038`, `039`, `040`, `041`, `042`, `043`, `044` | The authenticated `NadpcoApi` provider coexists with `CodalDb`: company catalog, statements, curated fundamental indexes, and product/service monthly activity flow through bounded synchronization into normalized PostgreSQL rows and provider-neutral scanner reads. Automatic scheduled incremental synchronization through the existing orchestration pipeline (`044`) is implemented and disabled by default pending vendor token confirmation. |
| NADPCO authoritative company catalog ready | `039` change request, `043` change request, `044` change request, `020` change request; checklist orders `45`-`48` | PostgreSQL `Companies` has been clean-slate repopulated from NADPCO `/api/v3/BaseInfo/Companies`; every NADPCO company response field is persisted in `Companies` or a related normalized table; recurring daily refresh inserts/updates only; company-catalog-only DataAdmin orchestration is available; CyclicalWaves no longer inserts, updates, or overwrites company catalog metadata. |
| AI orchestration V2 ready | `047` | Microsoft Agent Framework Agents, tool calling, provider abstraction, Billing integration, telemetry, memory integration, and V1/V2 runtime switching are available. Native Workflow orchestration is delivered separately in `056`. |
| Query intelligence & platform learning | `028` | Every unanswered/partial query (metric gap, data gap, parser limitation) is captured, classified, and logged as structured feedback that drives catalog expansion, data ingestion prioritization, and AI model improvements. |
| Owned web app connected | `031`, `032`, `033`, `036` | TanStack users authenticate through backend-owned Identity and reach tenant-scoped .NET APIs; chat, history, credits, watchlist quotes, and market context no longer rely on Supabase or canned financial mocks. |
| Admin operations ready | `035` | Authorized administrators can operate Identity, tenancy, plans, subscriptions, credit adjustments, and audit reads through granular tenant-scoped backend APIs without direct database changes. |
| Symbol point lookup ready | `045` | Users can ask "PE حفاری چقدر است؟" and receive the latest metric value for named symbols through the same AI facade, with freshness status, billing, and conversation persistence. |
| Native Workflow Orchestration ready | `056` | Microsoft Agent Framework V2 uses native Workflow execution primitives instead of manual orchestration. Workflow state, telemetry, step execution, fault handling, and future Deep Research / Multi-Agent expansion are supported while preserving existing API contracts and V1 rollback capability. |
| Comprehensive Analysis sync live | `065` | CyclicalWaves تحلیل جامع content is seeded in the local database via a one-time full sync and refreshed daily through a Hangfire recurring job; `PlainTextSummary` is populated at write time; the read model exposes `SyncedAt` for freshness evaluation. |
| Comprehensive Analysis AI query ready | `066` | Users can ask «آخرین تحلیل تکنیکال شغدیر را بده» or «رصد معاملات عمده این هفته» through `POST /api/ai/v1/query` and receive a structured answer drawn from the locally stored CyclicalWaves تحلیل جامع posts, with `DataCitation` per item, confidence score, missing-answer feedback, and conversation persistence — no new public endpoints, no HTML in LLM context, no RAG infrastructure required. |
## Completion Log

Add one row only after verification. Do not mark an item complete solely because source files exist.

| Date | Spec | Status | Verification evidence / notes |
|---|---|---|---|
| 2026-06-27 | `080-monthly-production-sales-quality-ranking` | Completed | Implemented deterministic monthly production/sales quality ranking end-to-end. Added application contracts for ranking query/response and recalculation, a weighted score calculator with independent confidence and reweighting for unavailable dimensions, persisted `MonthlySalesQualityRankingSnapshots` with EF migration `20260627193001_AddMonthlySalesQualityRankingSnapshots`, repository/query use cases, public ranking endpoint `GET /api/ai/v1/monthly-sales-quality-rankings`, admin recalculation endpoint `POST /api/v1/admin/monthly-sales-quality-rankings/recalculate`, and V2 AI routing/persistence/rendering for Persian ranking questions. NADPCO monthly activity normalization now triggers ranking recalculation per affected single-month Jalali period after trend/product-mix refresh, logging ranking failures without rolling back raw monthly ingestion. Added focused unit coverage for the score calculator, ranking intent rules including ZWNJ and industry-scope extraction, the ingestion recalculation hook, and updated native workflow message-contract tests for the new payload. Documentation added: `docs/monthly-sales-quality-ranking.md`. Verification: `MSBUILDDISABLENODEREUSE=1 dotnet build src/backend/FinancialCopilot.sln -m:1` passed; `MSBUILDDISABLENODEREUSE=1 dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj -m:1 --filter "FullyQualifiedName~MonthlySalesQualityRanking080Tests|FullyQualifiedName~NadpcoApiMonthlyActivityNormalizerTests|FullyQualifiedName~NativeWorkflowMessageContractTests"` passed (36). One follow-up solution build retried once on a transient Windows testhost file lock for `FinancialCopilot.Infrastructure.dll` and then succeeded unchanged. |
| 2026-06-24 | `077-ai-monthly-production-sales-trend-query` | Follow-up | Updated the trend chart contract/rendering unit from `میلیون ریال` to `میلیارد تومان` for user-facing chart consumption. Backend trend payload values now convert persisted snapshot amounts with `value * 0.0001` before they leave the trend query use case, so `LatestMonthlySalesAmount`, prior-year comparison values, YTD values, and chart-point bar/line series are all emitted in `میلیارد تومان` with decimal precision. The immediate V2 text answer and persisted assistant-content renderer now format trend values with up to three decimals instead of rounded whole-number million-rial output. Documentation for specs `077` and `078` plus the repo README were aligned to the new chart unit. Verification: `MSBUILDDISABLENODEREUSE=1 dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj -c Release -m:1 --no-restore --filter "MonthlyActivityTrend077Tests|NativeWorkflowMessageContractTests" /p:UseSharedCompilation=false` passed (41); `MSBUILDDISABLENODEREUSE=1 dotnet test tests/FinancialCopilot.IntegrationTests/FinancialCopilot.IntegrationTests.csproj -c Release -m:1 --no-restore --filter V2MonthlyActivityTrendEndpointTests /p:UseSharedCompilation=false` passed (2). One first integration attempt hit a transient Windows file lock on `FinancialCopilot.Infrastructure.dll`; rerun passed unchanged. |
| 2026-06-24 | `077-ai-monthly-production-sales-trend-query` | Completed | Implemented AI monthly production/sales trend delivery on the active `MicrosoftAgentFrameworkV2` path, not only V1. Added deterministic trend intent routing and symbol/company-name extraction, wired `MonthlyActivityTrendResponse` through V2 workflow messages/persistence/final API response, and added deterministic Persian trend rendering plus chart payload exposure in the HTTP contract. `MonthlyActivityTrendQueryUseCase` now uses the latest persisted 12-month average as the v1 horizontal reference line. Verification added/updated: unit tests for trend intent/use-case coverage and workflow message contracts, plus V2 API-boundary tests proving `/api/ai/v1/query` returns `monthlyActivityTrendResult` for symbol and company-name trend questions without quote columns or agent tool-loop selection. Verification: `MSBUILDDISABLENODEREUSE=1 dotnet build src/backend/FinancialCopilot.sln -c Release -m:1 /p:UseSharedCompilation=false` passed with 0 warnings/errors; `MSBUILDDISABLENODEREUSE=1 dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj -c Release -m:1 --no-restore --filter "MonthlyActivityTrend077Tests|NativeWorkflowMessageContractTests"` passed (40); `MSBUILDDISABLENODEREUSE=1 dotnet test tests/FinancialCopilot.IntegrationTests/FinancialCopilot.IntegrationTests.csproj -c Release -m:1 --no-restore --filter V2MonthlyActivityTrendEndpointTests /p:UseSharedCompilation=false` passed (2). Full solution `dotnet test src/backend/FinancialCopilot.sln -c Release -m:1 --no-restore /p:UseSharedCompilation=false` still reports 6 unrelated baseline integration failures in scanner/quote/confidence coverage (`ScannerExecutionEndpointTests`, `CachedScannerExecutionEndpointTests`, `ExplainableAnswerEndpointTests`, `V2ShgolDirectPriceRegressionEndpointTests`); these were not introduced by spec 077. |
| 2026-06-20 | `072-centralize-financial-metric-alias-and-intent-routing-registry` / `073-cyclicalwaves-direct-period-metric-query-coverage` | Completed | Added a shared direct-metric routing registry over the governed alias resolver so V2 preflight and deterministic symbol-lookup fallback use one metric/period phrase source instead of disconnected hard-coded lists. Extended symbol lookup pairs with optional period selectors, exact-period CyclicalWaves row selection over `DerivedMetrics` keyed by `ExternalCompanyId`, Persian period-specific display labels, AVG-12M alias coverage for `میانگین فروش ... ماهه`, and Missing/null behavior without silent period substitution. Preserved the older monthly-sales snapshot path for `... مشابه دوره قبل` phrasing while enabling exact-period lookups for `... سال قبل`. Verification: `MSBUILDDISABLENODEREUSE=1 dotnet build src/backend/FinancialCopilot.sln --configuration Release -m:1 --no-restore` passed with 0 warnings/errors; `MSBUILDDISABLENODEREUSE=1 dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --configuration Release --filter "FullyQualifiedName~SymbolLookupParserTests|FullyQualifiedName~FinancialSemanticLayerTests|FullyQualifiedName~SymbolLookupToolAdapterTests" -m:1 --no-restore` passed (73); `MSBUILDDISABLENODEREUSE=1 dotnet test tests/FinancialCopilot.IntegrationTests/FinancialCopilot.IntegrationTests.csproj --configuration Release --filter "FullyQualifiedName~CyclicalWavesDirectPeriodMetricLookupTests|FullyQualifiedName~MonthlySalesLookupTests|FullyQualifiedName~CyclicalWavesMonthlySalesLookupTests|FullyQualifiedName~PeSymbolLookupRegressionTests|FullyQualifiedName~SymbolLookupEndpointTests" -m:1 --no-restore` passed (39). |
| 2026-06-17 | `045` / `047` / `057` / `059` / `069` / `070` / `071` | Spec alignment / regression hardening | Aligned symbol lookup, monthly-sales routing, renderer ownership, CyclicalWaves revenue persistence, and V2 tool-routing specs after recurring `آخرین فروش کچاد` regressions. Spec 045 now explicitly follows post-068 `Companies -> ExternalCompanyId -> DerivedMetrics / FinancialStatements / MonthlyReports` lookup and removes the active `Symbols`/`SymbolId`/`ISymbolNameResolver` model. Specs 057/059/069/070/071 now share the monthly-sales routing rule: `فروش`, `آخرین فروش`, `فروش ماه`, `فروش ماهانه`, `فروش این ماه`, `فروش YTD`, and `متوسط فروش 12/۱۲ ماهه` route to `MONTHLY_SALES`, while explicit `درآمد فصلی` / `فروش فصلی` route to `REVENUE`. Spec 047 now requires V2 tools to preserve original user message and treat LLM tool arguments as advisory. Architecture note `docs/tahlilapp-ai-data-flow.md` was updated from `ISymbolNameResolver`/`Symbols` to `CompanyResolverService`/`ExternalCompanyId` and documents monthly-sales renderer ownership. Implementation hardening added explicit `درآمد فصلی` and `فروش فصلی` aliases for `REVENUE`, parser regression tests for those negative monthly-sales cases, and an API regression proving `متوسط فروش 12 ماهه کچاد` still uses the CyclicalWaves monthly-sales snapshot with `AVG_12M_MONTHLY_SALES` and no `REVENUE`/price columns. Verification: `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --filter "FullyQualifiedName~SymbolLookupParserTests" -m:1 --no-restore` passed (25); `MSBUILDDISABLENODEREUSE=1 dotnet test tests/FinancialCopilot.IntegrationTests/FinancialCopilot.IntegrationTests.csproj --configuration Release --filter "FullyQualifiedName~MonthlySalesLookupTests" -m:1 --no-restore` passed (6); `MSBUILDDISABLENODEREUSE=1 dotnet build src/backend/FinancialCopilot.sln --configuration Release -m:1 --no-restore` passed with 0 warnings/errors. |
| 2026-06-17 | `070-cyclicalwaves-monthly-sales-average-snapshot` | Regression fix | Hardened latest-sales routing after the CyclicalWaves DerivedMetrics mapping work. V2 `lookup_symbol_metrics` now passes the original user message to the deterministic symbol parser instead of an LLM-rewritten tool argument, preventing rewrites such as `REVENUE کچاد` from bypassing monthly-sales routing. `LlmSymbolLookupParser` now forces sales-snapshot phrases (`فروش`, `آخرین فروش`, `فروش ماه`, `فروش ماهانه`, `فروش این ماه`, `فروش YTD`, `متوسط فروش 12 ماهه`/`۱۲ ماهه`) to `MONTHLY_SALES` when the model emits sales/revenue/average-sales terms. Regression tests cover `REVENUE کچاد`, `revenue`, `sales`, and `AVG_12M_MONTHLY_SALES` parser outputs; monthly-sales API tests still prove the CyclicalWaves KCHAD response contains the sales snapshot table and excludes `REVENUE`, `LATEST_PRICE`, and `DAILY_CHANGE_PCT`. Verification: `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --filter "FullyQualifiedName~SymbolLookupParserTests" -m:1 --no-restore` passed (23); `MSBUILDDISABLENODEREUSE=1 dotnet test tests/FinancialCopilot.IntegrationTests/FinancialCopilot.IntegrationTests.csproj --configuration Release --filter "FullyQualifiedName~MonthlySalesLookupTests" -m:1 --no-restore` passed (5); `MSBUILDDISABLENODEREUSE=1 dotnet build src/backend/FinancialCopilot.sln --configuration Release -m:1 --no-restore` passed with 0 warnings/errors. |
| 2026-06-17 | `071-cyclicalwaves-derivedmetrics-full-snapshot-persistence` | Completed | Added CyclicalWaves-only full snapshot persistence into `DerivedMetrics`: mapped `last_year_average_12_month_sale`, registered self-source passthrough definitions/policies/calculators for quarterly `REVENUE`, `NET_PROFIT`, `GROSS_PROFIT`, and `OPERATING_PROFIT`, preserved CyclicalWaves Rials passthrough evidence for monetary values, added percent evidence for margins, and kept Noavaran monthly activity normalization untouched. `MetricRecalculationProcessor` now resolves historical self-sourced source periods against the active semantic catalog date so prior-year same-month CyclicalWaves source rows can persist without creating historical null growth rows. Regression coverage seeds KCHAD values and asserts `AVG_12M_MONTHLY_SALES`, `MONTHLY_SALES` including prior same-month, `REVENUE`, `AVG_4Q_REVENUE`, profit metrics, margins, `PE_TTM`, and `PS_TTM` are persisted with CyclicalWaves evidence and no `NoavaranCurrentApi` evidence. Verification: `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --filter "FullyQualifiedName~CyclicalWavesNormalizerTests|FullyQualifiedName~CyclicalWavesRelativePeriodResolverTests|FullyQualifiedName~DerivedMetricCalculatorTests" -m:1 --no-restore` passed (31); `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --filter "FullyQualifiedName~MetricRecalculationProcessorTests|FullyQualifiedName~FinancialSemanticLayerTests" -m:1 --no-restore` passed (17); `MSBUILDDISABLENODEREUSE=1 dotnet build src/backend/FinancialCopilot.sln --configuration Release -m:1 --no-restore` passed with 0 warnings/errors. |
| 2026-06-16 | `069-noavaran-monthly-sales-composite-lookup` | Completed | Hardened `LlmSymbolLookupParser` so slash-separated composite metric phrases first try the literal term, then resolve individual `/` segments with user-message/user-language precedence; this fixes LLM output such as `latest monthly sales / sales / revenue` without changing the raw term recorded on the parsed pair. Added governed persisted monthly-sales facts `MONTHLY_SALES_YTD` and `MONTHLY_SALES_YTD_PREVIOUS_MONTH`, backed by Noavaran `MonthlyReports.OutputType=1` and `OutputType=4`; registered input sources, semantic definitions, source policies, recalculation dataset candidates, DI registrations, and single-component calculators so ingestion materializes them into `DerivedMetrics`. Extended `EfCoreSymbolMetricLookupService` so a `MONTHLY_SALES` lookup returns a composite persisted snapshot: latest monthly sales, prior fiscal-year same Persian month from persisted prior `MONTHLY_SALES`, fiscal-year-to-date sales, and fiscal-year-to-previous-month sales; lookup remains `ExternalCompanyId`-based and does not aggregate `MonthlyReportLineItems` at query time. Tests added/updated for parser composite resolution, OutputType 1/4 input filtering, and `/api/ai/v1/query` monthly-sales snapshot response. Verification: `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --filter "FullyQualifiedName~SymbolLookupParserTests|FullyQualifiedName~MonthlyActivityMetricInputSourceTests" -m:1 --no-restore` passed (24); `dotnet test tests/FinancialCopilot.IntegrationTests/FinancialCopilot.IntegrationTests.csproj --filter "FullyQualifiedName~MonthlySalesLookupTests" -m:1` passed (1). One first parallel unit run hit a transient Defender file lock while the integration project was also building shared assemblies; rerun passed. |
| 2026-06-17 | `069-noavaran-monthly-sales-composite-lookup` | Follow-up | Updated related monthly-sales specs and lookup rendering so monthly production/sales answers no longer show stock price or daily price change. `EfCoreSymbolMetricLookupService` now omits `LATEST_PRICE` and `DAILY_CHANGE_PCT` when every requested display metric is a monthly production/sales metric (`MONTHLY_SALES`, composite monthly sales columns, sales quantity/rate, production quantity, monthly sales growth, or TTM sales), while keeping market context for valuation and non-monthly metric lookups. V2 prompt guidance was aligned, and the monthly-sales API regression test asserts the price/change columns are absent. Verification: `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --filter "FullyQualifiedName~SymbolLookupParserTests|FullyQualifiedName~MonthlyActivityMetricInputSourceTests" -m:1 --no-restore` passed (26); `dotnet test tests/FinancialCopilot.IntegrationTests/FinancialCopilot.IntegrationTests.csproj --filter "FullyQualifiedName~MonthlySalesLookupTests" -m:1 --no-restore` passed (1); `MSBUILDDISABLENODEREUSE=1 dotnet build src/backend/FinancialCopilot.sln --configuration Release -m:1 --no-restore` passed. One first parallel integration run hit a transient Windows file lock on `FinancialCopilot.Infrastructure.dll`; rerun passed. |
| 2026-06-17 | `045-symbol-metric-point-lookup` / `009-explainable-results` / `069-noavaran-monthly-sales-composite-lookup` | Follow-up | Added the shared formatted-number rule to specs and implementation. Monetary, price, market-cap, volume, quantity, production, and sales amount cells render as grouped whole numbers without `.00`; ratio-like metrics (`P/E`, `P/S`, `P/B`, percentages, growth, margins, ROE/ROA) keep meaningful decimals but trim trailing zero-only fractions. `FinancialNumberFormatter` now centralizes scanner/symbol-lookup formatted values, and filter threshold formatting trims redundant zeroes. Verification: `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --filter "FullyQualifiedName~ScannerResultColumnPolicyTests|FullyQualifiedName~AnswerConsistencyTests|FullyQualifiedName~ExplainableAnswerTests" -m:1 --no-restore` passed (20); `MSBUILDDISABLENODEREUSE=1 dotnet test tests/FinancialCopilot.IntegrationTests/FinancialCopilot.IntegrationTests.csproj --configuration Release --filter "FullyQualifiedName~SymbolLookupEndpointTests|FullyQualifiedName~PeSymbolLookupRegressionTests|FullyQualifiedName~MonthlySalesLookupTests|FullyQualifiedName~AiFacadeV2EndpointTests" -m:1 --no-restore` passed (16); `MSBUILDDISABLENODEREUSE=1 dotnet build src/backend/FinancialCopilot.sln --configuration Release -m:1 --no-restore` passed. Debug integration build was blocked by a running local `FinancialCopilot.API` process from Visual Studio locking Debug output DLLs; Release verification passed. |
| 2026-06-17 | `069-noavaran-monthly-sales-composite-lookup` | Follow-up | Tightened monthly-sales snapshot rendering so the table is the answer. When any monthly-sales snapshot cell has a non-missing value, deterministic symbol-lookup text is exactly `Unit: million Rials`; LLM-authored explanatory prose, clarification suggestions, and false "value did not return" language are suppressed even when that prose contains no numeric conflict. Verification: `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --filter "FullyQualifiedName~AnswerConsistencyTests" -m:1 --no-restore` passed (12); `MSBUILDDISABLENODEREUSE=1 dotnet test tests/FinancialCopilot.IntegrationTests/FinancialCopilot.IntegrationTests.csproj --configuration Release --filter "FullyQualifiedName~MonthlySalesLookupTests" -m:1 --no-restore` passed (1); `MSBUILDDISABLENODEREUSE=1 dotnet build src/backend/FinancialCopilot.sln --configuration Release -m:1 --no-restore` passed. |
| 2026-06-17 | `069-noavaran-monthly-sales-composite-lookup` | Follow-up | Completed the response-composition fix beyond `TextAnswer`: monthly-sales snapshot persisted assistant content now uses the deterministic symbol-lookup prose builder, so the immediate API response, persisted `MessageResponse.Content`, and structured `AssistantContent.TextAnswer` render only `Unit: million Rials` beside the table when sales data exists. Added the exact `آخرین فروش کچاد؟` API-boundary regression with seeded canonical Rial values rendered as million Rials (`90,879,722`, `787,016,400`, `605,344,668`), no price/change columns, no false missing-data prose, and a reloaded conversation assertion that narrative fields are empty/null or exactly the unit label. Verification: `MSBUILDDISABLENODEREUSE=1 dotnet test tests/FinancialCopilot.IntegrationTests/FinancialCopilot.IntegrationTests.csproj --configuration Release --filter "FullyQualifiedName~MonthlySalesLookupTests" -m:1 --no-restore` passed (2); `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --filter "FullyQualifiedName~AnswerConsistencyTests" -m:1 --no-restore` passed (12); `MSBUILDDISABLENODEREUSE=1 dotnet build src/backend/FinancialCopilot.sln --configuration Release -m:1 --no-restore` passed with 0 warnings/errors. |
| 2026-06-17 | `069-noavaran-monthly-sales-composite-lookup` | Follow-up | Synced the monthly-sales story/tasks with the Persian frontend rendering rule: `Unit: million Rials` remains a backend compatibility value only, while the chat UI localizes it to `واحد: میلیون ریال` as muted table metadata at the top-left of the table container. Updated `mapAssistantBlock` and `MessageList` so the technical unit string is suppressed as standalone assistant text and monthly-sales tables remain the main answer; added a React component regression for `آخرین فروش کچاد چقدر بوده؟` proving the localized label appears in the table metadata area and the raw English string is absent. Verification: `npm.cmd test -- message-list.test.tsx` passed (1); `npm.cmd run build` passed. |
| 2026-06-17 | `070-cyclicalwaves-monthly-sales-average-snapshot` / `069-noavaran-monthly-sales-composite-lookup` | Follow-up | Corrected the provider scope for monthly-sales average snapshots. The `متوسط فروش ۱۲ ماهه` / `AVG_12M_MONTHLY_SALES` default applies only to CyclicalWaves-sourced `DerivedMetrics`; explicit CyclicalWaves same-period requests replace it with `فروش ماه مشابه دوره قبل` and never fall back to the average when prior-year data is missing. Noavaran monthly activity behavior from spec 069 remains unchanged: default Noavaran snapshots keep `فروش ماه مشابه دوره قبل`, YTD, and YTD-to-previous-month and do not use the average column. Specs 057/059/069 were restored to Noavaran semantics, and new spec 070 captures the CyclicalWaves-only behavior. Verification: `dotnet test tests/FinancialCopilot.IntegrationTests/FinancialCopilot.IntegrationTests.csproj --configuration Release --filter "FullyQualifiedName~MonthlySalesLookupTests" -m:1 --no-restore` passed (5); `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --filter "FullyQualifiedName~AnswerConsistencyTests" -m:1 --no-restore` passed (12); `MSBUILDDISABLENODEREUSE=1 dotnet build src/backend/FinancialCopilot.sln --configuration Release -m:1 --no-restore` passed with 0 warnings/errors; `npm.cmd test -- message-list.test.tsx` passed (1); `npm.cmd run build` passed. |
| 2026-06-29 | `nadpco-monthly-report-lineitems-extra-products-data-integrity-2026-06-24` / `duplicate-monthly-report-line-items-natural-key-collision-2026-06-25` | Correction note | Corrected the bug-contract docs after they were found to allow or describe the wrong report identity model. Canonical Noavaran monthly report identity excludes `categoryId` in current ingestion, backfill, and fallback identity paths; category remains line-item evidence only. Added a selective SQL repair script for existing `:category-{id}` report rows and regression tests that lock the canonical fallback key and prevent numeric `CategoryID` from splitting logical line items. |
| 2026-06-17 | `070-cyclicalwaves-monthly-sales-average-snapshot` | Regression fix | Fixed latest-sales alias routing so `آخرین فروش کچاد چقدر بوده؟`, `فروش ماهانه ...`, and `فروش کچاد` stay on `MONTHLY_SALES` / monthly-sales snapshot even if the LLM parser emits a generic sales term such as `فروش` or `REVENUE`. The CyclicalWaves default table remains `فروش ماهانه`, `متوسط فروش ۱۲ ماهه`, `فروش YTD`, `فروش YTD تا ماه قبل`; it does not render `REVENUE`, `LATEST_PRICE`, or `DAILY_CHANGE_PCT`. Noavaran spec 069 behavior remains unchanged. Verification: `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --filter "FullyQualifiedName~SymbolLookupParserTests" -m:1 --no-restore` passed (19); `dotnet test tests/FinancialCopilot.IntegrationTests/FinancialCopilot.IntegrationTests.csproj --configuration Release --filter "FullyQualifiedName~MonthlySalesLookupTests" -m:1 --no-restore` passed (5). |
| 2026-06-10 | `057-nadpco-monthly-activity-freshness-and-sales-lookup` / order `61` | Completed | **Phase A (manual reverse backfill):** Application-pure `ShamsiMonthCalculator` (`ShamsiMonth` struct; previous-month with Farvardin→Esfand rollover; descending sequence to the permitted `1404/01` floor; Persian-calendar last-day). `MonthlyActivityBackfillCoordinator` (+ `IMonthlyActivityBackfillCoordinator`/`IMonthlyActivityBackfillStateReader` contracts) walks months newest-first (`140502 → … → 140401`), enqueueing one bounded company-month `DataSyncRequest` per eligible company per month through the existing publisher/consumer/normalizer pipeline with deterministic idempotency keys (`nadpco-monthlybf-{yyyyMM}-{coID}`) — completed pairs are skipped on re-start and failed pairs retried (resume, never restart); per-month progress is computed from `ProviderSyncRuns` and the planned scope persisted in new single-row `MonthlyActivityBackfillStates`, whose `IsCompleted` is the durable backfill-complete marker. DataAdmin endpoints `POST/GET /api/v1/admin/noavaran-current/monthly-backfill` (start idempotent — `AlreadyCompleted` no-op; progress shows months completed/remaining/failed); never scheduler-invoked. The month window travels on `DataSyncRequest.SourceDateRangeStart/EndJalali` (persisted on run rows = run-state evidence), is applied per-run via the extended scoped `INoavaranCurrentApiBoundaryOverride.SetMonthlyActivityWindow`, and the provider client still clamps to the vendor-permitted 1404 floor. **Phase B (steady-state):** incremental `NadpcoApiScheduledSyncService` runs request monthly activity ONLY for the previous Shamsi month once the marker exists (key suffix `-m{yyyyMM}`), and skip monthly activity entirely (warning) while it does not; manual full-reload keeps the configured full range. **Phase C (read path):** catalog now defines `MONTHLY_SALES` (aliases «فروش ماهانه», «آخرین فروش», …), `MONTHLY_SALES_QUANTITY` («مقدار فروش»), `MONTHLY_PRODUCTION_QUANTITY` («تولید», «مقدار تولید»), `MONTHLY_SALES_RATE` («نرخ فروش») as Monthly metrics with self-sourced dependencies + `monthly-*-source-v1` identity policies; single-component `AdditiveCompositeMetricCalculator`s persist one `DerivedMetrics` row per company-month; new input sources aggregate `MonthlyReportLineItems` (quantity sums over carrying lines; rate = quantity-weighted Σamount÷Σquantity); `MetricRecalculationProcessor` dataset map extended. Bare «فروش» stays on quarterly `REVENUE` by documented policy. Line-item model audit: added normalized `Title`/`Unit`/`SalesRate` columns (vendor rate was evidence-JSON-only) populated by the normalizer. **Noavaran company scope (user change request):** ALL per-company current-API requests (scheduled sync, monthly backfill, fundamental-index catch-up) now enumerate companies through `NoavaranCompanyScope` — `PrecedencyRight = 0` (equities, no حق تقدم) on بورس/فرابورس/پایه market ids — mirrored by the `NoavaranEligibleCompanies` PostgreSQL view for operators; the company-catalog sync stays unscoped. EF migration `20260610100455_AddMonthlyActivityBackfillAndLineItemDetails` (additive: 3 line-item columns, backfill state table, the view) generated + applied locally. Verification: full build clean (0 warn/0 err); Unit 484/484 (incl. 7 new `ShamsiMonthCalculatorTests`, 5 new `MonthlyActivityBackfillCoordinatorTests` covering newest-first windows/resume-skip/marker/failed-month/scope, 5 new `MonthlyActivityMetricInputSourceTests`, 7 new `MonthlyMetricAliasResolutionTests`, 4 new scheduled-sync gating/scope tests); Architecture 5/5; Integration 248 passed incl. new `MonthlySalesLookupTests.AiQuery_LatestMonthlySales_ReturnsLatestPersistedMonth` («آخرین فروش غگلپا چقدر است؟» through `POST /api/ai/v1/query` returns the latest persisted month value 987,654,321 — never the deliberately-seeded quarterly REVENUE); the 10 remaining integration failures are the documented pre-existing `SymbolLookupEndpointTests`/`PeSymbolLookupRegressionTests` confidence failures confirmed identical on a clean baseline. **Live vendor contract verified (spec task 2, 2026-06-10):** operator-run credentialed calls confirmed both monthly endpoints take Shamsi bounds as year+month query-string tokens (`fromDate=140502&toDate=140502`, `outputTypeId` for ProductSales) with `companyIds` in the JSON body; sending dates in the body makes v3 ServiceSales return HTTP 500 (the root cause of 805 historical monthly-sync failures and zero `MonthlyReports` rows). Client rewritten to the verified contract; live v2 nested `productSales[]` response shape and v3 `revenueDuringThePeriod` field captured as DTO/normalizer regression fixtures (`Normalize_LiveNestedProductSalesShape_*`, `Normalize_LiveServiceSalesShape_*`, updated provider/boundary tests); `productId=0` placeholder now falls back to deterministic natural keys; ServiceSales failures are isolated (empty payload + warning) so they can no longer poison product-sales persistence; JSON extension-data numeric helpers guard `ValueKind` (latent string-typed-number crash fixed). Post-fix suite: Unit 488/488, Architecture 5/5, Integration 248 + the same 10 pre-existing failures. |
| 2026-06-09 | `050-nadpco-api-all-fundamental-index-catchup` / order `55` | Completed | Added the all-index coverage catch-up alongside the curated 041 path (curated path untouched — still the only DerivedMetrics writer). New `ProviderDataset.FundamentalIndexCoverage` + `IFundamentalIndexCoverageProvider`; `NadpcoApiDataProviderClient.FetchAllFundamentalIndexesAsync` sends empty `companyIndexIds` with explicit `fromYear`/`toYear` (AC #1/#3). New non-scannable staging table `NadpcoFundamentalIndexObservations` (unique key `(provider, company, indexId, periodType, periodEnd)`) + `NadpcoApiFundamentalIndexCoverageNormalizer` that persists EVERY vendor index (mapped and unmapped), flags governed candidates via the 041 allowlist (flag only, no filtering), mirrors the curated deterministic variant selection and Jalali→Gregorian conversion, preserves company/index/group/value/unit + audited/represented/composing + Jalali dates + announcement + raw-payload checksum, and never writes DerivedMetrics (AC #5/#6/#7/#8). `FundamentalIndexCatchUpCoordinator` enumerates local NADPCO companies and enqueues bounded per-company coverage requests (year range carried in `SourceDateRangeStart/EndJalali`; `MaxReadParallelism`-bounded; per-company failure isolation; run lease rejects concurrent runs; AC #2/#9). DataAdmin endpoints `POST /api/v1/admin/nadpcoapi/fundamental-index-catch-up` (default 1403→1405, validated) + `.../runs`; run history in `FundamentalIndexCatchUpRuns` (AC #4/#9). EF migration `20260609105852_AddNadpcoFundamentalIndexCoverage` (two new tables; additive) generated + applied. Docs: `docs/nadpco-fundamental-index-catchup.md`. Verification (API/Worker stopped): full build clean (0 err); unit suite 447 passed / 1 failed — same pre-existing unrelated `NadpcoApiCompanyNormalizerTests.Normalize_PopulatesSupportedCompanyMetadata` (`TseSymbol` `ABYP` vs `آبین`); new `NadpcoFundamentalIndexCoverageTests` 4/4 pass (all-index incl. unmapped persisted without DerivedMetrics, provenance + Jalali period, deterministic audited-variant selection, idempotent rerun); ArchitectureTests 5/5; `AdminDataOperationsEndpointTests` 42/42 (incl. 3 new catch-up tests: default 1403→1405, invalid-range rejected, normal-user-forbidden). Deferred (spec open questions): scheduled-selection of catch-up and governed-promotion of additional reviewed indexes remain future work. |
| 2026-06-09 | `053-noavaran-current-api-ingestion` / order `54` | Completed | Consolidated the current-API ingestion story: most machinery already existed from specs 042–044 (scheduled worker, run history, status/runs endpoints, provenance stamping `LogicalVendor=NoavaranAmin`/`SourceMode=CurrentIncremental`, cache invalidation + recalculation, canonical-symbol reuse) and was renamed to `NoavaranCurrentApi` by spec 051 (AC #1/#2/#5/#6/#7/#8/#9 already satisfied; verified, not rebuilt — no second ingestion path). Added the genuine gaps: (AC #4) boundary-driven gap report `GET /api/v1/admin/noavaran-current/gaps` via `ICurrentApiGapReader`/`EfCoreCurrentApiGapReader` — compares archive vs current-API row counts per (company, dataset, Gregorian fiscal year) at/after the boundary and reports periods the current API covers that the archive lacks (pure read; never touches archive freeze/import state). (AC #3) DataAdmin backfill `POST /api/v1/admin/noavaran-current/backfill` with optional `fromShamsiYear` that lowers the start boundary for that run only (no persisted config mutation); monthly activity stays clamped to the vendor-permitted 1404 boundary. The override travels with each `DataSyncRequest` (new `FromShamsiYearOverride`) so it reaches the worker scope; `FinancialDataSyncProcessor` applies it to a scoped `INoavaranCurrentApiBoundaryOverride` that the provider client consults for `fromYear`/`fromDate`, falling back to configured options. `INadpcoApiScheduledSyncService.ExecuteAsync` gained an optional `fromShamsiYearOverride` (stamped on enqueued company requests, distinct idempotency keys). (AC #9) separate current-API health `GET /api/v1/admin/noavaran-current/health` via `ICurrentApiBackfillCoordinator.GetHealthAsync` (current-API provider health + latest scheduled-sync status), distinct from archive freeze/import state. (AC #10) backfill coordinator has no archive-state dependency, so current-API failures cannot change archive freeze/import. No new migration (gap report reads existing rows; backfill reuses the existing pipeline). Docs: `docs/noavaran-current-api-ingestion.md`. Verification (API/Worker stopped to release locks): full build Domain→Application→Infrastructure→API→tests clean (0 err); unit suite 443 passed / 1 failed — same pre-existing unrelated `NadpcoApiCompanyNormalizerTests.Normalize_PopulatesSupportedCompanyMetadata` (`TseSymbol` `ABYP` vs `آبین`); new `NoavaranCurrentApiBoundaryTests` 2/2 pass (override lowers statement fromYear, monthly clamps to 1404; no-override uses configured 1403); ArchitectureTests 5/5 pass; `AdminDataOperationsEndpointTests` 39/39 pass (incl. 5 new current-API tests: health, gaps, backfill-passes-override, backfill-rejects-implausible-year, normal-user-forbidden). Updated the unit + integration stubs for the new `ExecuteAsync` signature. |
| 2026-06-09 | `052-noavaran-archive-one-time-import` / order `53` | Completed | Added the DataAdmin one-time archive import lifecycle over the existing archive ingestion path (single source of truth; no second pipeline). Application contracts `ArchiveImportContracts.cs`: `ArchiveImportAction` (DryRun/Import/Validate/Freeze/ReImport), `ArchiveImportRunStatus` (incl. `RejectedFrozen`), `ArchiveImportDataset` scope, `ArchiveImportRequest`/`Run`, `ArchiveFreezeState`, `ArchiveCoverageSummary`/`Row`, `ArchiveImportValidationResult`, and `IArchiveImportCoordinator`/`IArchiveImportRunReader`/`IArchiveCoverageReader`/`IArchiveFreezeStateStore`. Infrastructure `ArchiveImport.cs`: `ArchiveImportCoordinator` (freeze gate — a normal Import on a frozen archive returns `RejectedFrozen`; a `ReImport` requires an explicit reason that is persisted, AC #3/#5), `EfCoreArchiveImportRunRepository` (run history + running-lease + hung recovery, mirroring the NADPCO scheduled-sync repository, AC #6), `EfCoreArchiveFreezeStateStore` (single-row freeze marker keyed by source name), `EfCoreArchiveCoverageReader` (coverage by dataset + Gregorian fiscal year from `PeriodEnd`, AC #9), and `EvaluateCompanyMappingAsync` (validates archive company→canonical-symbol mapping, AC #7). Added optional `dryRun` to `ICodalDbScheduledSyncService.ExecuteAsync` (counts without enqueue/watermark, AC #2). DataAdmin endpoints (AC #1) `POST/GET /api/v1/admin/noavaran-archive/{dry-run|import|re-import|validate|freeze|freeze-state|runs|coverage}` with dataset scoping (AC #8). AC #10: additive `SourceProvider` on `ScannerTableRow` (from company `ProviderName`), `DataCitation`, and `DataCitationResponse` so explainable answers cite archive provenance. AC #4: archive import is not a hosted service; the architecture guard `NoavaranArchiveSource_IsNotDrivenByARecurringHostedWorker` was extended to also reject an `ArchiveImport` hosted worker. EF migration `20260609085843_AddArchiveImportRunsAndFreezeState` (two new tables `ArchiveImportRuns`/`ArchiveFreezeStates`; purely additive — no changes to existing tables) generated and applied (`database update` reported up-to-date). Docs: `docs/noavaran-archive-one-time-import.md`. Verification (API/Worker stopped to release file locks): `dotnet build` Domain→Application→Infrastructure→API clean (0 warn/0 err); unit `ArchiveImportCoordinatorTests` 5/5 pass; full unit suite 441 passed / 1 failed — the same pre-existing unrelated `NadpcoApiCompanyNormalizerTests.Normalize_PopulatesSupportedCompanyMetadata` (`TseSymbol` `ABYP` vs `آبین`); ArchitectureTests 5/5 pass (incl. the extended no-recurring-archive-worker guard); `AdminDataOperationsEndpointTests` 34/34 pass (incl. 4 new archive endpoint tests: dry-run, freeze-then-import-rejected, coverage, normal-user-forbidden). Also updated the integration factory's `StubCodalDbScheduledSyncService` to the new `dryRun` signature. Deferred to order 54 (spec 053): actual current-API ingestion from 1404 onward. |
| 2026-06-08 | `051-noavaran-archive-and-current-api-strategy` / order `52` | Completed | Corrected the source model: Noavaran Amin is one logical vendor with archive (`NoavaranArchiveSql`, was `CodalDb`) and current API (`NoavaranCurrentApi`, was `NadpcoApi`) physical sources. Added authoritative `ProviderSources` catalog + `LogicalVendor`/`PhysicalSource`/`SourceMode` enums and `ProviderSourceProvenance` (Application). Renamed the two physical source names through the catalog across options classes, config sections (`appsettings.json` API+Worker), provider router wiring, normalizers, admin controller, display resolver, and CyclicalWaves linkage — one authoritative owner, no scattered literals. Added row-level provenance (`LogicalVendor`/`SourceMode`) to `Companies`/`FinancialStatements`/`MonthlyReports` and batch-level provenance (`LogicalVendor`/`PhysicalSource`/`SourceMode`/Shamsi date range) to `ProviderSyncRuns`; `FinancialDataSyncProcessor` derives and persists it. Added `DataSyncRequest.Mode`/`SourceDateRange*`; the archive orchestrator stamps `ArchiveOneTime`, the current API orchestrator stamps `CurrentIncremental`. Added `SourcePriorityOptions`/`ISourcePriorityResolver` (per-dataset priority + Shamsi 1403 archive/current boundary), `IIdentityConflictLog`/`LoggingIdentityConflictLog` (coalesced, bounded, non-throwing cross-source identity-conflict logging), and `ISourceFreshnessReader`/`SourceFreshnessReader` + `GET /api/v1/admin/source-freshness` reporting frozen archives distinctly from current sources. Archive remains one-time/manual-only (admin endpoints `codaldb/full-sync`/`incremental-sync`); architecture test `NoavaranArchiveSource_IsNotDrivenByARecurringHostedWorker` guards against a recurring archive worker. EF migrations: `20260608120102_AddNoavaranSourceProvenanceAndRename` (FinancialIngestion — provenance columns/indexes + in-place `ProviderName` rename and provenance backfill) and `20260608120500_RenameProviderRawPayloadSourceNames` (FinancialProvider — data-only payload rename). Docs: `docs/noavaran-amin-source-strategy.md`. Tests: 5 new unit (`NoavaranSourceStrategyTests`) + 2 new integration (`NoavaranSourceCoexistenceTests`) pass; rename-affected unit/integration assertions updated to catalog constants. Verification (sandbox lacks NuGet network for Worker/ArchitectureTests restore and lacks Postgres/AI for some integration suites): `dotnet build` Domain→Application→Infrastructure→API clean (0 warn/0 err); `dotnet test tests/FinancialCopilot.UnitTests` 436 passed / 1 failed — the pre-existing unrelated `NadpcoApiCompanyNormalizerTests.Normalize_PopulatesSupportedCompanyMetadata` (`TseSymbol` expects `ABYP`, normalizer yields `آبین`), already noted in the order-51 entry; Integration source-coexistence + ingestion + rename-sensitive suites pass; the 10 integration failures (`SymbolLookupEndpointTests`/`PeSymbolLookupRegressionTests`) were confirmed to fail identically on a clean stash baseline — environment/AI-pipeline dependent, not caused by this change. Deferred to later orders: actual archive one-time import (order 53), current API ingestion (order 54), one-time import run-state record (spec 052). |
| 2026-06-06 | `049-llm-provider-switching-deepseek-support` / order `51` | Completed | Added explicit `AiProvider` configuration with `DefaultProvider` routing for OpenAI/DeepSeek while preserving existing `AiModelProviders` registrations. Added `DeepSeekHostedAiModelTransport` over Chat Completions with JSON structured output, function tool calling, single-completion streaming adapter support, health check, retry/error mapping, and normalized token usage. Added provider diagnostics `GET /api/admin/ai/provider` and `GET /api/v1/admin/ai/provider` under DataAdmin. Added per-correlation AI usage accumulation so V1 `AiModelExecutionService` and V2 MAF bridge calls persist provider/model/token/cost facts into nullable Billing ledger fields via migration `20260606090000_AddAiProviderUsageLedgerFields`; AI facade and usage APIs expose the same additive fields. Added `docs/ai-provider-switching.md` covering architecture, configuration, DeepSeek setup, troubleshooting, and future providers. Tests added/updated for configured DeepSeek switching, resolver filtering, DeepSeek adapter mapping/missing credentials, and billing ledger usage persistence. Verification: focused unit `AiModelProviderServicesTests` passed (6); focused integration `AiModelProviderAdapterTests` plus billing finalization passed (12); full `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed Architecture (4) and Integration (237), with one unrelated existing Unit failure: `NadpcoApiCompanyNormalizerTests.Normalize_PopulatesSupportedCompanyMetadata` expected `ABYP` but actual was `آبین`. |
| 2026-06-05 | `044-nadpco-api-scheduled-sync-worker` change request / order `47` | Completed | Added `CompanyCatalog` to `NadpcoScheduledSyncOptions.DatasetSelection` and API/Worker appsettings defaults with daily cadence remaining `86400` seconds. `NadpcoScheduledSyncCoordinator` now invokes `ExecuteCompanyCatalogAsync(cleanSlate: false)` when `CompanyCatalog` is selected; it never passes `cleanSlate: true`, so destructive cleanup remains DataAdmin-maintenance-only. Company-catalog-only schedules run only the non-destructive catalog refresh; mixed schedules run catalog refresh plus the existing incremental orchestration for other selected datasets. Run history exposes `CompanyCatalog` through `DatasetSelectionJson`; company-catalog-only diagnostics include `CompanyCatalogRefresh`, request count, companies considered, and `cleanSlate=false`. Updated README and NADPCO provider docs to distinguish initial clean-slate import from recurring scheduled refresh. Tests added/updated for scheduled non-destructive catalog refresh, company-catalog-only scheduling, no clean-slate invocation, and later catalog refresh insertion of a newly listed NADPCO `coID`. Verification: focused unit `NadpcoScheduledSyncCoordinatorTests|NadpcoApiCompanyNormalizerTests|NadpcoApiScheduledSyncServiceTests` passed (26); focused `AdminDataOperationsEndpointTests` passed (30); full `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed (Unit 379, Architecture 3, Integration 228). |
| 2026-06-05 | `020-cyclicalwaves-data-provider` change request / order `48` | Completed | Removed CyclicalWaves `Companies` upserts from ticker sync. Added `CyclicalWavesCompanyLinkageResolver` so CyclicalWaves symbol, financial-statement, and monthly normalizers link to existing NADPCO-backed company/symbol metadata instead of creating company catalog rows. Financial and monthly observations still persist; when linkage succeeds they store the NADPCO `ExternalCompanyId`, and when linkage fails they retain the source id and attach a `MissingData` linkage warning. CyclicalWaves symbol enrichment now creates/updates only `Symbols` rows and never overwrites NADPCO company names, symbols, industry, market, ISIN, registration, or listing metadata. Regression coverage added in `CyclicalWavesNormalizerTests` for no CyclicalWaves company rows, NADPCO metadata non-overwrite, successful linked observation persistence, and missing-linkage warnings. Verification: focused `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --configuration Release --no-restore --filter CyclicalWavesNormalizerTests` passed (15); full `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed (Unit 381, Architecture 3, Integration 228). |
| 2026-06-05 | `043-nadpco-api-sync-orchestration` change request / order `46` | Completed | Added `NadpcoApiSyncRunMode` with `FullSync`, `IncrementalSync`, `CompanyCatalogCleanSlate`, and `CompanyCatalogRefresh`; extended `NadpcoApiSyncResult` and DataAdmin responses with `runMode`. Added `NadpcoApiSyncStates.LastRunMode` plus migration `20260605110229_AddNadpcoApiSyncRunModeTelemetry`. Added `INadpcoApiScheduledSyncService.ExecuteCompanyCatalogAsync(cleanSlate)` and implementation in `NadpcoApiScheduledSyncService`: clean-slate mode runs order `45` cleanup, immediately invalidates scanner cache after destructive metadata cleanup, then enqueues NADPCO Symbols/company catalog import; refresh mode enqueues only the company catalog request and relies on the existing idempotent normalizer for insert/update/no-duplicate behavior. Added protected DataAdmin routes `POST /api/v1/admin/nadpcoapi/company-catalog/clean-slate` and `POST /api/v1/admin/nadpcoapi/company-catalog/refresh`; sync-state reads now include `lastRunMode`. Tests added/updated for full/incremental run-mode telemetry, clean-slate cleanup invocation/cache invalidation, refresh-only enqueue/idempotent path, publisher failure recovery, endpoint authorization, response telemetry, and cleanup counts. Verification: focused unit `NadpcoApiScheduledSyncServiceTests|NadpcoScheduledSyncCoordinatorTests` passed (14); focused `AdminDataOperationsEndpointTests` passed (30); full `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed (Unit 376, Architecture 3, Integration 228). |
| 2026-06-05 | `039-nadpco-api-company-catalog-sync` change request / order `45` | Completed | Added `NadpcoCompanyCatalogCleanSlateService` and `INadpcoCompanyCatalogCleanSlateService` for a controlled PostgreSQL company-catalog clean slate: delete metric recalculation requests, company-linked feature jobs, feature snapshots, derived metrics, and symbols; null StockMarketDB trading-instrument company links; then delete `Companies`. Registered the service in Infrastructure DI. Extended `NormalizedCompanyRow` and migration `20260605101558_AddNadpcoCompanyCatalogDetails` so every NADPCO `/api/v3/BaseInfo/Companies` sample field is persisted in `Companies` or existing related normalized dimension rows: `coID`, `coCode`, names, symbols, Pinglish, floor, industry, market, TSE/ISIN identifiers, rights/status, all Jalali/Gregorian lifecycle dates, fund, national-id, registration, and market-board metadata. Updated `NadpcoApiCompanyNormalizer` to populate the new columns and removed deferred-field logging. Tests added/extended in `NadpcoApiCompanyNormalizerTests`: sample row `coID=13226`, idempotency, and clean-slate dependency handling. Live destructive delete/backfill was not executed here; DataAdmin exposure and operator execution remain order `46`, daily refresh remains order `47`, and CyclicalWaves company-write removal remains order `48`. Verification: `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --configuration Release --no-restore --filter NadpcoApiCompanyNormalizerTests` passed (9); `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed (Unit 373, Architecture 3, Integration 225); separate Architecture rerun passed (3). |
| 2026-06-05 | `045-symbol-metric-point-lookup` | Completed | Added `SymbolLookup` to `DetectedIntent` enum. Created `SymbolLookupContracts.cs` with `SymbolLookupRequest`, `SymbolLookupTableResult`, `SymbolLookupParseResult`, `ISymbolLookupParser`, `ISymbolNameResolver`, `ISymbolMetricLookupService`. Implemented `LlmSymbolLookupParser` (LLM structured-output call, metric-term resolution via `IMetricAliasResolver`). Implemented `EfCoreSymbolNameResolver` (case-insensitive match on `Symbols.SymbolCode`, `Companies.ExternalCompanyId`, `Companies.Name`; ambiguous → null + warning log). Implemented `EfCoreSymbolMetricLookupService` (resolves symbol names, queries `DerivedMetrics` for latest `(SymbolId, MetricCode)` rows, supplements `LATEST_PRICE` from `IMarketQuoteResolver`; unresolved symbols → `UnresolvedSymbols` list). Extended `LlmAiIntentDetector` system prompt to distinguish `Scanner` (threshold/filter) from `SymbolLookup` (named symbols + metric value, no threshold). Extended `AiQueryOrchestrationService` with `SymbolLookup` branch: parse → lookup → feedback collection for unresolved symbols via `IMissingAnswerFeedbackCollector`. `SymbolLookupTableResult` re-uses `ScannerTableColumn`/`ScannerTableRow`/`ScannerTableCell` contracts; `ScannerTableResponse` serves as wire type for both scanner and lookup. `AiQueryHttpResponse` and `AssistantMessageContentResponse` extended with nullable `SymbolLookupTable`. Unresolved symbols promoted to `MissingDataWarnings` in the HTTP response. Frontend `mapAssistantBlock` prefers `symbolLookupTable` over `scannerTable` (both render via `ScannerResultTable`). DI registered: `LlmSymbolLookupParser`, `EfCoreSymbolNameResolver`, `EfCoreSymbolMetricLookupService`. Tests added: 7 lookup-parser unit tests (Persian/English pairs, unresolvable metric, mixed, empty LLM output), 5 symbol-name-resolver unit tests (exact code, case-insensitive, name substring, no match, ambiguous), 6 endpoint integration tests (`SymbolLookupEndpointTests`: found symbol, empty rows for unknown symbol, clarification for unknown metric, multi-symbol, billing ledger entry, message reload). Architecture: lookup parser and service live in Application/Infrastructure — they do not import controllers or billing persistence. Verification: `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` — Unit 372, Integration 225, Architecture 3, all passing. |
| 2026-06-04 | bug: pe-ttm-missing-from-derived-metrics | Fixed | Scanner returned 0 results for P/E queries because `PE_TTM` was absent from `DerivedMetrics`. Root cause: calculation chain `LATEST_PRICE ÷ TTM_EPS` required `SHARES_OUTSTANDING` which no provider supplies. CyclicalWaves already wrote `PE_RATIO` and `PS_RATIO` into `FinancialStatementLineItems` (532 rows each, ThreeMonths period) but no path existed to promote them into `DerivedMetrics`. Fix: (1) Added `SourceLineItemPassthroughMetricCalculator` — reads a source MetricCode from line-item inputs and stores it under a target MetricCode. (2) Declared `PE_RATIO`/`PS_RATIO` as source metrics in `PhaseOneFinancialSemanticCatalog`; changed PE_TTM/PS_TTM dependency to `[PE_RATIO]`/`[PS_RATIO]`; updated policies to `"vendor-pe-ratio-passthrough-v1"` / `"vendor-ps-ratio-passthrough-v1"`. (3) Added `PE_RATIO`/`PS_RATIO` to `SourceMetricsByDataset[FinancialStatements]` in `MetricRecalculationProcessor` so future CyclicalWaves syncs trigger recalculation automatically. (4) Replaced `ValuationRatioMetricCalculator(PE_TTM, LATEST_PRICE, TTM_EPS)` with `SourceLineItemPassthroughMetricCalculator(PE_TTM, PE_RATIO)` in DI; same for PS_TTM. (5) Inserted 534 `MetricRecalculationRequests` for all existing CyclicalWaves companies with PE_RATIO data — `DerivedMetricRecalculationWorker` drains these on next tick after API+Worker restart. Full test suite: Unit 360, Integration 219, Architecture 3 — all passing. Bug spec: `specs/bugs/pe-ttm-missing-from-derived-metrics/`. Future: restore arithmetic chain when `SHARES_OUTSTANDING` becomes available from a provider. |
| 2026-06-04 | checklist maintenance | Completed | Gap audit of all 44 spec folders vs. checklist. Findings: (1) `020-cyclicalwaves-data-provider` was the only spec folder absent from the checklist; added as order `19a` in Stage 6 with evidence it was satisfied by spec `004`/`005` MVP work. (2) `FinancialDataIngestionTests` and `DerivedMetricPersistenceTests` referenced as persistently failing in logs from specs `021`–`044` were verified to pass (7+2 = 9 tests, all green) — those warnings in earlier log entries were stale. (3) `OwnedIdentityEndpointTests.Register_ProvisionsFreeBillingAccountForUsage` was the sole real test failure: the Free plan `IncludedCredits` was updated to `10000` in `BillingEntityConfigurations.cs` (migration seed) but the test assertion remained `10m`; fixed to `10000m`. (4) NADPCO milestone row updated to include spec `044`. (5) Added **Deferred and Open Items** section listing the 7 explicitly deferred work items with their unblock conditions. Verification: `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore -m:1` — Unit 360, Integration 218, Architecture 3, all passing. |
| 2026-06-03 | `044-nadpco-api-scheduled-sync-worker` | Completed | Added application contracts for scheduled NADPCO trigger source, statuses, coordinator, run reader, status view, and alert sink. Added disabled-by-default `NadpcoScheduledSyncOptions`, `NadpcoScheduledSyncWorker`, and `NadpcoScheduledSyncCoordinator`, which wraps the existing `INadpcoApiScheduledSyncService` incremental orchestration with persisted run history, manual trigger metadata, retries, maximum-duration timeout, database-backed running lease/overlap prevention, stale lease recovery (`HungRecovered`), missed-run skip policy persistence, and replaceable alerting. Added `NadpcoScheduledSyncRuns` persistence and EF migration `20260603185442_AddNadpcoScheduledSyncRuns`. Added protected DataAdmin endpoints: `POST /api/v1/admin/nadpcoapi/scheduled-sync/run`, `GET /api/v1/admin/nadpcoapi/scheduled-sync/status`, and `GET /api/v1/admin/nadpcoapi/scheduled-sync/runs`. Updated API/Worker configuration, README, NADPCO docs, and spec status. Tests added: 6 coordinator unit tests (disabled schedule, manual run, overlap prevention, missed skip policy, failure retry/alerting, hung lease recovery) and 3 admin endpoint tests (manual trigger, status, history). Verification: build passed; focused coordinator unit tests passed (6); focused AdminDataOperations endpoint tests passed (27); Architecture passed (3); full solution passed Unit (360) + Architecture (3), and Integration excluding the unrelated existing `OwnedIdentityEndpointTests.Register_ProvisionsFreeBillingAccountForUsage` assertion passed (217). Full solution still has that unrelated existing failure: expected `10` credits but current behavior returns `10000`. Deferred: `ExecutionTimeUtc`, `BatchSize`, `MaxConcurrency`, and `DatasetSelection` are persisted/exposed scheduler snapshots while concrete bounded fan-out remains owned by existing NADPCO orchestration; deeper bounded catch-up queueing remains future work. |
| 2026-06-03 | `043-nadpco-api-sync-orchestration` | Completed | Added provider-specific NADPCO orchestration over the existing provider-neutral data-sync path. Application contracts now expose `INadpcoApiScheduledSyncService`, sync results, and sync-state reads. Infrastructure adds `NadpcoApiSyncStates` persistence (migration `AddNadpcoApiSyncState`), `EfCoreNadpcoApiSyncStateStore`, and `NadpcoApiScheduledSyncService`, which enqueues bounded `DataSyncRequest`s for catalog symbols plus per-company financial statements, fundamental indexes, and monthly product/service activity using `ProviderName="NadpcoApi"`. Incremental mode documents and records overlap-window reconciliation because NADPCO endpoints lack reliable modified-since cursors; remote history bounds remain controlled by existing provider options. Per-company enqueue failures are isolated and block progress advancement. DataAdmin endpoints added: `POST /api/v1/admin/nadpcoapi/full-sync`, `POST /api/v1/admin/nadpcoapi/incremental-sync`, and `GET /api/v1/admin/nadpcoapi/sync-state`. Docs/config updated with activation order, cadence, failure recovery, and `OrchestrationOverlapDays`. Tests added: 5 unit orchestration/state/failure cases and 4 admin endpoint/authorization cases. Verification: Unit passed (354); AdminDataOperations endpoint focus passed (24); Architecture passed (3); Integration excluding known unrelated credit test passed (214); full solution passed Unit (354) + Architecture (3) but still has the unrelated existing failure `OwnedIdentityEndpointTests.Register_ProvisionsFreeBillingAccountForUsage` expecting `10` credits while current behavior returns `10000`. |
| 2026-06-03 | `042-nadpco-api-monthly-activity-sync` | Completed | Added bounded NADPCO monthly activity requests for `ProductSales` and `ServiceSales` using numeric `companyIds`, configurable Jalali date bounds, and optional output type. Added shared `JalaliDateResolver` and reused it from both CodalDB and NADPCO monthly normalizers. Added tolerant NADPCO product/service DTOs and `NadpcoApiMonthlyActivityNormalizer`, which writes `ProviderName="NadpcoApi"` monthly reports/line items, retains zero activity, maps service rows to sales-only line items, preserves title/unit/rate/output/category/publication/industry/instrument evidence in `WarningsJson`, uses deterministic natural keys when vendor product/service ids are omitted, and relies on the existing `MonthlyProductionSales` recalculation request path. No migration required after schema audit. Registered the normalizer and updated docs/config/spec status. Tests added: 9 NADPCO monthly normalizer/processor cases plus 1 provider bounded-fetch case. Verification: `dotnet test tests/FinancialCopilot.UnitTests/FinancialCopilot.UnitTests.csproj --configuration Release --no-restore` passed (349); Architecture passed (3); Integration excluding the known unrelated credit test passed (210); `git diff --check` passed with only line-ending warnings. Full solution run built all projects and passed Unit (349) + Architecture (3), but Integration still has one unrelated existing failure: `OwnedIdentityEndpointTests.Register_ProvisionsFreeBillingAccountForUsage` expects `10` credits while current behavior returns `10000`. |
| 2026-06-03 | `041-nadpco-api-fundamental-index-sync` | Completed | Added a dedicated `ProviderDataset.FundamentalIndexes` and processor/admin routing for NADPCO fundamental indexes, separate from CodalDb `FinancialRatios`. Added bounded `CompanyFundamentalIndex/Values` request DTOs and client call using numeric `companyIds`, curated `companyIndexIds`, and optional year/period/variant filters. Added `NadpcoApiFundamentalIndexMap` with a conservative reviewed allowlist for existing governed metrics (`CURRENT_RATIO`, `NET_WORKING_CAPITAL`, `CURRENT_ASSETS_TO_TOTAL_ASSETS`, `ASSET_TURNOVER`, `TANGIBLE_FIXED_ASSETS_TURNOVER`, `AVERAGE_COLLECTION_PERIOD`, `DEBT_TO_EQUITY`, `COMPREHENSIVE_LIQUIDITY_INDEX`); percentage-like metrics remain deferred until NADPCO percent/fraction scale is verified. Added `NadpcoApiFundamentalIndexNormalizer` to select canonical variants, convert Jalali dates via .NET `PersianCalendar`, persist vendor-precomputed `DerivedMetricRow`s with `CalculationPolicyVersion="nadpco-api-fundamental-index-source-v1"`, retain vendor index title/group/unit and source flags as evidence, and keep policy separation from engine-calculated rows. Registered the normalizer and updated docs/config/spec status. Tests added: 8 normalizer/processor unit cases, 1 provider bounded-fetch unit case, and 1 scanner integration proving NADPCO source-policy rows are scannable through the existing `DerivedMetrics` path. Verification: focused NADPCO tests passed (9); focused scanner integration passed (1); full Unit passed (339); Architecture passed (3); Integration excluding the known unrelated credit test passed (210); `git diff --check` passed with only line-ending warnings. Full serial solution test still has one unrelated existing failure: `OwnedIdentityEndpointTests.Register_ProvisionsFreeBillingAccountForUsage` expects `10` credits while current behavior returns `10000`. |
| 2026-06-03 | `040-nadpco-api-financial-statement-sync` | Completed | Added bounded NADPCO statement fetches for `BalanceSheet`, `IncomeStatement`, and `CashFlow` using company batches plus reviewed item allowlists and optional year/period/variant filters. Added NADPCO statement DTOs, item maps, deterministic canonical variant selection, and `NadpcoApiFinancialStatementNormalizer` that writes distinct corrected `StatementType` rows, maps only governed `MetricCode`s, uses Gregorian periods, retains Jalali dates/source flags/source units/assumed `MillionRials` scale as evidence, performs idempotent upserts, coexists with `CodalDb`, and publishes recalculation requests. Added `OPERATING_CASH_FLOW` to the semantic catalog and normalized line-item input registrations. Updated NADPCO docs, API/Worker config defaults, and spec task status. Tests added: 9 NADPCO statement-normalizer unit cases plus 1 provider bounded-fetch allowlist case. Verification: targeted NADPCO statement/provider tests passed (10); full Unit passed (330); Architecture passed (3); Integration excluding the known unrelated credit test passed (209); `git diff --check` passed with only line-ending warnings. Full serial solution test still has one unrelated existing failure: `OwnedIdentityEndpointTests.Register_ProvisionsFreeBillingAccountForUsage` expects `10` credits while current behavior returns `10000`. |
| 2026-06-03 | `039-nadpco-api-company-catalog-sync` | Completed | Added provider-local DTOs for `GET /api/v3/BaseInfo/Companies` and `NadpcoApiCompanyNormalizer` for `ProviderName="NadpcoApi"` / `ProviderDataset.Symbols`. The normalizer upserts provider-scoped company, symbol, industry, floor/group, and market rows from `coID`, names, symbols, `tseCode`, ISINs, and classification metadata; uses an instrument-code-first canonical-symbol mode added to `CanonicalSymbolLinkageResolver` while preserving CodalDb's existing ISIN-first behavior; logs missing identifiers, instrument-code fallback, conflicting duplicate `coID` identifiers, and deferred unsupported catalog attributes retained in raw payload evidence. Registered the normalizer in Infrastructure DI. Added docs in `docs/nadpco-api-provider.md` and updated spec status. Tests added: 8 NADPCO company-normalizer unit cases plus 1 resolver priority case covering metadata mapping, instrument priority, fallback warning, idempotency, duplicate conflict warning, missing identifier skip, and raw-before-normalization through `FinancialDataSyncProcessor`. Verification: targeted `NadpcoApiCompanyNormalizerTests|CanonicalSymbolLinkageResolverTests` passed (15); full Unit passed (320); Architecture passed (3); Integration excluding the known unrelated credit test passed (209); full `dotnet test src\backend\FinancialCopilot.sln --configuration Release --no-restore -m:1` still has one unrelated pre-existing failure: `OwnedIdentityEndpointTests.Register_ProvisionsFreeBillingAccountForUsage` expects `10` credits while current behavior returns `10000`. |
| 2026-06-03 | `038-nadpco-api-provider-foundation` | Completed | Added the `NadpcoApi` HTTP provider foundation under Infrastructure without replacing `CyclicalWaves` or `CodalDb`: secrets-backed `NadpcoApiProviderOptions`, Basic `/api/v2/Token` token provider, thread-safe cache, Bearer auth handler with one `401` refresh/retry, provider-local timeout/retry/circuit-breaker handler, typed raw-payload client for companies, financial statements, monthly activity, and fundamental-index endpoints, redacted provider error handling, health check via authenticated token request, and provider-name routing for symbols/statements/monthly/financial-ratios. Raw payloads use `ProviderName="NadpcoApi"` and the existing `(ProviderName, Checksum)` dedupe path. Added docs in `docs/nadpco-api-provider.md` and README. Tests added: 7 unit cases for token reuse, expiry refresh, redacted auth failure, 401 retry, timeout/circuit behavior, raw checksum dedupe, and CodalDb coexistence including ratio routing. Verification: `dotnet test tests\FinancialCopilot.UnitTests\FinancialCopilot.UnitTests.csproj --configuration Release --no-restore` passed (311); `dotnet test tests\FinancialCopilot.ArchitectureTests\FinancialCopilot.ArchitectureTests.csproj --configuration Release --no-restore` passed (3); `dotnet test tests\FinancialCopilot.IntegrationTests\FinancialCopilot.IntegrationTests.csproj --configuration Release --no-restore --filter "FullyQualifiedName!~OwnedIdentityEndpointTests.Register_ProvisionsFreeBillingAccountForUsage"` passed (209); `git diff --check` passed. Full `dotnet test src\backend\FinancialCopilot.sln --configuration Release --no-restore -m:1` has one unrelated pre-existing failure: `OwnedIdentityEndpointTests.Register_ProvisionsFreeBillingAccountForUsage` expects `10` credits while current configuration returns `10000`. Live NADPCO token response shape/lifetime was not tested because no vendor credentials were provided; scheduled NADPCO reads remain documented as disabled until vendor contract or controlled smoke confirmation. |
| 2026-06-02 | `037-frontend-admin-panel` | Completed | Added a protected TanStack `/admin` route with JWT-claim permission guards and a permission-aware sidebar entry; typed `/api/v1/admin/*` frontend integration; Persian RTL modules for users, roles/permissions, tenant membership, plans/capabilities, subscriptions, usage ledger, idempotent credit adjustments, and security/Billing audits; confirmed reason-bearing sensitive mutations; correlation-bearing admin ProblemDetails rendering; and operational documentation. Credit idempotency keys persist across retry and regenerate after success. Documented the verified backend gap: no bounded customer-account search endpoint exists yet, so Billing operations currently accept an explicit account GUID. Added deterministic admin permission tests. Verification passed: targeted ESLint with zero warnings, `npm run test:admin-permissions`, `npm run test:api-url`, `npm run build`, generated `/admin` route inspection, `git diff --check`, and temporary hidden-Vite HTTP smoke (`GET /admin` `200`, `GET /auth` `200`); temporary listener stopped. In-app browser visual inspection could not run because no browser runtime was attached and `playwright` is absent. |
| 2026-06-02 | `036-frontend-local-api-connectivity` | Completed | Fixed the local browser registration `404`: browser auth previously used an empty `VITE_FINANCIAL_COPILOT_API_BASE_URL` fallback and posted to the frontend SSR origin `http://localhost:8080/api/auth/v1/register` while the implemented .NET API listens on `http://localhost:5074`. Added a shared validated frontend API URL builder with deterministic local fallback, aligned TanStack server-function URL resolution while preserving the optional server-only override, added tracked `src/frontend/.env.example`, and updated the local ignored `.env`. Development CORS now explicitly allows credentialed `http://localhost:8080` and `http://localhost:5173`. Extended owned-identity integration coverage for `8080` credentialed preflight and scoped `HttpOnly; SameSite=Strict; Path=/api/auth/v1` cookie clearing, plus deterministic frontend URL tests. Live smoke against a fresh Release API process passed: preflight `204` with `Allow-Origin: http://localhost:8080` and credentials, register `200`, refresh `200`, logout `204`. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 303, Integration 204, Architecture 3. Frontend `npm run test:api-url`, targeted Prettier, targeted ESLint, and `npm run build` passed. Existing frontend/API dev processes must restart to load updated environment and CORS configuration. |
| 2026-06-02 | `035-admin-identity-and-entitlement-management` | Completed | Added an API-first admin application boundary and narrow `admin.*` policy catalog for user status/session administration, role lifecycle and permission mappings, user-role assignment, tenant memberships, immutable plan publication and append-only `PlanCapabilities`, optimistic subscription revision updates, Billing-owned idempotent credit adjustment, immutable usage-ledger reads, and security/Billing audit visibility. Admin policies require web-user JWTs and reject SaaS API keys. Added serializable PostgreSQL protection for final active `SuperAdmin` mutations with audited lockout-risk rejection, correlation-bearing RFC 7807 admin errors, Auth migrations `20260602081431_AddSecurityAdminAudits` and `20260602082015_AddAdminRoleStatus`, and Billing migration `20260602082024_AddAdminBillingAuditsAndSubscriptionConcurrency`. Added 8 integration tests covering permission/API-key denial, role lifecycle, redacted security audits, tenant rejection, final-admin protection, idempotent Billing adjustment, append-only capabilities/subscription assignment, stale revisions, and Billing-audit reads. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore -m:1` passed: Unit 303, Integration 202, Architecture 3. |
| 2026-06-02 | `034-frontend-assisted-query-metadata` | Completed | Extended authenticated AI metadata with backend-owned bilingual periods and bounded normalized PostgreSQL discovery reads: `GET /api/ai/v1/metadata/periods`, `.../symbols?search=&limit=`, and `.../industries?search=&limit=` (`search` max 100 chars, `limit` 1..50). Reused the governed semantic metric registry for `GET .../metrics`; no public scanner parse/execute endpoint was added. Added owned-JWT frontend metadata server functions and an assisted filter dialog that handles loading, empty, error, and capped-result states and composes a Persian user-visible prompt into the existing textarea for normal `POST /api/ai/v1/query` submission. Added 10 integration cases covering bilingual periods, normalized symbol search with limits, industry search, bound rejection, and authentication. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore -m:1` passed: Unit 303, Integration 194, Architecture 3. Frontend targeted Prettier check, targeted ESLint, and `npm run build` passed. Repository-wide `npm run lint` remains blocked by the pre-existing CRLF/Prettier backlog outside this story (5795 errors and 6 warnings). |
| 2026-06-02 | `033-frontend-usage-watchlist-market-summary` | Completed | Added actor-scoped `WatchlistSymbols` persistence keyed by tenant, actor id, actor type, and symbol with migration `20260602041327_AddActorWatchlists`; plan-capability-backed symbol-limit lookup (`Watchlist.Symbols`), normalized symbol validation, batched quote enrichment, and explicit no-quote fallback. Added permission-protected `GET`/`PUT /api/v1/watchlists/me`, `GET /api/v1/market/summary`, generic normalized index observations and positive/negative top movers, nullable unsupported real-money-flow/industry-trend/insight fields, a short-lived market-summary cache, and StockMarketDB quote/index projection invalidation. Replaced the remaining Supabase sidebar subscription/watchlist reads and `STOCK_DB`/`MARKET_SNAPSHOT` widget imports with owned-JWT server proxy calls to `GET /api/v1/usage/me`, watchlist, and market-summary APIs; added localized loading, empty, stale, and error states and removed `prototype-sidebar.functions.ts`. Added 1 unit cache-invalidation test and 6 integration tests covering actor isolation, quote fallback, invalid symbols, normalized summary/unavailable fields, and unauthenticated rejection. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 303, Integration 184, Architecture 3. Frontend targeted Prettier check, targeted ESLint, and `npm run build` passed. Repository-wide `npm run lint` remains blocked by the pre-existing CRLF/Prettier backlog outside this story (6522 errors and 6 warnings). |
| 2026-06-01 | `032-frontend-chat-conversation-cutover` | Completed | Added actor-scoped conversation lifecycle (`POST` empty create, list/detail/message reads, `DELETE`, ownership validation before continuation), backend-owned default/first-message titles, and atomic exchange persistence with versioned structured assistant payload reloads. Added PostgreSQL migration `20260601160253_AddConversationLifecycle` and applied it locally. Replaced frontend Supabase thread/message writes, `generateMockReply`, and local credit decrement with owned-JWT server proxy calls to the generic conversation APIs and the single `POST /api/ai/v1/query`; the home chat now uses the atomic `conversationId=null` path. Adapted rendering for backend text, filter chips, scanner rows/freshness, citations, confidence, follow-ups, and usage; hid unsupported deep-research/filter prototype controls. Isolated remaining prototype sidebar usage/watchlist reads for `033`. Added 3 integration tests for empty create/delete, structured reload/title promotion, and same-tenant peer denial. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 302, Integration 178, Architecture 3. Frontend targeted Prettier check and `npm run build` passed. Repository-wide `npm run lint` remains blocked by pre-existing CRLF/Prettier findings outside this story. |
| 2026-06-01 | `031-frontend-authenticated-api-bridge` | Completed | Added backend-owned ASP.NET Core Identity persistence, tenant memberships, stable permission catalog and permission policies, short-lived JWT issuance, hashed rotating HttpOnly refresh-token sessions with replay-family revocation, auth APIs, Billing-owned versioned `PlanCapabilities`, Free/Pro/Plus/Premium capability seed migrations, monthly limited-operation quota checks before wallet reservation, credentialed local CORS, frontend backend-auth session handling, and a typed FinancialCopilot API client. Applied `AuthDbContext` and `BillingDbContext` migrations locally. HTTP smoke check passed: frontend auth route `200`, credentialed CORS preflight `204`, register `200` with 10 permissions, refresh `200`, logout `204`. `dotnet test src/backend/FinancialCopilot.sln --configuration Release` passed: Unit 302, Integration 175, Architecture 3. Frontend targeted Prettier check and `npm run build` passed. Repository-wide `npm run lint` remains blocked by pre-existing CRLF/Prettier findings outside this story. Supabase chat persistence remains isolated for `032`. |
| 2026-05-26 | `001-project-foundation` | Completed | Audited existing modular solution, API middleware/endpoints, Billing module, and architecture tests. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 4, Integration 16, Architecture 1. |
| 2026-05-26 | `002-auth-and-tenancy` | Completed | Added explicit `ActorType` and canonical `ActorId` current-context contract for user/API-client Billing handoff without billing decisions in auth. Verified JWT/API-key, 401/403, tenant/client access and per-actor rate limit behavior. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 5, Integration 16, Architecture 1. |
| 2026-05-26 | `013-billing-and-credits-domain` | Completed | Implemented the isolated Billing foundation: account modes/credit-line policy, immutable usage and financial ledgers, atomic/idempotent reservation/finalization/expiry/refund/adjustment persistence, wallet projection, versioned operation pricing, usage/admin reads, invoice/subscription/payment boundary contracts, transactional outbox records and retry diagnostics, and scheduled worker maintenance for reservation expiry plus optional configured outbox dispatch. AI facade charging/usage metadata is assigned to `010`; normalized provider facts to `014`; telemetry correlation to `018`; live payment/subscription/invoice/outbox transport and scale-out claiming remain explicitly deferred commercial/operations integrations. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 40, Integration 49, Architecture 1. |
| 2026-05-26 | `003-financial-domain-model` | Completed | Added normalized financial entities and provenance/quality evidence, validated symbol/period/metric/version/percentage primitives, supported monthly/3/6/9/12-month/latest-month/latest-quarter/TTM period semantics, reusable YoY/MoM comparison policy, extensible registered metric identity and calculation-policy input contracts, and derived-metric version evidence aligned for `015`/`006`. Recorded downstream ownership for aliases, ingestion persistence, and calculation execution in the feature task log. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 51, Integration 49, Architecture 1. |
| 2026-05-26 | `015-financial-semantic-layer` | Completed | Added versioned semantic definitions, bilingual alias and ambiguity resolution, effective-version/policy/dependency services, calculator strategy registry contracts, Phase 1 governed catalog registration, QoQ comparison semantics, dependency-version evidence for derived observations, scanner/explanation handoff contracts, EF semantic catalog read models, and authenticated metrics metadata API. Production numeric calculators remain owned by `006`, complete parser orchestration by `007`, and answer assembly by `009`. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 59, Integration 52, Architecture 2. |
| 2026-05-26 | `004-third-party-data-provider-abstraction` | Completed | Added Application financial-provider/health/raw-payload/error and batch quote contracts; Infrastructure raw payload persistence; deterministic mock adapter with live/previous-trading-day fallback; configurable typed HTTP adapter with configuration-based credentials, logging/error mapping, timeout/retry/circuit-breaker handling; and DI/configuration wiring. Ingestion processing is assigned to `005`, and protected provider-health HTTP operations to `012`. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 59, Integration 57, Architecture 2. |
| 2026-05-27 | `005-data-ingestion-and-normalization` | Completed | Verified worker/RabbitMQ data-sync consumption boundary and completed build wiring; added PostgreSQL EF migrations for normalized ingestion tables and required raw provider payload storage; verified raw-before-normalization behavior, normalized symbol/statement/monthly upserts, sync run status/failure persistence, idempotent repeated consumption, and derived-metric recalculation request enqueueing with new integration tests. Status HTTP endpoints remain owned by `012`; deterministic calculation execution remains owned by `006`. `dotnet restore src/backend/FinancialCopilot.sln` and `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 59, Integration 61, Architecture 2. |
| 2026-05-27 | `006-derived-metrics-engine` | Completed | Added deterministic registered calculator strategies for quarterly net-profit growth, monthly sales YoY/MoM growth, TTM sales, TTM earnings, TTM EPS, P/E, and P/S; extended semantic definitions/policies for required dependencies; added normalized metric input strategies, application calculation/recalculation command contracts, persisted derived observations with policy/version/source/dependency evidence, and PostgreSQL migration. Tests cover missing/zero denominators, quote as-of/source retention, independent calculator registration, and ingestion-to-persisted-derived-metric execution. `dotnet restore src/backend/FinancialCopilot.sln` and `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 66, Integration 63, Architecture 2. |
| 2026-05-27 | `014-ai-model-provider-abstraction` | Completed | Added provider-neutral Application contracts for completions, structured output, tools, streaming, embeddings, health, capability discovery, routing, and normalized execution usage facts; capability-based tenant/residency/local policy resolver; schema validation with audited fallback attempts; Infrastructure configuration, deterministic fake adapter, hosted contracted-transport adapter boundary, Ollama local adapter, contract-pending Abravran boundary, and metadata-only telemetry sink. Concrete hosted vendor transport activation awaits an approved selected-provider contract; AI facade/scanner orchestration is owned by `007`, Billing handoff by `010`, and expanded trace persistence by `018`. `dotnet restore src/backend/FinancialCopilot.sln` and `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 71, Integration 67, Architecture 3. |
| 2026-05-27 | `018-ai-observability-and-telemetry` | Completed | Added Application observability contracts: `AiErrorCategory` enum (8 domain categories), `PromptRedactionPolicy` with DefaultSafe (no-capture default), `PromptTrace`, `ToolExecutionTrace`, `ProviderLatency`, `TokenUsage`, `CostTelemetry`, `AiExecutionTrace`, `WorkflowTelemetry`, and `IAiWorkflowTelemetrySink` interface. Infrastructure: OpenTelemetry-compatible `AiObservabilityActivitySource` static class with `FinancialCopilot.AI` ActivitySource, span/attribute name constants, and span factory methods; `LoggingAiWorkflowTelemetrySink` using Activity.Current enrichment and structured ILogger output with privacy-safe redaction enforcement; enhanced `LoggingAiExecutionTelemetrySink` with token/cost/cache/retry structured fields. DI registration for `IAiWorkflowTelemetrySink`. Advanced dashboard sinks and OTel SDK exporters remain incremental; facade orchestration is assigned to `007`. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 84, Integration 83, Architecture 3. |
| 2026-05-27 | `007-natural-language-scanner-parser` | Completed | Implemented Application contracts: `ScannerQueryPlan`, `ScannerCondition`/`ScannerMetricReference` with filter-origin and policy-version evidence, `ScannerQueryPlanValidator`, `IScannerQueryParser`; AI orchestration contracts (`IAiIntentDetector`, `IAiQueryOrchestrationService`, `IBillingFacadeHook`); Conversation/Message repository interfaces and DTOs. `LlmAiIntentDetector` (Scanner/Clarification/Unknown classification), `LlmScannerQueryParser` (LLM proposes user terminology; backend resolves canonical MetricCode via `IMetricAliasResolver` with BCP-47 normalization; handles Resolved/Ambiguous/NotFound; 10-column overflow guard; PolicyVersion v1), `ScannerQueryPlanValidator`. `AiQueryOrchestrationService` orchestrates intent→parse→persist with `IBillingFacadeHook` reservation/finalization; `NoOpBillingFacadeHook` for Phase 1. Infrastructure: `ConversationDbContext` with `ConversationRow`/`MessageRow`, EF migration `InitialConversations`, `ConversationRepository`/`MessageRepository`. `AiFacadeController` implements `POST /api/ai/v1/query`, `GET /api/ai/v1/conversations`, `GET /api/ai/v1/conversations/{id}`, `GET /api/ai/v1/conversations/{id}/messages`. Tests: 8 unit tests (plan validation, English/Persian alias resolution, unrecognized terms, column limit, filter-origin), 6 integration tests (end-to-end query, conversation history, multi-turn). `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 96, Integration 90, Architecture 3. |
| 2026-05-27 | `008-scanner-execution-engine` | Completed | Implemented Application contracts: `IScannerExecutionService`, `IScannerResultColumnPolicy`, `IScannerResultRanker`, `IMarketQuoteResolver`; DTOs: `ScannerTableColumn/Cell/Row`, `ScannerExecutionFacts`, `ScannerTableResult`, `ScannerExecutionRequest`. `ScannerResultColumnPolicy` builds default (Symbol/Company/Price/Change%/MarketCap) plus condition-metric and user-requested columns, capped at 10. `ScannerResultRanker` scores by how strongly values beat thresholds, ties broken alphabetically. Infrastructure: `EfCoreScannerExecutionService` executes AND-condition plan against `FinancialIngestionDbContext.DerivedMetrics`, resolves market quotes in batch via `IMarketQuoteResolver`, surfaces `Live`/`PreviousTradingDay`/`Persisted`/`Missing` cell freshness status, and emits `ScannerExecutionFacts`; `ProviderMarketQuoteResolver` wraps `IMarketDataProvider`. `AiQueryOrchestrationService` extended with `IScannerExecutionService`; calls execution only when parse succeeds and no clarification required. `AiQueryResponse`/`AiQueryHttpResponse` extended with `ScannerTableResult`/`ScannerTableResponse` (columns, rows, executionFacts, warnings). `AiFacadeApiFactory` made non-sealed; adds in-memory `FinancialIngestionDbContext` so existing AI facade tests keep passing without real PostgreSQL. Tests: 8 unit tests (column policy caps, dedup, metric columns, ranker order, missing-value row handling); 6 integration tests (PE<6 returns 2 matching symbols, default columns include PE_TTM, LIVE symbol has Live price freshness, FALLBACK has PreviousTradingDay freshness, dual-condition AND filter returns only 1 match, execution facts counts). `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 104, Integration 96, Architecture 3. |
| 2026-05-27 | `009-explainable-results` | Completed | Implemented Application contracts: `ConditionFilterChip`, `MetricEvidenceSummary`, `DataCitation`, `ConfidenceFactors`, `ConfidenceScoreResult`, `ExplainableAnswer`, `ExplainableAnswerRequest`, `ScannerExplanationRequest/Output`, `IConfidenceScoreCalculator`, `IExplainableAnswerBuilder`, `IScannerExplanationGenerator`. `ConfidenceScoreCalculator` computes deterministic score from interpretation certainty (0.4 weight), evidence completeness (0.35), source freshness (0.25), and warning penalty (−10%/warning, max −30%) with policy version "v1". `ExplainableAnswerBuilder` builds filter chips with operator symbols/labels/origin/inferred-flag, metric evidence with canonical version/policy evidence from `ScannerCondition.MetricReference`, data citations from non-Missing cells, and delegates optional prose and follow-up questions to `IScannerExplanationGenerator` with graceful failure isolation. `LlmScannerExplanationGenerator` uses `IAiModelExecutionService` with `AiWorkloadKind.ExplanationGeneration` and `ScannerExplanationOutput` structured schema to generate explanation text and 2–3 suggested follow-up questions from the original query, matched symbols, and filter chips. `AiQueryOrchestrationService` extended: calls builder after scanner execution; uses AI-generated `ExplanationText` as assistant message content when available. `AiQueryResponse` extended with `ExplainableAnswer?`; `AiQueryHttpResponse` extended with `ExplainableAnswerResponse?` (filterChips, metricEvidence, dataCitations, confidence, suggestedFollowUpQuestions, explanationText). All fake AI clients updated to handle `ScannerExplanationOutput` schema. No LLM alters the deterministic confidence score. Tests: 15 unit tests (ConfidenceScoreCalculator: 7 cases; ExplainableAnswerBuilder: 8 cases including graceful AI failure); 6 integration tests (presence, filter chip, deterministic confidence=1.0 on clean data, metric evidence with policy version, suggested questions, explanation text). `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 119, Integration 102, Architecture 3. |
| 2026-05-27 | `010-usage-metering-and-billing-readiness` | Completed | Replaced the AI facade no-op billing registration with `AiFacadeBillingHook`, which resolves organization API-client or individual user accounts, reserves operation-based capacity before workflow execution, calculates versioned completed/clarification/validation/failure/cancellation/cache outcomes, and finalizes usage through the existing Billing reservation/ledger/wallet services. Extended `POST /api/ai/v1/query` with backend-calculated usage metadata and persisted ledger `CompletionStatus` so zero-charge outcomes remain auditable. Integration tests verify exactly one attributed ledger entry per API-client request, individual-account charging, and zero-credit clarification accounting; scanner/explainable flows continue through the billed facade. Updated billing/API documentation. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 119, Integration 104, Architecture 3. |
| 2026-05-27 | `011-cache-and-performance` | Completed | Added `IScannerCache` application boundary and internal plan/result cache integration in `AiQueryOrchestrationService`; added `DistributedScannerCache` backed by `IDistributedCache` with configuration-selectable Redis support, tenant/actor-or-API-client/data-version-scoped keys, plan/result TTL policies, and local distributed-memory fallback. Completed data synchronization and derived-metric persistence now rotate the scanner data-version token to invalidate prior cached results. Cached tables retain freshness source timestamps, return `executionFacts.fromCache`, and continue through Billing reservation/finalization with the configured cached multiplier (`1.0` fresh, `0.2` cached in integration evidence). Added tests for billed cached facade reuse, invalidation forcing fresh execution, and ingestion/metric invalidation notification; updated API/data/cache documentation. `dotnet restore src/backend/FinancialCopilot.sln` and `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 119, Integration 106, Architecture 3. |
| 2026-05-27 | `012-admin-data-operations` | Completed | Added a separate `DataAdmin` authorization policy and protected operational API endpoints: `POST /api/v1/admin/data-sync/symbols`, `POST /api/v1/admin/data-sync/financial-statements`, `POST /api/v1/admin/data-sync/monthly-reports`, `GET /api/v1/admin/data-sync/runs`, and `GET /api/v1/admin/provider-health`. Trigger actions publish existing `DataSyncRequest` messages instead of duplicating ingestion logic; status reads expose requested/started/completed timestamps, status, processed record and error details; provider health delegates to `IFinancialDataProviderHealthService`. Added RabbitMQ configuration/documentation and integration tests verifying data-admin authorization, normal-user/API-client/billing-admin rejection, queued requests, required company references, history fields, and health output. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 119, Integration 113, Architecture 3. |
| 2026-05-27 | `017-ai-evaluation-and-regression` | Completed | Implemented internal evaluation domain in Application layer: `GoldenQuestion`, `GoldenAnswer` (via `EvidenceRequirement`/`ProseRequirement`), `EvaluationDataset`, `PromptVersion`, `EvaluationRunMetadata`, `EvaluationScore`, `EvaluationRunResult`, `RegressionResult`, `RegressionReport`. Interfaces: `IEvaluationDatasetRepository`, `IPromptVersionRegistry`, `IEvaluationRunRepository`, `IInterpretationScorer`, `IClarificationScorer`, `IEvidenceCompletenessScorer`, `IConfidenceProtectionScorer`, `IRegressionReporter`, `IAiEvaluationRunner`. Deterministic scorers: exact-match interpretation/clarification, deterministic evidence completeness (backend MetricEvidence), confidence protection (asserts backend policy version "v1" and score ∈ [0,1]). Regression reporter classifies Minor/Major/Critical by score drop thresholds. `AiEvaluationRunner` iterates golden questions through `IAiQueryOrchestrationService` with synthetic non-billable tenant/actor IDs; only used with fake providers in CI. Golden datasets: Phase 1 (12 questions) covering bilingual English/Persian parsing, semantic metric resolution, ambiguity/clarification routing, evidence completeness, confidence protection, and billing-metadata protection. `InMemoryEvaluationRunRepository` and `NoOpPromptVersionRegistry` provide Phase 1 defaults; both replaceable at composition root. No public API contract altered. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 155, Integration 113, Architecture 3. |

| 2026-05-27 | `016-derived-feature-foundation` | Completed | Added feature domain/version/dependency/snapshot/job vocabulary and candidate feature codes; stable Application registry/calculator/snapshot/query/scheduler interfaces; deterministic calculation guardrails with dependency and input-fingerprint evidence; PostgreSQL persistence and migration for versioned definitions, historical snapshots, and idempotent computation jobs; RabbitMQ requested/completed/failed transport with worker processing and validated closed-period messages. No advanced feature score implementation or ML/online feature-store infrastructure was introduced; these remain separately scoped. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 159, Integration 116, Architecture 3. |

| 2026-05-28 | `019-conversation-memory-strategy` | Completed (Production-Ready) | Elevated from MVP stubs to full production implementation. Added PostgreSQL persistence for `MemoryConsentPolicies`, `MemoryRecords` (soft-delete), and `MemoryAuditEvents` via `MemoryDbContext` with EF migration `AddMemoryTables`. Real implementations: `EfCoreMemoryConsentService` (grant/revoke/query with idempotent upsert), `EfCoreMemoryAuditService` (fire-and-forget audit with resilient exception swallowing), `EfCoreMemoryControlService` (inspect/write/delete/deleteAll with audit trail), `EfCoreMemoryContextProvider` (short-term conversation memory derived from `IMessageRepository` without extra storage; durable memory subject to consent + `ConsentAwareMemoryProtectionPolicy` authorization). `AiQueryOrchestrationService` extended: retrieves authorized memory context before intent detection; enriches user message with provider-safe memory items (`MayBeIncludedInProviderPrompt` gate); records per-item audit events post-execution; attaches `MemoryDisclosures` to `AiQueryResponse`. Memory Management API: `GET/POST /api/v1/memory/consent`, `DELETE /api/v1/memory/consent/{type}/{purpose}`, `GET/POST /api/v1/memory/records`, `DELETE /api/v1/memory/records/{id}`, `DELETE /api/v1/memory/records` — all restricted to `ActorType.User` (API clients return 403). `AiQueryHttpResponse` extended with `MemoryDisclosures`. DI wiring updated; `AiFacadeApiFactory` extended to replace `MemoryDbContext` with in-memory for all existing integration tests. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 168, Integration 135 (+19), Architecture 3. |

| 2026-05-30 | `021-codaldb-provider-foundation` | Completed | Added the read-only CodalDB (MS SQL Server) provider behind the `004` interfaces, coexisting with CyclicalWaves. `CodalDbProviderOptions` (section `"CodalDb"`, connection string from config/secrets, command timeout, retry), `CodalDbConnectionFactory` (`ApplicationIntent=ReadOnly`, no writes/DDL), explicit `CodalDbSqlResilience` (bounded transient-error retry + terminal mapping to `FinancialProviderException`). SQL isolated behind `ICodalDbQueryExecutor`/`SqlCodalDbQueryExecutor` so the client is fully unit-testable. `CodalDbPayloadSerializer` produces canonical JSON (rows ordered by key) + SHA-256 so unchanged data yields identical checksums for dedup. `CodalDbDataProviderClient` implements `ISymbolDataProvider`/`IFinancialStatementProvider`/`IMonthlyProductionSalesProvider`/`IFinancialDataProviderHealthService` (Fetch* → `ProviderRawPayload` with `ProviderName="CodalDb"`, correct dataset/`codaldb://…` endpoint/external ref; `CheckAsync` → `SELECT COUNT(*) FROM Companies`; soft-deleted statements filtered); deliberately NOT `IMarketDataProvider`. Provider selection added via `IFinancialDataProviderRouter` (name-keyed, built at composition root) consumed by `FinancialDataSyncProcessor` through an optional, backward-compatible param: `DataSyncRequest.ProviderName` routes to a named provider, else the configured primary (CyclicalWaves). DI registers options/factory/resilience/executor/client/router; `Microsoft.Data.SqlClient` 5.2.2 added to Infrastructure. Tests: 4 serializer + 6 client (faked executor: payload metadata, invalid CoID, `CheckAsync` Healthy/Unavailable-wrapping) + 1 processor routing integration test. `dotnet test` (offline, see notes): Unit 217 (+10), Architecture 3, Integration 132/137. **The 5 failing integration tests are a PRE-EXISTING regression on `develop` unrelated to this story** — commit `7a13b5a` renamed `MockFinancialDataProvider.ProviderName` to `"DeterministicMockFinancialProvider"`, orphaning its payloads from the `"ConfiguredFinancialProvider"` normalizers (`KeyNotFound` on normalizer lookup); recommended fix is to align the Mock's provider name with the normalizers it feeds (separate from 021). **Deferred/notes:** live SQL queries in `SqlCodalDbQueryExecutor` are compile-checked only (the optional `CODALDB_INTEGRATION`-gated live smoke test was not added; spec marks it optional). **Env caveat:** the private NuGet mirror `repo.scratnet.com` is down; packages (incl. `Microsoft.Data.SqlClient` + transitives) were restored from nuget.org into the local cache for this session — the mirror must host them for normal CI restore. |

| 2026-05-30 | `022-codaldb-company-symbol-sync` | Completed | Company-master-data enrichment slice was implemented in prior session (commit `3e40db9`) and verified. Added Domain: `CompanyIdentifiers` VO (trim/normalize 5 identifier fields to null), `SymbolLinkageBasis` enum, pure `CanonicalSymbolLinkageResolver` (ISIN-first priority: SymbolIsin→CompanyIsin→InstrumentCode→TseSymbol→CompanySymbol, cross-provider alignment with CyclicalWaves). Infrastructure: extended `NormalizedCompanyRow` (NameEnglish, identifiers, ISINs, InstrumentRefPlaceholder non-identifying, nullable FKs to Industries/Groups/Markets dimensions, SourceModifiedAt watermark); added dimension rows + EF migration `20260530165910_EnrichCodalCompanyMasterData` (additive/nullable). `CodalDbCompanyRecord` JSON payload DTO + `CodalDbSymbolNormalizer` (upserts dimensions and company/symbol rows, canonical linkage, idempotent, logs alignment warnings when no ISIN). Provider routing via `DataSyncRequest.ProviderName` + `IFinancialDataProviderRouter` (wired in order 20/spec 021) now fully operational. Tests: 6 unit (CanonicalSymbolLinkageResolver), 7 unit (CodalDbSymbolNormalizer), 1 integration (Processor_RoutesCodalDb* enrichment + dimensions + routing). Docs: `docs/company-master-data-model.md`. `dotnet test`: Unit 217, Architecture 3, Integration 132/137 (5 pre-existing). Build clean. |

| 2026-05-31 | `026-codaldb-derived-growth-metrics` | Completed | `AdditiveCompositeMetricCalculator` added (sums component observations for the `EffectivePeriod`; missing/null component → null + MissingData warning). `CodalDiscreteQuarterDeriver` converts cumulative 3/6/9/12-month observations to discrete `ThreeMonths` observations for QoQ calculators (grouped by `PeriodStart` = fiscal-year start; Q1 unchanged; Qn discrete = cumulative(n) − cumulative(n−1)). `LineItemMetricInputSource` (generic, keyed by `MetricCode`) replaces/subsumes `NetProfitMetricInputSource` in DI for all financial-statement line-item metrics; `NetProfitMetricInputSource` preserved as a backward-compatible delegating wrapper. Catalog additions: `EBIT` definition + `ebit-composite-v1` policy (NET_PROFIT + FINANCE_COSTS + INCOME_TAX; OPERATING_PROFIT proxy fallback documented); 12 engine-derived growth metric definitions (6 YoY + 6 QoQ for REVENUE/GROSS_PROFIT/OPERATING_PROFIT/EPS/EBIT/TOTAL_EQUITY) with bilingual aliases + `GrowthComparison` qualifiers matching the resolver; 12 growth policies; 6 vendor-precomputed growth rate codes (`SALES_GROWTH_RATE`/`NET_PROFIT_GROWTH_RATE`/`EQUITY_GROWTH_RATE`/`TOTAL_ASSETS_GROWTH_RATE`/`TOTAL_DEBT_GROWTH_RATE`/`TANGIBLE_FIXED_ASSETS_GROWTH_RATE`) added to catalog + `CodalDbRatioItemMap` (ids 6902/6903/8092/6904/8091/6905). DI: 1 EBIT composite + 12 growth `PercentageGrowthMetricCalculator` + 8 `LineItemMetricInputSource` instances registered. All growth metrics scannable with no scanner-engine changes. Tests: 5 unit (CodalDiscreteQuarterDeriverTests: Q1 pass-through, Q2 discrete, full-year 4 quarters, two fiscal years independent, null value); 4+8 unit (EbitCompositeCalculatorTests + CodalDbGrowthMetricCalculatorTests: EBIT sum, missing/null component, wrong-period ignore, YoY growth %, zero denominator, missing prior, QoQ discrete, EBIT YoY); 3 integration (EPS_GROWTH_YOY scanner returns CODAL2, SALES_GROWTH_RATE scanner returns CODAL2, distinct policy versions verified). `dotnet test` passed: Unit 266 (+15), Integration 137/142 (5 pre-existing), Architecture 3. |
| 2026-05-31 | `025-codaldb-precomputed-ratios` | Completed | Added `ProviderDataset.FinancialRatios` + `IFinancialRatioProvider` interface; `CodalRatioRow` query model; `ICodalDbQueryExecutor.QueryFinancialRatiosAsync` (filtered by `CompanyId` + curated `ItemID` set — no full-table scan); `CodalDbDataProviderClient` implements `IFinancialRatioProvider`; `FinancialDataSyncProcessor` extended with optional `IFinancialRatioProvider` (handles `FinancialRatios` dataset); `POST /api/v1/admin/data-sync/financial-ratios` endpoint (CodalDb-hardcoded provider name); `CodalDbRatioItemMap` (16 curated `FinancialRatioItems.Id → MetricCode` entries; Current-ratio duplicate 65 vs 41066 resolved to 65; percentage encoding documented as percent-scale); 16 `FinancialMetricDefinition` + bilingual aliases added to `PhaseOneFinancialSemanticCatalog` with new `Days` unit; `CodalDbRatioNormalizer` (`ProviderName="CodalDb"`, `Dataset=FinancialRatios`) upserts `DerivedMetricRow`s with `CalculationPolicyVersion="codal-ratio-source-v1"`, canonical variant selection per `(PeriodEnd, PeriodType, ItemId)`, vendor source evidence, symbol resolution from spec-022 rows, idempotent on unique key; never invokes `IFinancialMetricCalculator`; never overwrites engine-calculated metrics. `IFinancialRatioProvider` registered in DI; `CodalDbRatioNormalizer` registered as `IFinancialPayloadNormalizer`. Tests: 8 unit (mapped ratio creates `codal-ratio-source-v1` row, vendor evidence, audited selection, percent-scale stored as-is, distinct policy version doesn't collide with engine rows, idempotent, unmapped ignored, symbol-not-found skips); 2 integration (ROE>15 returns CODAL1 company, current-ratio>1 returns CODAL1 — vendor-precomputed ratios scannable through existing `DerivedMetrics` read path with no scanner-engine changes). `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 251 (+8), Integration 134/139 (5 pre-existing), Architecture 3. |
| 2026-05-31 | `024-codaldb-monthly-activity-ingestion` | Completed | `CodalDbMonthlyReportNormalizer` (`ProviderName="CodalDb"`, `Dataset=MonthlyProductionSales`) deserializes `CodalMonthlyActivityRow[]`, converts Jalali `(Year, Month)` to Gregorian month start/end via `System.Globalization.PersianCalendar` (no custom arithmetic), upserts `NormalizedMonthlyReportRow` (`ExternalReportId=Id`) and one `NormalizedMonthlyReportLineItemRow` per product (`ProductCode=ProductId`, `ProductionQuantity`, `SalesQuantity`, `SalesAmount`). Deferred fields `ProductTitle`/`ProductSaleRate`/`ProductUnit` noted in `WarningsJson`. Zero-amount months retained. Idempotent on `(ProviderName, ExternalReportId)` and `(MonthlyReportId, ProductCode)`. `MonthlySalesMetricInputSource` verified provider-agnostic (sums all line-item `SalesAmount` per report — multi-product CodalDB months are correctly aggregated with no engine changes). Registered in DI. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-restore` passed: Unit 243 (+7), Integration 132/137 (5 pre-existing), Architecture 3. |
| 2026-05-31 | `029-financial-statement-schema-fix` | Completed | Added a `StatementType` column to `NormalizedFinancialStatementRow` (uses the existing `FinancialStatementType` enum: `IncomeStatement`/`BalanceSheet`/`CashFlow`), changed the unique key from `(ProviderName, ExternalStatementId)` to `(ProviderName, ExternalStatementId, StatementType)` with a supporting non-unique index on `(ProviderName, StatementType)`, and fixed all three financial-statement normalizers consistently: `CyclicalWavesFinancialStatementNormalizer` (replaced `statement.PeriodType = "IncomeStatement"` with `StatementType = "IncomeStatement"` + `PeriodType = "ThreeMonths"`); `CodalDbFinancialStatementNormalizer` (drops the `:INC`/`:BS` suffix on `ExternalStatementId`, passes `FinancialStatementType` through `UpsertStatementRowAsync` so both rows share the source `StmtId` and the new column disambiguates them); `FinancialStatementPayloadNormalizer` (configured HTTP provider: added required `StatementType` field to the `StatementDocument` JSON contract + `Enum.TryParse` validation for both `Period` and `StatementType`, throws `FinancialProviderException(InvalidResponse, …)` on unknown values so a future HTTP adapter cannot reintroduce the spec-020 class of bug). Migration strategy was clean-slate truncate (per scope review): the new migration `20260531154529_AddStatementTypeAndFixUniqueKey` truncates `FinancialStatementLineItems`/`FinancialStatements`/`DerivedMetrics`/`MetricRecalculationRequests`/`MonthlyReportLineItems`/`MonthlyReports`/`ProviderRawPayloads` and resets `CodalDbSyncStates` before adding the NOT NULL column and new indexes — avoids fragile backfill SQL and avoids invalidating `DerivedMetrics.SourceEvidenceJson` `documentId` references. Operator runs `POST /api/v1/admin/codaldb/full-sync` post-migration to repopulate; migration `Down()` reverses the schema only (truncated data not restored — documented in XML doc comment). Tests added: 4 new unit (3 `ConfiguredFinancialProviderNormalizerTests` for valid payload + invalid `Period` + invalid `StatementType`; 1 `CyclicalWavesNormalizerTests` regression guard verifying `StatementType="IncomeStatement"` + `PeriodType="ThreeMonths"`); 3 new integration (`FinancialStatementSchemaTests`: income+balance with same external id permitted, EF metadata exposes the correct unique index triple and the old two-column index is gone, two providers sharing the same external id permitted). Existing `CodalDbFinancialStatementNormalizerTests` reworked to filter by `StatementType` instead of `ExternalStatementId.EndsWith(":INC"/":BS")` and to assert the suffix is gone. `dotnet test src/backend/FinancialCopilot.sln --configuration Release` totals: **Unit 288 (+4), Architecture 3, Integration 163 (+3) — all spec 029 tests pass.** The same 5 pre-existing `FinancialDataIngestionTests`/`DerivedMetricPersistenceTests` failures from spec 021's completion log persist and are unrelated. **Note on the schema-uniqueness test:** EF Core in-memory does not enforce unique-index violations at SaveChanges time, so the test asserts on EF metadata (`entityType.GetIndexes()`) rather than runtime rejection. PostgreSQL enforces the `UNIQUE` constraint in production via the migration. Cash-flow line items and balance-sheet point-in-time semantics remain out of scope per the spec. Operator runbook note: the Worker process MUST be stopped before applying this migration to avoid racing the truncate cascade; restart afterward and trigger a fresh CodalDb full-sync. |
| 2026-05-31 | `028-missing-answer-feedback-pipeline` | Completed | Added the missing-answer feedback pipeline as a fire-and-forget Phase 1 capability. Domain: `MissingAnswerFeedbackClassification` enum (`MetricGap`, `CalculationGap`, `DataCoverageGap`, `DataQualityGap`, `ParserLimitation`, `UnknownGap`) + immutable `MissingAnswerFeedback` record (no framework deps; PII-free, opaque `ActorId`). Application: `MissingAnswerFeedbackRequest`, `IMissingAnswerFeedbackCollector` (must-not-throw contract), `IMissingAnswerFeedbackRepository` (`UpsertAsync`/`QueryAsync`/`GetCountByClassificationAsync`), and a pure `MissingAnswerFeedbackClassifier` that decides the classification from `(MetricRegistered, DerivedMetricRowCountForMetric, TotalSymbolCount, MatchedSymbolCount, CoverageThreshold=0.5)` and returns null when the answer is healthy (no feedback emitted). Persistence: `MissingAnswerFeedbackRow` + EF config on `FinancialIngestionDbContext` (piggyback rather than introducing a new context per spec note) with the coalesce unique key `(ActorId, QueryHashSha256, Classification, DateBucket)` + supporting indexes on ActorId/Classification/SubmittedAt/DateBucket/RequestedMetricCode; EF migration `AddMissingAnswerFeedbackTable` (table + 6 indexes including the unique coalesce key); model snapshot updated. Implementations: `EfCoreMissingAnswerFeedbackRepository` upserts by coalesce key (increments `FrequencyCount` instead of inserting a duplicate row within the same `DateBucket`), serves paginated queries with multi-filter support and a classification-sum summary; `NoOpMissingAnswerFeedbackCollector` (default — zero overhead when disabled); `AsyncFireAndForgetMissingAnswerFeedbackCollector` (production wrapper that uses `Task.Run` + a fresh `IServiceScopeFactory` scope so a fire-and-forget write is never tied to a request-scoped DbContext, swallowing exceptions with structured warning logs); SHA-256 helper for the query hash and bounded truncation for `QueryText` (500) and `Context` (2000). Scanner integration: `EfCoreScannerExecutionService` now takes `IFinancialMetricRegistry` + `IMissingAnswerFeedbackCollector`, classifies the result post-rank (and on the no-symbol/no-condition short-circuit path) using the primary condition's `MetricCode`, looks up `DerivedMetrics` row count for that code, and calls `collector.CollectAsync` with a JSON-serialized condition context. A defensive `try/catch` wraps the entire feedback block so a collector failure can never alter the scanner response — the collector contract already forbids throwing, but the scanner enforces it as a second safety net. `ScannerExecutionRequest` extended with optional `ActorId`/`QueryText` (back-compat default null; missing actor disables feedback for that call). `AiQueryOrchestrationService` populates both fields from the existing `AiQueryRequest` so the facade flow automatically gets feedback for every scanner-routed message. Configuration: `MissingAnswerFeedbackOptions` (section `"MissingAnswerFeedback"`) with `Enabled` default `false`; DI factory chooses no-op vs. async fire-and-forget collector based on the flag. Admin API: `GET /api/v1/admin/missing-answer-feedback` (filters `dateFrom`/`dateTo`/`classification`/`metricCode`/`actorId`/`skip`/`take` with classification validation 400) and `GET /api/v1/admin/missing-answer-feedback/summary` (counts by classification + total) — both under the existing `DataAdmin` policy and rate limiter. Tests: 7 new unit tests (`MissingAnswerFeedbackClassifierTests` covering MetricGap / CalculationGap / DataCoverageGap-on-zero-match / sparse-coverage / healthy-no-feedback / empty-universe / custom-threshold), 6 integration tests for the repository (`MissingAnswerFeedbackTests`: new entry insert, coalesce same key increments frequency, different classification keeps separate rows, query filters by actor/classification, count-by-classification sums frequency, no-op collector silently accepts without persisting), 6 integration tests for scanner+feedback (`ScannerExecutionWithFeedbackTests`: empty result with unregistered metric → MetricGap, empty with registered but no data → CalculationGap, sparse 2/10 match → DataCoverageGap, healthy ≥50% match → no feedback, throwing collector does not break scanner response, missing ActorId/QueryText → no collector invocation), and 6 new integration tests for the admin endpoints (returns items, classification filter, invalid classification 400, summary aggregation, non-admin 403, API-client 403). `dotnet test src/backend/FinancialCopilot.sln --configuration Release` totals: **Unit 284 (+7), Architecture 3, Integration 160 (+18) — all spec 028 tests pass.** The same 5 pre-existing `FinancialDataIngestionTests`/`DerivedMetricPersistenceTests` failures noted in spec 021's completion log persist on `develop` (commit `7a13b5a` provider-name rename); they continue to fail after this story but are not caused by it. **Deferred (explicit, per spec scope):** parser/clarification-incomplete feedback (`LlmScannerQueryParser` → `ParserLimitation` for abandoned clarifications) requires conversation-state tracking that is out of Phase 1 scope and is documented as deferred; automatic catalog expansion, ML retraining pipeline, and user-facing "we don't have this data" messages are explicitly out of scope per the user story. |
| 2026-05-31 | `027-codaldb-scheduled-sync-and-recalculation` | Completed | Closed the recalculation gap with a provider-agnostic `MetricRecalculationProcessor` (Infrastructure ingestion module) that drains `MetricRecalculationRequests` where `ProcessedAt IS NULL`, resolves affected symbols from `(ProviderName, ExternalCompanyId)`, resolves dependent metrics by intersecting registered `FinancialMetricDefinition.Dependencies` with a data-driven `ProviderDataset → source MetricCode` map (FinancialStatements: NET_PROFIT, REVENUE, GROSS_PROFIT, OPERATING_PROFIT, EPS, TOTAL_EQUITY, FINANCE_COSTS, INCOME_TAX; MonthlyProductionSales: MONTHLY_SALES), filters to metrics with a registered `IFinancialMetricCalculator`, unions inputs across all dependencies via `INormalizedMetricInputReader`, picks distinct closed periods that match the metric's `SupportedPeriodTypes`, selects the active `CalculationPolicyVersion` from `IMetricCalculationPolicyProvider.GetPolicies`, and invokes the existing `DerivedMetricRecalculationCommand.ExecuteAsync` — calculation stays in the `006` engine (one source of truth). Bounded batch + idempotent via `DerivedMetrics` unique key + per-row/per-metric failure isolation + `LastError`/`AttemptCount`/`LastAttemptAt` diagnostics. Added `MetricRecalculationRequestRow.ProcessedAt/AttemptCount/LastAttemptAt/LastError` + EF migration `AddRecalculationOutboxAndCodalSyncState` (also adds `CodalDbSyncStates` table). `DerivedMetricRecalculationWorker : BackgroundService` with `PeriodicTimer(IntervalSeconds)` + `DerivedMetricRecalculationOptions` (mirrors `BillingMaintenanceOptions`/`Worker`/`BillingOutboxProcessor`); registered in `FinancialCopilot.Worker` host alongside the existing Billing/DataSync/Feature workers — benefits CyclicalWaves and CodalDb alike. Added `CodalDbScheduledSyncService` (mirrors `CyclicalWavesFullSyncService`): records run start, queries changed company ids via new `ICodalDbQueryExecutor.QueryChangedCompanyIdsAsync(since)` (UNION across Companies/Statements/MonthlyActivity/FinancialRatios `ModifiedDateTime > @since`, full-reload when `since` is null), enqueues one `DataSyncRequest(Symbols)` plus three per-company `DataSyncRequest`s (FinancialStatements/MonthlyProductionSales/FinancialRatios) with `ProviderName="CodalDb"` via `IDataSyncRequestPublisher` and bounded parallelism (`CodalDbProviderOptions.MaxReadParallelism`), advances the watermark from `QueryMaxModifiedDateTimeAsync` only when every company succeeded (per-company failure isolation). `ICodalDbSyncStateStore`/`EfCoreCodalDbSyncStateStore` persists per-dataset watermark + run timestamps in `CodalDbSyncStates`. Added DataAdmin admin endpoints `POST /api/v1/admin/codaldb/full-sync` and `…/incremental-sync` returning `AdminCodalDbSyncResponse`. `dotnet test src/backend/FinancialCopilot.sln --configuration Release --no-build` results: **Unit 277 (+11), Architecture 3, Integration 142+18 covering spec 027 — all newly-added tests pass.** Tests added: `MetricRecalculationProcessorTests` (6 unit: pending → DerivedMetrics written; idempotent re-processing; bounded batch; Symbols dataset no-op; failure isolation; unregistered metric not computed), `CodalDbScheduledSyncServiceTests` (5 unit: only changed companies enqueued incrementally; full-reload ignores watermark; watermark advances on full success; no-change run leaves watermark unchanged; per-company publisher failure isolated and blocks watermark advance), `MetricRecalculationOutboxTests` (1 integration: pending request → precomputed `NET_PROFIT_GROWTH_YOY=100%` row readable via the same precompute-and-store contract the scanner consumes), and `AdminDataOperationsEndpointTests` extensions (4 integration: DataAdmin full-sync + incremental-sync invocation modes; non-admin and API-client return 403). **Pre-existing failures unrelated to this story:** the same 5 `FinancialDataIngestionTests`/`DerivedMetricPersistenceTests` failures noted in spec 021's completion log persist on `develop` (commit `7a13b5a` renamed `MockFinancialDataProvider.ProviderName`, orphaning its payloads from the configured-provider normalizers); they continue to fail after this story but are not caused by it. **Deferred (explicit, per spec scope):** timer-based nightly `CodalDbNightlySyncWorker` is not registered — the admin endpoints and the existing `DerivedMetricRecalculationWorker` are sufficient for Phase 1, and an external scheduler can hit the endpoints; live SQL paths in `SqlCodalDbQueryExecutor.QueryChangedCompanyIdsAsync`/`QueryMaxModifiedDateTimeAsync` are compile-checked only (no `CODALDB_INTEGRATION`-gated live smoke). |
| 2026-05-30 | `023-codaldb-financial-statement-ingestion` | Completed | Added Domain: 10 new source `MetricCode` definitions to `PhaseOneFinancialSemanticCatalog` (REVENUE, TOTAL_REVENUE, GROSS_PROFIT, OPERATING_PROFIT, EPS, EPS_CONSOLIDATED, FINANCE_COSTS, INCOME_TAX, TOTAL_EQUITY, CAPITAL) with English + Persian aliases and multi-period support (ThreeMonths/SixMonths/NineMonths/TwelveMonths); added `PerShare` MetricUnit for EPS metrics. `CodalDbProviderOptions.PreferConsolidatedStatements` (default: true). Infrastructure: `CodalDbStatementItemMaps` (governed `ItemId→MetricCode` static dictionaries, 9 income + 2 balance entries — data-driven, no code branches); `CodalDbStatementSelectionPolicy` (groups by `(PeriodEnd.Date, PeriodType)`, selects canonical variant: audited > latest representment > consolidated/parent by config, stable StmtId tie-break); `CodalDbFiscalPeriodMapper` (maps `FiscalYearEnd`/`PeriodEnd`/`PeriodType` byte to `FiscalPeriodType` enum name + absolute `PeriodStart = FiscalYearEnd.AddYears(-1).AddDays(1)`, retains Jalali strings as evidence, no estimation/StaleData); `CodalDbFinancialStatementNormalizer` (ProviderName="CodalDb", Dataset=FinancialStatements; emits one `:INC` + one `:BS` row per selected statement sharing fiscal period window; `PeriodType` field = `FiscalPeriodType` enum name for `Enum.Parse<FiscalPeriodType>` round-trip compatibility with `NetProfitMetricInputSource`; selection evidence + assumed scale + Jalali dates in `WarningsJson`; idempotent on `(ProviderName, ExternalStatementId)` + `(StatementId, MetricCode)`). Registered `CodalDbFinancialStatementNormalizer` in DI. Verified `NetProfitMetricInputSource` is provider-agnostic (filters only by `MetricCode`, not by `ProviderName`). Tests: 6 unit (selection policy: audited>unaudited, representment, consolidated/parent, multi-period), 4 unit (period mapper: 3/6/12-month, Jalali retained), 9 unit (normalizer: income+balance rows, unmapped ignored, NET_PROFIT mapping, equity, idempotency, evidence in WarningsJson, multi-period, FiscalPeriodType enum name, empty balance). `dotnet test`: Unit 236 (+19), Architecture 3, Integration 132/137 (5 pre-existing). Build clean. |

## Agent Handoff Rule

At the start of an implementation turn, the agent must:

1. Read this checklist, `specs/README.md`, and the selected story/tasks.
2. Review current code and existing uncommitted changes before implementation.
3. Update only the selected item to `[~]` while it is actively being worked on.
4. Implement and verify the story.
5. Change `[~]` to `[x]` and add completion evidence only after meeting the completion gate.
